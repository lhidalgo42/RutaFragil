# DECISIONS.md — Ruta Frágil

**D98, decisión del dueño (2026-09-17):** que la carga empuje a la tripulación
es juego. La [revisión 06](docs/revisiones/M2-GATE_revision_06.md) documenta la
expulsión; la resolución del dueño en
[D98 y D95 v5](docs/planes/M2-GATE_plan.md#4-decisiones-de-esta-tarea) descarta
la inmunidad propuesta por el revisor: se mantienen réplicas FREE cinemáticas
y colisión tripulante–carga. No se implementa la opción B. Se autoriza corregir
la pérdida de inercia aérea: conservar la velocidad acarreada del último tick
con apoyo y sumar el paseo; al aterrizar el motor retoma el acarreo.
Se mide antes/después el salto a 80 km/h de pie y caminando, sobre baches,
el empujón de caja lenta y el control sin carga con presión −0,5.
El límite de 0,30 m en los saltos en recta se aplica al residuo respecto al
paseo esperado, según la aclaración del
[prompt de ronda 6](docs/planes/M2-GATE_ronda6_prompt_codex.md), no a toda la
distancia recorrida durante el vuelo. Se autoriza el trabajo; no se acredita
aún su resultado.

Se acepta y reporta la asimetría actual: la carga rígida no desplaza a la
tripulante del anfitrión como lo hace la réplica cinemática con la del cliente.
El empujón simétrico en anfitrión y single queda en M3/BACKLOG. Los empujones
por correcciones de réplica también se cuentan; solo si el dueño los siente
injustos se medirá un tope de velocidad de corrección relativo al bus.
No se suprime la colisión ni se cambia bus, suspensión o tuning.

**D95 v5 / D96, vigentes desde ronda 6 (2026-09-17):** primera condición:
cero ticks fuera de `BusInterior.is_inside_local` en ambas tripulantes locales.
El apoyo es descriptivo; se retira el umbral del 95 %. Una pérdida de apoyo
con contacto de carga en los tres ticks anteriores cuenta como empujón:
se reportan número, recuperación, duración y desplazamiento local máximos;
ninguno puede durar más de 45 ticks. El p99 cliente ≤1,5× anfitrión de la misma
corrida se calcula solo con apoyo y sin contacto de carga en esos tres ticks,
y se juzga solo con al menos diez ciclos confirmados por instancia.
Los percentiles sin filtrar se conservan como descriptivos. Continúan el
observador postnodos 1000, penetración local máxima 5 cm y rachas ≤3 ticks
por encima de 3 cm, errores remotos con interpretación de latencia, FPS p1
con ventana/VSync off/max_fps=0 y los demás controles del plan.

D96 conserva **dos fallos válidos**, corridas 2 y 4; el tercero no se ha
consumido. Sus CSV dan `hull_exit_ticks` anfitrión/cliente **0/16.784** y
**0/14.494**, respectivamente. El experimento `r5_final_300` da **0/13.066**
y no cuenta como iteración. El p99 filtrado v5 no se reconstruye con esos
CSV: falta contacto de carga por tick.
D98 autoriza **una sola ronda experimental adicional**. La precondición es
exactamente 300 s, cero salidas, ningún empujón >45 ticks y ≥10 ciclos por
instancia, sobre commit limpio y configuración idénticos al eventual tercer
gate, sin cambios intermedios. Si no cumple, se para y el revisor redacta el
plan B con D98; si la tercera iteración falla, se aplica D96. No hay otra
ronda posterior. El gate humano D97 sigue pendiente.

**Histórico de rondas 4–5:** los umbrales y estados de autorización siguientes
quedan sustituidos por D95 v5/D98; se conserva lo decidido y observado entonces.

**Resultado histórico de ronda 5 (antes de D98):** el experimento300s sobre25c7a72 incumple la
precondición D: apoyo cliente27,333333 %, pérdida final13078ticks y último
tick sin apoyo. Se detiene la experimentación conforme a E; dos fallos
válidos D96, tercero sin consumir. El revisor redactará el planB. El arreglo
de fase se conserva con test; el acarreo explícito se rechaza por romper
cero aire sobre baches. [Evidencia18](docs/evidencia/M2-GATE/18_experiment_300.md).

**D95/D96, ronda 5 (2026-09-17):** [revisión 05](docs/revisiones/M2-GATE_revision_05.md)
introduce un umbral provisional de reposo de 3 cm: la racha cuenta solo
profundidades estrictamente mayores; el máximo permitido sigue en 5 cm.
Histogramas excluyen cuerpos no simulados, conservando diagnóstico separado.
Antes del tercer intento: 300 s completos, misma configuración y commit limpio,
ambos peers con apoyo ≥95 %, último tick apoyado y pérdidas ≤30 ticks.
La propuesta de que esta sea la última ronda de experimentos queda PENDIENTE
DEL DUEÑO en el plan; el ejecutor la aplica como límite provisional conforme
al prompt. Si no hay experimento apto, para; el revisor redacta el plan B.
No se cambia ADR-003 ni se aprueba jugabilidad.

**D95, corrección autorizada por revisión 04 (2026-09-16):** primero se juzga
el apoyo de la tripulante local en el bus: menos del 95 % de los ticks falla
por «la tripulante no puede viajar». En ese caso el desplazamiento local se
conserva como dato descriptivo, no se cita ni se juzga como jitter. El cociente
p99 ≤ 1,5× solo se juzga si ambas instancias cumplen la precondición de apoyo.
La penetración se juzga exclusivamente en cuerpos simulados localmente:
tripulante propia no sentada y carga FREE dinámica con autoridad local;
réplicas, HELD y STRAPPED quedan en diagnóstico separado. Rendimiento solo
evaluable con ventana, VSync desactivado y `Engine.max_fps=0`; no se borran los
FPS históricos, se retira su uso como motivo de fallo. Se mantienen punto de
muestreo 1000, diez ciclos por peer, errores remotos y demás criterios.
Los experimentos de reducción se identifican expresamente y no consumen D96.
La corrida 2 conserva su fallo válido por falta de apoyo; sus datos también
registran penetración de las tripulantes locales (0,074849 m/7 ticks y
0,200542 m/58 ticks), discrepancia con «queda un motivo» de la revisión.

**Resolución M2-GATE, 2026-09-16 (no es un ADR nuevo):** g2.1 queda retirado
por [revisión 03](docs/revisiones/M2-GATE_revision_03.md). Se elimina su test;
no se ajusta el umbral ni se registra una ventaja física del punto de escritura.
NetBusSync conservó inicialmente la conexión a physics_frame como elección de implementación.
Esa señal se emite antes de procesar los nodos de física (Godot 4.7.2).
D95 exige el mismo observador independiente para anfitrión, cliente y todas
las filas: `_physics_process`, `process_physics_priority=1000` y
`process_priority=1000`, después de los escritores y del movimiento. El
observador solo lee; cada JSON y cada tabla declaran ese punto. En 4.7 la
prioridad que ordena callbacks físicos es process_physics_priority, no
process_priority. Las cifras históricas de otras fases no son comparables.
D91–D98 viven en el plan; contrato en evidencia `00_phase0_contract.md` y
trazas preservadas en `12_orden_de_escritura.md`. El gate humano sigue pendiente.

**Integración M2-GATE, 2026-09-16:** la corrida 1 fue inválida (cliente 5 ciclos,
expulsado). El receptor reiniciado por llegada produjo saltos de 0/0,6167 m
en una trayectoria analítica de 18,5 m/s con intervalos de entrega 1/3 ticks.
Se sustituye por cola de tiempos del origen y reloj continuo, con margen
de dos snapshots (66,7 ms); el error remoto conserva esa latencia, sin compensarla.
Los preflights 03/04 mantienen el observador a 1000 y comparan la escritura:
la variante de nodo a -100 mantuvo el cliente dentro durante 45 s, pero no
cumplió D95 ni los ciclos. Se elige esa variante para la siguiente corrida;
no se restablece g2.1 ni se afirma una ley del motor. Carga escribe a -90.
La validación de alcance reconstruye la mano del peer en el marco del bus de
su snapshot; el render remoto sigue recibiendo transformadas de mundo.
Fuera del bus la mano conserva coordenadas de mundo. No cambia el alcance.

Registro de decisiones del proyecto. Jerarquía: maestro v0.2 > briefing v0.2 > plan de tarea (`docs/planes/`).

## ADR-000..009

Resúmenes citados del maestro (`docs/RUTA_FRAGIL_documento_maestro_v0.2_godot.md`); si un resumen difiere del maestro, manda el maestro.

- **ADR-000** — **Engine: Godot 4.7.2 stable, fijado hasta la demo** (maestro §15).
  - *Citado del maestro:* Godot no tiene LTS en 4.x: cada rama estable se mantiene al menos hasta que la siguiente recibe su primer parche, con cadencia de ~6 meses. Regla: se adoptan solo parches de 4.7.x; el salto a 4.8 se evalúa entre milestones, con backup y tarea dedicada; **nunca dev/beta**.
  - *Añadido por el proyecto (no está en el maestro):* versión pineada por hash `4.7.2.stable.official.ed1daf0bf`; la hace cumplir `test_engine_is_pinned_to_4_7_2` (`tests/smoke_test.gd`) en ambas máquinas.
- **ADR-001** — **Red** (maestro §15): API de alto nivel de Godot (`SceneMultiplayer`, `MultiplayerSpawner`, `MultiplayerSynchronizer`, RPCs). Transporte: **ENet** en desarrollo/LAN y **GodotSteam MultiplayerPeer** (relay de Steam, NAT y cifrado incluidos) en release, ambos detrás de una interfaz `NetworkBackend` propia para cambiar de uno a otro sin tocar gameplay. Solo el MultiplayerPeer oficial de GodotSteam (godotsteam.com): varios forks comunitarios están archivados. Interpolación propia en clientes (buffer 100 ms); netfox se evalúa solo si la interpolación propia no basta.
- **ADR-002** — **Voz: Steam Voice vía GodotSteam** (maestro §15): captura `Steam.getVoice` → red → `AudioStreamGenerator` sobre un `AudioStreamPlayer3D` por jugador (= proximidad). Regresión registrada respecto a v0.1: no existe Vivox para Godot; la calidad de Steam Voice es menor y la proximidad es artesanal. Alternativa: addon godot-voip. Es el costo #1 del cambio de motor y se acepta conscientemente.
- **ADR-003** — **Interior caminable en movimiento** (maestro §16; EL riesgo técnico). Opción A (elegida, pragmática): jugadores `CharacterBody3D` sobre el bus como plataforma móvil (Jolt soporta plataformas cinemáticas; heredan velocidad de la plataforma); paquetes **amarrados** = hijos congelados del bus, sin física; **en mano** = cinemáticos; **sueltos** = `RigidBody3D` reales en espacio de mundo con Jolt (si hace falta, tick de física a 120 Hz solo en ruta); fricción alta en el piso y "amortiguador" de velocidad relativa. Opción B (plan B): re-parentar el interior al espacio local del bus con pseudo-fuerzas (`−m·a_bus`) y sincronizar transforms locales. **Gate M2 duro:** caminar, agarrar y amarrar dentro del bus a 80 km/h sobre baches, 60 fps en la máquina de referencia, sin jitter ni atravesamientos durante 5 minutos; si A falla 3 iteraciones → B antes de construir nada encima.
- **ADR-004** — **Cámara: primera persona** (maestro §9.1): primera persona con manos y cuerpo visibles; el conductor ve desde la cabina y puede alternar a una cámara exterior de persecución. Razón: el interior es estrecho — una cámara en tercera persona colisiona con paredes y estantes; el género (Lethal Company, R.E.P.O., Content Warning) valida la primera persona para la comedia por voz. Revisitar solo con evidencia de playtest.
- **ADR-005** — **Lenguaje: GDScript con tipado estático** (maestro §15). Razón: un solo toolchain en Mac y Windows (sin .NET SDK), addons clave en GDScript, iteración más rápida para el agente. Puertas abiertas: C# o GDExtension solo si el profiler demuestra un cuello de botella en M2/M4.
- **ADR-006** — **Autoridad de red** (maestro §17): la autoridad de física del **bus** es del **peer del conductor** mientras está sentado (`set_multiplayer_authority` al sentarse; vuelve al host al pararse). **Paquetes sueltos** dentro del bus: misma autoridad que el bus; **en mano**: autoridad de quien los sostiene; **amarrados**: sin física, replicados como hijos del bus. **Personajes:** autoridad del propio jugador sobre su movimiento (co-op, sin anticheat); los demás interpolan. **Host:** economía, contratos, spawns, estado de sistemas, resultados de `CoopInteractable`, muerte/revivir, cronómetros. Tasas: bus 30 Hz; paquetes sueltos 20 Hz snapshot + interpolación; escalares vía `MultiplayerSynchronizer`; eventos discretos vía RPC confiable. Reconexión entre contratos; a mitad de ruta el reconectado aparece en la camilla.
- **ADR-007** — **Física del bus** (maestro §18): `RigidBody3D` + suspensión por raycast (4 ruedas, resorte/amortiguador por script) en vez de `VehicleBody3D`: control total del feel arcade, comportamiento predecible con Jolt y con la autoridad transferible. Volcadura detectada por producto punto del eje "arriba" del bus.
- **ADR-008** — **Modelo de guardado** (maestro §12.2, nuevo en v0.2): la **empresa** (dinero, herramientas, desbloqueos, cosméticos del bus, cuota) pertenece al **host** — se juega sobre la empresa del host, como la nave en Lethal Company. Cada jugador conserva su progreso **personal** (cosméticos de personaje, estadísticas) en cualquier sesión. 3 slots de empresa por cuenta. Guardado automático al cerrar cada contrato. Si el host se va, la sesión termina (sin migración de host en v1, §12.1).
- **ADR-009** — Puente MCP: **ninguno en M0**. **No proviene del maestro: es la decisión D30 del plan v1.1.** Disparador de revisión: la primera milestone con iteración visual (M2/M8) o cuando el bug #988 de Godot AI lleve ≥4 semanas cerrado (ver D30).

## D1–D29

Copia del D-log del briefing (`docs/CONTEXTO_RUTA_FRAGIL_v0.2.md` §3); si difiere, manda el briefing (y sobre él, el maestro).

| # | Decisión | Porqué |
|---|---|---|
| D1 (act. D18) | MVP = Ciudad (tutorial), Cerro, **Pantano**. Submarino y Volcán se diseñan ahora, se construyen en EA. | 5 biomas al lanzamiento es alcance suicida; los éxitos del género lanzaron chicos. |
| D2 | Diferenciador: bus caminable + paquetes con cuidados activos + acciones sincronizadas; toda amenaza exterior genera ≥1 tarea interior. | El nicho "manejar y entregar co-op" está saturado; el hook es la tripulación. |
| D3 | ~~Unity 6.3 LTS~~ **SUPERSEDIDA por D17.** | — |
| D4 | ~~NGO + Facepunch~~ **SUPERSEDIDA por D21.** | — |
| D5 | ~~Vivox~~ **SUPERSEDIDA por D22.** | — |
| D6 (act.) | ADR-003 interior caminable, versión Godot: amarrado = hijo congelado, en mano = cinemático, suelto = RigidBody3D Jolt; jugadores CharacterBody3D sobre plataforma móvil. Gate duro M2, plan B = espacio local. | Sigue siendo EL riesgo técnico; se valida antes de construir encima. |
| D7 | Revivir en camilla del bus + botiquín + recuperar el cuerpo. Estatuas tipo PEAK descartadas. | Base móvil; separar el revivir del vehículo rompe el pilar 2. |
| D8 (act.) | Bus hundido, ahogado **o atascado**: rescate manual → grúa NPC pagada → pérdida total (run fallida, conserva 25%). | El fracaso es gameplay primero y castigo después. |
| D9 | Recursos del bus: Combustible / Integridad / Desgaste separados + Blindaje consumible. | Dos relojes crean decisiones distintas. |
| D10 | Sin tuning mecánico ni deformación visual; cosméticos sí. | Alcance; el estado se comunica con humo/sonido/paneles. |
| D11 (act.) | Moddabilidad día 1 en Godot: scripts sin cifrar, contenido en Resources+JSON con carga desde `user://mods/` y packs `.pck`, `max_players` configurable (default 4, probado a 8, bus con 6 posiciones). | La puerta a mods se deja abierta con arquitectura. |
| D12 | Economía: contratos → pago por integridad y tiempo; cuota cada 3 contratos; derrota conserva 25%. Tabla de tuning v0 en §8.1 del maestro. | Sin presión no hay tensión; sin fail-forward no hay reintento. |
| D13 (act.) | Entorno dual: misma versión exacta de Godot (4.7.2) en Mac y Windows; Git+LFS para binarios; `.godot/` no se versiona; escenas/recursos en texto; Forward+ (MoltenVK en Mac) → shaders se verifican en ambas; máquina débil = referencia; gate M4 entre ambas máquinas con 2 cuentas Steam. | Dos máquinas sin disciplina de versionado corrompen el proyecto. |
| D14 | Windows primero; macOS post-lanzamiento. | La Mac vale para desarrollo y pruebas de red, no como target v1. |
| D15 (act. D19) | Workflow IA: una tarea por sesión, reglas R1–R14, plan antes de tocar archivos, tests headless obligatorios, gates humanos M2/M4/M8. | "95% IA" solo funciona con verificación humana disciplinada. |
| D16 (24/08/2026) — CERRADA | Style board del bus: interior canónico (`Referencias/bus_interior_canonico_v1.png`) y exterior canónico (`bus_exterior_canonico_v1.png`, guardar tras limpiar texto por inpaint). Bus largo, livrea amarillo/rojo, techo crema, ventanas corridas. La fase de concept del vehículo terminó. | Iterar sin congelar referencia quemaba créditos. |
| **D17 (07/09/2026)** | **Motor: Godot 4.7.2 stable** (ADR-000). Solo parches 4.7.x; salto a 4.8 entre milestones; nunca dev/beta. | **Razón declarada por el dueño:** que la IA pueda desarrollar y probar sin un humano en cada iteración. Se acepta con frontera explícita (§0.8 del maestro): autonomía total de iteración (headless, tests, red multi-instancia, export a staging) y supervisión humana concentrada en los gates — no eliminada. Costos aceptados: sin Vivox, menos precedentes de co-op 3D con física, addons pequeños o archivados. |
| **D18 (07/09/2026)** | Progresión de biomas: Ciudad → Cerro → Pantano → Submarino → Volcán. Eliminados: Islas, Bosque, Desierto, Ártico, Viento (sus mecánicas se absorben: lianas en Pantano, calor en Volcán). | Cadena coherente de dificultad; Pantano valida flotación, achique y aire antes de Submarino. |
| **D19 (07/09/2026)** | Modelo "95% IA / humano validador": el humano no revisa código, valida comportamiento con checklist PASA/FALLA/NO PROBADO por gate (§0.7 del maestro). | Sin definir la validación, "el humano valida" es una frase, no un rol. |
| **D20 (07/09/2026)** | Cámara en primera persona con manos/cuerpo visibles; conductor con cámara exterior opcional (ADR-004). | Interior estrecho; el género valida primera persona para comedia por voz. |
| **D21 (07/09/2026)** | Red: API de alto nivel de Godot; ENet en desarrollo/LAN, GodotSteam MultiplayerPeer oficial en release, detrás de `NetworkBackend` propio. Autoridad del bus al peer del **conductor** mientras conduce; paquetes sueltos con el bus; en mano con quien los sostiene; host dueño de economía/contratos/sistemas (ADR-001/006). | 100 ms de latencia en el volante arruina el manejo; dueño-autoridad es el patrón del género. |
| **D22 (07/09/2026)** | Voz: Steam Voice vía GodotSteam con proximidad artesanal (AudioStreamPlayer3D por jugador) + radio del bus. Alternativa: godot-voip. Regresión respecto a Vivox aceptada conscientemente. | No existe Vivox para Godot; es el costo #1 del cambio de motor. |
| **D23 (07/09/2026)** | GDScript con tipado estático (ADR-005); física Jolt (default Godot); bus RigidBody3D con suspensión por raycast (ADR-007); renderer Forward+. Sin C#/GDExtension sin ADR. | Un solo toolchain en ambas máquinas; el agente itera más rápido; control total del feel arcade. |
| **D24 (07/09/2026)** | Guardado: la **empresa** pertenece al host (3 slots); progreso personal por jugador; si el host se va, la sesión termina (sin migración de host en v1) (ADR-008). | Modelo Lethal Company; la migración de host es un pozo de complejidad fuera de alcance. |
| **D25 (07/09/2026)** | `CoopInteractable` (concurrente / sincronizado / relevo) como sistema núcleo: toda tarea "épica" se construye sobre él (enderezar bus, winche, flotadores, sellar escotilla, revivir en pareja). | El dueño pidió acciones sincronizadas; un sistema único evita código especial por caso. |
| **D26 (07/09/2026)** | Alcance de validación del dueño: físicas, funcionalidad del bus y de los paquetes, assets y texturas generados. Diálogos de NPC y sonido: al final (M8). **"Concreto" = jugable, no = con arte:** las físicas y el bus se validan sobre greybox en M1–M3, no sobre assets finales. | Validar físicas sobre arte terminado es el orden más caro posible; las primeras validaciones del dueño caen en M1 (conducir) y M3 (paquetes). |
| **D27 (07/09/2026)** | Assets 3D, texturas, SFX/música y voces gibberish se generan con ComfyUI, siempre vía Blender y el checklist §10.1 (colisión autorada, nunca el mesh generado). **Hunyuan3D 2.x prohibido para assets publicados** (licencia excluye UE, Reino Unido y Corea del Sur; Steam es global); Trellis (MIT) por defecto; toda licencia de modelo se copia en `CREDITS.md`; se declara IA en Steam. No existe sistema de diálogo: "diálogos" = tablas de líneas de reacción + texto de contratos, en M8. | Un modelo con territorio restringido convierte cada asset en un pasivo legal; y los meshes generados no son game-ready sin limpieza. |
| **D28 (07/09/2026)** | Combustible como loop de recolección física: tanque chico (40 L) que no alcanza la ruta; bidones de 10 L como props (se agarran, amarran, caen, ocupan volumen de carga); zonas de combustible gratis en ramas de una ruta **lineal** (surtidor, barriles); estaciones raras y pagadas; vertido por toma exterior con derrame en marcha. Retuning: consumo 4 L·km, rutas 6–10 km. **Proceso para mecánicas futuras:** entran por D-log con test de pilares, milestone destino y retuning que exigen; nada entra a un milestone en curso. | Con el tanque de 100 L de la v0 el sistema sobraba; la tensión real es bidones vs paquetes en el mismo espacio de carga. |
| **D29 (07/09/2026)** | HUD por capas: diegético (tablero, bidón, stickers) → siempre visible (retícula, voz, objeto en manos, cronómetro, entregas) → contextual (combustible <20%, aire <70%, compañero derribado, sistema fallando, zona de combustible cercana, marcador de entrega) → panel Tab con el estado completo. Nada permanente que ya esté en el tablero; la capa siempre visible no crece sin D-log. | "Stats en el HUD" sin capas destruye la lectura diegética y el pilar 3; con capas, la tripulación tiene los datos para coordinarse sin pantalla de Excel. |

## D30–D71 (D30–D39 fijadas en el plan M0-T0.1, tabla 4.1; D40 añadida en la ronda 2; D41 añadida en la ronda 4; D42 añadida en la ronda 4 (dueño); D43–D47 del plan M0-T0.2 §4; D44/D45/D47 precisadas en la ronda 2; D46 en la ronda 3; D48–D52 del plan M0-T0.3 §4; D53–D58 del plan M0-T0.4 §4; D59–D65 del plan M1-T1.1 §4; D66–D70 del plan M2-T2.1 §4; D71 en el paso 0 de M2-T2.1 (r1.1/r1.2 de T1.1); D72–D77 del plan M2-T2.2 §4)

| # | Decisión | Valor | Por qué |
|---|---|---|---|
| D30 | Puente MCP | **Ninguno en M0.** Se documenta como ADR-009 en `DECISIONS.md` con disparador de revisión: primera milestone con iteración visual (M2/M8) o cuando el bug #988 de Godot AI lleve ≥4 semanas cerrado. | Los tres candidatos (Godot AI/hi-godot, godot-mcp/hybridindie, Swallowtail ex godot-mcp-go) exigen editor abierto para todo lo que no sea envolver la CLI; ninguno aporta a headless. Godot AI publicó 4.0.0→4.0.2 el 07/09/2026 con un bug abierto de conexión en Windows 11 + 4.7.2 (#988); Swallowtail se renombró el 07/09; hybridindie tiene 9 estrellas, 1 mantenedor, 20 bugs abiertos. Cada uno agrega `addons/` + runtime externo (Python/Go/Node): choca con R5. Todo lo que T0.1–T0.4 necesitan se hace con archivos de texto + `godot --headless`. |
| D31 | Driver de render Windows | `rendering/rendering_device/driver.windows="d3d12"` | Decisión del dueño 2026-09-08. Proponer corrección de ADR-000/D13 a "Forward+; backend por plataforma: D3D12 en Windows, Metal en macOS" (macOS usa Metal por defecto desde 4.4; "MoltenVK" del maestro ya no es exacto). |
| D32 | gdUnit4 | **v6.2.1**, tag de GitHub `godot-gdunit-labs/gdUnit4` (repo movido desde MikeSchulze), MIT. Solo la carpeta `addons/gdUnit4/` **sin su subcarpeta `test/`** (auto-tests del addon; la distribución oficial la excluye por `export-ignore`). `src/dotnet/GdUnit4CSharpApi.cs` es parte de la distribución y queda inerte en build estándar. Borrado de `test/` autorizado por el dueño el 2026-09-08 por R12 (ejecutado en la ronda 3). | AssetLib sigue en 6.2.0; 6.2.1 arregla un crash del runner CLI (GD-1267). La tabla de compatibilidad llega a Godot 4.7.1; 4.7.2 salió dos días antes que 6.2.1 y no está listado. Godot declara "sin incompatibilidades conocidas" 4.7.1→4.7.2. Único issue registrado con 4.7.2: aviso cosmético con literales `1_000` en tests parametrizados (#1319). Se trata como soportado en la práctica; se re-verifica cuando salga 6.2.2. |
| D33 | Carpeta de tests | `tests/` (§20) + `[gdunit4] settings/test/test_lookup_folder="tests"` | gdUnit4 asume `test`; sin el ajuste "Crear test" del editor los pone en la carpeta equivocada. |
| D34 | Tipado forzado por configuración | `untyped_declaration=2`, `inferred_declaration=2` (error); `unsafe_method_access`, `unsafe_property_access`, `unsafe_cast`, `unsafe_call_argument` = 1 (warn). **No tocar `debug/gdscript/warnings/directory_rules`** (su default excluye `res://addons`). | R3 habla de declaraciones: esas dos claves la cumplen. Si `res://addons` deja de estar excluido con `inferred_declaration≠0`, el plugin de gdUnit4 se niega a cargar ("Loading GdUnit4 Plugin failed"). `treat_warnings_as_errors` **no existe** para GDScript en 4.7 (solo `debug/shader_language/…`). |
| D35 | Ubicación de los .md | `docs/` con `.gdignore`. §20 no lista la carpeta: desvío menor, reportado. Maestro y briefing fueron escritos por el revisor (Claude) desde el texto del chat con autorización del dueño el 2026-09-08 (revisión 01, B1); cada archivo lleva la cabecera "copia reconstruida desde el texto pegado en chat…; si existe el original, reemplazar esta copia por él". (m7: la versión anterior de esta fila decía "se escriben desde el chat" cuando aún eran stubs sin contenido; ahora los archivos tienen el contenido íntegro.) | El maestro llegó por chat, no como archivo. |
| D36 | SVG fuera de LFS | `icon.svg` y cualquier `.svg` en Git normal. Desvío del ejemplo de la documentación de Godot, anotado como comentario en `.gitattributes`. | SVG es texto diffable. En GitHub los objetos LFS son permanentes (borrarlos no libera cuota). |
| D37 | Nombre interno | `application/config/name="RutaFragil"` | El título es provisional (Apéndice C) y el nombre define la ruta `user://`. |
| D38 | Ramas | Commit inicial en `main` con solo metadatos de repo y docs; el proyecto Godot se construye en `m0/t0.1-foundations`; el dueño hace merge tras el gate. | §0.2: el agente nunca en `main`; el primer commit tiene que existir en alguna rama. |
| D39 | Ruta al ejecutable de Godot | Variable de entorno `GODOT_BIN`; si falta, `tools/godot_bin.local` (ignorado por Git); si falta, default por SO: Windows `C:\Godot\4.7.2\Godot_v4.7.2-stable_win64_console.exe`, macOS `/Applications/Godot.app/Contents/MacOS/Godot`. | Mismo nombre que usa gdUnit4. Mac y Windows tendrán rutas distintas. |
| D40 | Flags de depuración en gdUnit4 headless *(añadida en la ronda 2, prompt A.4; no está en el plan v1.1)* | El arnés corre gdUnit4 **sin** `-d` ni `--remote-debug tcp://127.0.0.1:0`: `--headless --path <raíz> -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -c -a res://tests -rd res://reports`. | Con los flags, cada corrida imprimía dos líneas `ERROR` ("remote port number must be between 1 and 65535", "Unable to connect") por el puerto 0, incompatibles con R7(a) (hallazgo m1). El puerto 0 solo neutralizaba el depurador interactivo `debug>` que activa `-d`. Experimento de la ronda 2 (2026-09-08, M0-T0.1): sin los flags, la corrida verde sale limpia (código 0, 3 tests) y una suite sin tipado (`var x = 1`, función sin `-> void`) sigue reportando `SCRIPT ERROR` con backtrace GDScript completo y código 105. |
| D41 | `*.scn`/`*.res` en LFS *(añadida en la ronda 4; antes una nota sin numerar al pie de esta sección)* | Mantener `*.scn`/`*.res` en LFS como red de seguridad por si entran por error (R14 los prohíbe en el proyecto). | Revisión 01 (notas) y ronda 3: la red ya existía; quitarla no protege nada y mantenerla documenta la intención. |
| D42 | Validación en Mac diferida *(añadida en la ronda 4)* | M0-T0.1 se acepta con `run_tests` verde solo en Windows. La Mac vuelve a ser obligatoria antes del gate M4 (red real Mac + Windows, D13). Residuo declarado: el arreglo de BOM POSIX de `run_tests.sh` (m-2.3) solo está probado bajo Git Bash; su prueba real en BSD sed queda para esa corrida. | Decisión del dueño en chat, 2026-09-09; desbloquea el cierre de M0-T0.1 sin la segunda máquina. |
| D43 | Nombre del Resource | `GameConfigData` (clase) + `GameConfig` (autoload, sin `class_name`). El autoload expone `max_players` como propiedad delegada para que la expresión literal de R1, `GameConfig.max_players`, funcione. | El maestro nombra `GameConfig` a la vez como Resource (§19) y como autoload (§20); Godot no permite ambos. **Contradicción reportada**; proponer corrección al maestro §19. |
| D44 | Dónde vive la verdad | Esquema en código (`@export` con **defaults neutros**: 0, 0.0, "", `{}`). Valores solo en datos: `res://data/*.tres` (artefacto autoritativo **para el editor/Inspector**, R2; en caliente gana la última capa; el test de deriva mantiene ambos iguales) y `res://data/*.json` (espejo, formato de modding y edición en caliente). Ambos versionados; dos herramientas headless sincronizan en ambos sentidos. | R2 prohíbe contenido en código: un `TuningTable.new()` tiene `starting_money == 0`, no 500; un test lo fija. El juego solo tiene valores sanos si los datos cargan; el loader reporta en voz alta si faltan (ronda 2, M4: un documento sin ninguna capa aplicada es **error**, no aviso; el autoload emite `push_error` solo si `!is_ok()` y un `push_warning` por cada aviso del reporte). **Nota mt6 (hecho verificado en la ronda 1):** `Dictionary ==` es estricta int/float también en **valores**, no solo en claves; `check_mirror` compara semánticamente vía ida y vuelta JSON; el texto exacto lo fija el test de deriva reforzado (mt6). |
| D45 | Formato, orden y fusión | Sobre (envelope) por documento: `{"tuning": {...}}`, `{"game_config": {...}}`; un archivo de mod puede traer varias secciones. Mods = archivos planos `user://mods/*.json`, orden de bytes (ASCII); ver esquema §5. Orden de capas: `.tres` → `res://data/<doc>.json` → `user://mods/*.json`. Fusión profunda propia (diccionarios anidados clave a clave; **arrays se reemplazan**). Validación por clave contra el esquema: `int` acepta solo `float` integral; `String`/`bool` estrictos; diccionarios tipados validados valor a valor y aplicados con `.assign()`. Un archivo de mod con **cualquier** error se rechaza entero (aviso en el reporte). Clave desconocida: aviso en mods, error en base. Base JSON inválido: capa omitida, `is_ok()` falso, el juego sigue con los valores del `.tres`. Un documento sin **ninguna capa aplicada** (ni `.tres` ni JSON base) es **error** (`is_ok()` falso), no aviso (ronda 2, M4). La API del reporte es `is_ok()`; el `push_error` **del reporte** vive solo en el autoload (`_ready`/`reload()`), que lo emite si `!is_ok()` y además emite un `push_warning` por cada aviso del reporte (los modders ven sus rechazos; los demás `push_error` del código son de rutas de bug). | Predecible para el modder (un archivo aplica o no), seguro ante `set()` silencioso, extensible a colecciones (`{"packages": [...]}`) sin cambiar la API. Carpetas de mod con manifiesto y `.pck` quedan para cuando exista un segundo tipo de contenido (BACKLOG). |
| D46 | Recarga | Explícita: `GameConfig.reload(base_dir: String = "res://data", mods_dir: String = "user://mods") -> DataLoadReport` (los argumentos existen para pruebas; el juego usa los defaults; ronda 3) + señal `reloaded(report)`. Sin vigilante automático de `mtime` en T0.2 (BACKLOG). Prueba a nivel de proceso con `src/tooling/print_game_data.gd`. | "Sin reabrir el editor" se prueba sin editor; el vigilante añade un `Timer` y resolución de 1 s que no aportan al criterio. |
| D47 | Nombres de campos | Ingleses, snake_case, con unidad explícita: `fuel_tank_liters`, `jerrycan_pour_seconds`, `player_walk_speed_mps`, `rent_increase_per_cycle` (0.15), `defeat_keep_fraction` (0.25). El "derrame ∝ velocidad" de §8.1 no tiene número: **no se inventa un campo**; va a BACKLOG para T1.2. **Actualizado en la ronda 2 (md6/mc7 de la revisión 01):** el desgaste del bus por contrato se renombra a `bus_integrity_max_loss_per_contract` — magnitud **positiva** que se resta de `bus_integrity_max` por contrato (§8.1: "−5 al máximo") — y `fuel_consumption_l_per_km` pasa a `float` (4.0): es una tasa, no una cantidad entera. | R9 y legibilidad del esquema para modders. |
| D48 | Bus placeholder (T0.3) | **"Disco con motor":** `RigidBody3D` caja (≈ 8 × 2,2 × 2,5 m, adelante = −Z) con `PhysicsMaterial` de fricción baja, empuje por `apply_central_force` solo con contacto, giro por `apply_torque` en Y, agarre lateral por script (amortigua la velocidad lateral local), tope de velocidad, centro de masa bajado. **Sin suspensión ni ruedas** (eso es T1.1). Masa desde `GameConfig.tuning.bus_mass_kg`; **velocidad máxima y aceleración desde dos campos nuevos de `TuningTable`** (`bus_max_speed_kmh` = 90, `bus_accel_0_60_kmh_s` = 5.0, §4.5); el resto (agarre, torque, umbral de volcadura, fricción, offset del centro de masa) son `@export` de la escena marcados *greybox* que T1.1 elimina. Se reengancha a `GameConfig.reloaded`. | R2: los dos números de diseño que el maestro da en §4.5 son tuning y van a datos; los demás son parámetros de un artefacto desechable, no contenido. Un placeholder sin suspensión no prejuzga el feel de T1.1 ni duplica su trabajo. Alternativas descartadas: implementar ya la suspensión por raycast — adelanta T1.1 sin su gate de feel y mezcla dos tareas (R6); un `AnimatableBody3D` sobre `PathFollow3D` — "recorre el circuito" pero no reacciona a baches ni rampas, y §21 exige "sobre baches". |
| D49 | Circuito y conductor de la demo | `Circuit` (Node3D) con `Marker3D` hijos ordenados como waypoints; `DemoDriver` (Node) que cada tick de física calcula `throttle/steer/brake` hacia el waypoint actual y llama `bus.set_drive(...)`; la matemática pura vive en `DemoSteering` (funciones estáticas, testeables sin física). Señales `waypoint_reached(index)`, `lap_completed(lap)`, `stuck`. Convención: `steer > 0` = giro a la izquierda (yaw positivo, mano derecha de Godot). | Separar matemática (unitaria, rápida) de física (integración, lenta) es lo que permite ≥ 10 tests en menos de 1 s y un test de escena corto. Las señales son el gancho para T0.4 (guion de red "conducir el circuito automático"). Alternativa descartada: `Path3D`/`Curve3D` — más nodos y sin ventaja para 8 puntos. |
| D50 | Playground greybox | Suelo 200 × 200 m; rampa (subida ≈ 8°, meseta 6 m, bajada) atravesada por el circuito; campo de baches (v1: 6 cajas 0,15 m × 0,4 m cada 3 m; **rehecho en la ronda 2 (r1.2)**: 2 mesetas de 1,5 m de alto × 16 m de ancho con rampa de entrada a 8° y caída vertical, separadas 9,6 m — más que el largo del bus — para que el chasis caiga entre ellas; la v1 era dinámicamente inerte: la caja de 8 m siempre apoyaba sobre dos crestas); `WaterZone` (Area3D 20 × 2 × 12 m con superficie a y = 0,3 m, malla translúcida) que el circuito **atraviesa**; luz direccional, `WorldEnvironment` con cielo procedural, `ChaseCamera` de persecución; colores planos (`StandardMaterial3D` en sub-recursos, bus amarillo/rojo del style board D16 si cuesta cero). Colisiones con `StaticBody3D` + formas primitivas, **no CSG**. ≤ 60 nodos (R8). | Cada elemento del ✅ de T0.3 existe y el circuito pasa por todos. Primitivas: predecibles en Jolt, sin mallas de colisión cóncavas. La cámara exterior de persecución es la de ADR-004 para el conductor, reutilizable. Alternativas descartadas: CSG — cómodo pero genera trimesh y aristas fantasma en las juntas; agua con flotación — es T7.2 (M7); en T0.3 el agua es una zona que **detecta** al bus (`bus_entered`/`bus_exited`) y nada más. |
| D51 | Modo demo y herramienta `run_demo` | Raíz `Playground` con `@export var demo_mode: bool = true`; en `_ready` habilita `DemoDriver`. Herramienta headless `src/tooling/run_demo.gd` (`SceneTree`): carga el Playground, corre la demo hasta `lap_completed` (código 0) o hasta `rolled_over`/`stuck`/tiempo máximo (código 1), imprime un reporte por línea (`DEMO ...`), `quit()` en todas las rutas; con `--fixed-fps 60` una vuelta completa cuesta segundos de reloj. Con ventana y `++ screenshot=<ruta>` guarda un PNG a los N segundos de juego (§0.8 lo permite). | Es la "reproducción de bugs sin humano" de §21 y la base del guion de T0.4. `--fixed-fps` hace el resultado determinista y rápido; la corrida en tiempo real (evidencia 04 del plan) demuestra que también vale a 60 Hz reales. Alternativa descartada: solo test de gdUnit4 — no sirve para correr la demo 10 minutos ni para capturar. |
| D52 | Escena principal | `application/run/main_scene = "res://scenes/playground.tscn"` en `project.godot`. Único cambio en `project.godot` en T0.3. | El gate del dueño es "F5 y ver el bus recorrer el circuito"; el hub (M5) lo reemplazará. Alternativa descartada: sin escena principal — el dueño tiene que buscar la escena a mano. |
| D53 | `NetworkBackend` (autoload) | Autoload `src/net/network_backend.gd` (sin `class_name`, mismo patrón que `GameConfig` de D43) con `host_game(port)`, `join_game(address, port)`, `leave()`, `is_host()`, `peer_ids()` y señales `hosted`, `joined`, `peer_joined`, `peer_left`, `connection_failed`, `server_lost`, `peer_ready`, `peer_done`. ENet detrás de `_impl`; nadie llama a `multiplayer` fuera de él. GodotSteam entra en M4 sin tocar a los llamadores. | ADR-001 y §20 lo exigen literalmente; es lo que permite cambiar de transporte sin tocar gameplay. Alternativa descartada: usar `multiplayer` directo en cada sitio — en M4 habría que tocar todo el código de juego. |
| D54 | Guion coordinador en GDScript | `src/tooling/run_net_scenario.gd` con roles `launcher \| host \| client` por `++ role=`. El lanzador usa `OS.create_process` con `--log-file` por hijo, limpia `user://netscenario/` antes de arrancar, espera con `OS.is_process_running`, mata supervivientes al expirar y agrega los resultados leyendo los JSON por proceso. Código 0 solo si todos los hijos salieron 0 y todos los asserts pasaron. | Un solo guion en el lenguaje del proyecto, idéntico en Windows y macOS (D42 lo reactiva sin reescribir). El stdout de los hijos no se ve en Windows: cada hijo reporta por archivo. Alternativa descartada: orquestar desde PowerShell y bash — dos implementaciones que divergen. |
| D55 | Qué replica y qué afirma el guion | Host: Playground con demo y autoridad del bus; `MultiplayerSpawner` que crea un `NetMarker` por peer (vía `add_spawnable_scene` + `add_child` con nombre legible) y `MultiplayerSynchronizer` que replica `global_transform` del bus a 30 Hz. Clientes: misma escena con `network_role="client"` (demo off, bus `freeze`). Asserts en orden: (1) 3 clientes conectan; (2) cada cliente recibe los 4 marcadores; (3) tras `waypoint_reached(2)` el host frena, 1,5 s de asentamiento y cada cliente está a ≤ 0,5 m y ≤ 5° del host; (4) todos salen 0. **Nota de implementación (desviación aceptada por el dueño):** spawner y sincronizador se crean en tiempo de ejecución en ambos extremos con rutas idénticas, nada de red se escribe en `playground.tscn`. Handshakes `mark_ready`/`mark_done` por RPC: los spawns replicados se borran al desconectar la autoridad (el host espera a que todos los clientes tomen su snapshot). | Es lo único que hoy existe de verdad y cubre las tres mecánicas que el juego necesita: spawn replicado, sincronización continua y convergencia comprobable. Comparar tras frenar y asentar evita afirmar posiciones interpoladas en movimiento. Agarrar/amarrar no existe (M3): contradicción del ✅ reportada al dueño en BACKLOG, no inventada. |
| D56 | Ruta corta por defecto | El guion se detiene en el **índice** de waypoint 2 (≈ 12 s de reloj), no en la vuelta completa (36,8 s). `++ waypoints=N` es el índice en el que parar (0-based: el circuito tiene índices 0–16); **`waypoints=16` es la vuelta entera** (r1.2: el presupuesto de tiempo del host se deriva de la ruta, y los clientes toman su foto cuando el host lo indica por RPC tras frenar y asentar). | `run_tests` debe seguir siendo usable en cada iteración; 12 s es el precio de una prueba de red real. La vuelta completa queda para las corridas largas de M2/M4. |
| D57 | Integración en `run_tests` | Paso 8 al final de `tools/run_tests.ps1` y `.sh`: corre el guion de red y falla el arnés si sale ≠ 0. Salida `-SkipNet` / `--skip-net` para iterar solo unitarias. Presupuesto: unitarios ≤ 60 s + red ≤ 60 s. `tools/run_net_tests.ps1`/`.sh` como envoltorio de una línea para correr solo la red. | El ✅ exige "integrado en `run_tests`" y el revisor necesita correr la parte de red aislada. Alternativa descartada: suite de gdUnit4 que lance procesos — mezcla el runner con el SO y rompe el presupuesto de las unitarias. |
| D58 | Puerto y limpieza | Puerto por defecto 47810, configurable con `++ port=`; el host prueba los cuatro siguientes si está ocupado y reporta cuál usó (`port.txt`). Cada hijo tiene su tiempo máximo (`++ seconds=`), el lanzador mata con `OS.kill` a los que sigan vivos al expirar y todo corre bajo `timeout` externo. | Un proceso huérfano en Windows se queda cargando CPU hasta matarlo a mano (ya pasó en T0.2). Puerto fijo sin alternativa: una corrida colgada bloquearía las siguientes. |
| D59 | `Bus` sustituye a `PlaceholderBus` | `src/vehicle/bus.gd` + `bus.tscn`, nodo `Bus` en la escena. Conserva la interfaz pública entera (`set_drive`, `speed_mps`, `forward`, `is_grounded`, `upright_dot`, señal `rolled_over`, grupo `bus`, congelable). **Las herramientas y los tests lo buscan por el grupo `bus`, nunca por nombre de nodo.** El placeholder y su test se borran (autorizado por este plan). | El acoplamiento por nombre obligaría a tocar cinco archivos cada vez que el bus cambie; por grupo es una línea y no vuelve a romperse. La interfaz conservada mantiene `DemoDriver`, `run_demo` y el arnés de red sin cambios de fondo. |
| D60 | Suspensión: cuatro rayos, resorte y amortiguador por script | Cada rueda es un `Marker3D` hijo (reposicionado desde tuning). Por tick y rueda: rayo desde el ancla hacia `-global_basis.y` (largo reposo + radio, excluyendo el cuerpo); si golpea, compresión = reposo + radio − distancia; fuerza = rigidez·compresión + amortiguación·tasa_de_compresión, aplicada con `apply_force(normal_suelo · fuerza, global_basis · desplazamiento_rueda)`. Tracción y freno en el mismo punto sobre el plano del suelo; agarre lateral por rueda como fuerza (nunca asignación de velocidad), con el eje girado por el ángulo de dirección en las ruedas delanteras; la parte del agarre que añadiría energía se elimina (la bomba de velocidad medida en el ajuste resultó ser el borde de la pista de pruebas, pero la regla se mantiene por principio). La matemática vive en funciones estáticas puras testeables. | Es literalmente ADR-007. Aplicar en el punto de la rueda produce balanceo, cabeceo al frenar y transferencia de peso: de ahí sale el "peso" que el dueño va a juzgar. Alternativas descartadas: `VehicleBody3D` (ADR-007), fuerza central (sin feel que juzgar). |
| D61 | Los parámetros de sensación viven en datos | Doce campos nuevos en `TuningTable` (grupo bus): `bus_wheelbase_m`, `bus_track_width_m`, `bus_wheel_radius_m`, `bus_suspension_rest_m`, `bus_suspension_stiffness_n_per_m`, `bus_suspension_damping_ns_per_m`, `bus_grip_lateral`, `bus_grip_lateral_handbrake`, `bus_brake_force_n`, `bus_steer_max_deg`, `bus_steer_speed_falloff`, `bus_center_of_mass_y_m`. (r1.1 de T1.1, cerrada en M2-T2.1: `bus_accel_0_60_kmh_s` renombrado a `bus_traction_force_n` — es la fuerza en newtons, no un tiempo; el 0–60 real medido es 6,93 s y la sensación aprobada en el gate no se tocó.) Valores en `data/tuning.{tres,json}` y en el esquema; el bus se reengancha a `GameConfig.reloaded`. | El gate es "se siente bien" y el dueño no lee código: edita un JSON, recarga y vuelve a probar (para lo que se construyó T0.2). |
| D62 | Primera sección `[input]` | Subconjunto de conducción de §9.2: `drive_accelerate` (W), `drive_brake` (S), `drive_steer_left` (A), `drive_steer_right` (D), `drive_handbrake` (Espacio), `toggle_camera` (C). Teclado ahora; el mando llega por Steam Input en M4. Verificado con sonda: las seis acciones registradas con esas teclas. | Sin teclas no hay gate. §9.2 ya fija el estilo del mapa. |
| D63 | Dos cámaras y un conmutador | `CabinCamera` (primera persona, hija del bus, grupo `cabin_camera`) y la `ChaseCamera` exterior de T0.3 (grupo `chase_camera`); `toggle_camera` (C) alterna cuál es la activa. La lógica vive en `BusInput`, no en las cámaras. | ADR-004 y §9.1 lo piden textualmente; la exterior ya existía probada. |
| D64 | Freno de mano derrapante | Mientras `drive_handbrake` está pulsado: el agarre lateral de las ruedas traseras baja a `bus_grip_lateral_handbrake` y no se aplica tracción. Firma medida: mismo manejo con y sin, el bus rota 73° frente a 47° (sobreviraje). | Es el verbo que §4.5 nombra y sale casi gratis del modelo por rueda de D60. |
| D65 | La vuelta automática es la prueba de regresión | `run_demo` debe terminar en vuelta completa con el bus nuevo (mide: 23,0 s, 64,3 km/h, min_upright 0,97) y el bloque de red debe pasar en ruta corta y larga. La geometría del campo de saltos NO necesitó ajuste: el bus con suspensión la cruza (medido en la demo). | Es la única prueba automática extremo a extremo; protege T0.3 y T0.4 de la cirugía. |
| D66 | Interior como escena propia | `src/vehicle/bus_interior.tscn`, instanciada como hija directa del bus. Sus `CollisionShape3D` se re-parentan al `RigidBody3D` del bus en `_ready` (diferido: hacerlo en cascada falla con "busy setting up children" y las formas quedaban huérfanas — medido). Mismo rid que el chasis: los rayos de suspensión lo excluyen gratis y nada empuja al chasis. | Separa lo que va a crecer (interior, anclajes, paneles) de lo probado (chasis y suspensión), y respeta el límite de nodos por escena (R8). |
| D67 | Medidas del interior (m) | Caja útil 2,30 × 7,60 × 2,05 libres; pasillo central 1,20 (dos personas de 0,70 se cruzan, §4.3); piso a 0,35 del fondo del chasis; puerta lateral 0,90 con dos escalones de 0,25; puertas traseras dobles 1,40 en total. Parabrisas abierto sobre el salpicadero (sin él la cámara de cabina veía un muro — medido en captura). Los escalones son placas que no tocan el suelo en reposo (tocarlo convertía el escalón en tren de aterrizaje y el bus no andaba — medido). | Son las cotas mínimas que hacen cierto el "pasillo de doble ancho" de §4.3 con un chasis de 2,5 de ancho. |
| D68 | Seis posiciones útiles | `Marker3D` en el nodo `Positions`: `driver`, `copilot`, `bench`, `shelf_left`, `shelf_right`, `stretcher`. Sin lógica: son anclajes que T2.2 y T2.3 consumen. | §4.3 pide seis posiciones y el resto del juego se refiere a ellas por nombre. |
| D69 | Anclajes de carga como Marker3D | Nodo `Restraints`, seis por muro (`restraint_left_0..5`, `restraint_right_0..5`). Sin física: T2.3 les cuelga el `restraint`. | Mismo motivo que D68. |
| D70 | El interior no cambia ni un dato de sensación | Las seis cifras de T1.1 se re-miden con el interior (evidencia 03): si alguna se moviera más de un 10 %, se corrige repartiendo las formas, nunca tocando `data/tuning.json`. Medido: todas dentro del 0,1 %. | El dueño aprobó una sensación concreta; esta tarea no la renegocia. |
| D71 | Demo automática sin consola *(r1.2 de T1.1, cerrada en el paso 0 de M2-T2.1)* | F5 queda como el asiento del conductor (la escena arranca en modo conducible desde M1-T1.1) y la tecla **F1** (`toggle_demo`) entrega el bus al `DemoDriver` — y lo devuelve — en tiempo de ejecución, sin línea de comandos. | El dueño aprobó en el gate de T0.3 que F5 mostrara la demo, y en el de T1.1 que F5 sirviera para conducir; con la tecla ambas quedan a un clic de distancia. Alternativa descartada: volver a `demo_mode=true` por defecto — exigiría el editor o la consola para conducir. |
| D72 | El interior se encoge para caber (arreglo del defecto de T2.1) | Piso a 0,20 del fondo del chasis (local −0,60), altura libre 1,90, techo a 1,30–1,35 — todo dentro de la silueta del chasis (medido por sonda de AABB: cero formas sobresalen). Los escalones de la puerta quedan dentro de la silueta y por encima del plano de contacto. No toca masa ni reparto: la sensación aprobada queda intacta (las seis cifras idénticas). | Es el defecto que el pliego de T2.1 introdujo (2,40 no caben en 2,20); cerrarlo antes de construir encima. Alternativa anotada para M8: subir el chasis a 2,7 m (más honesta, pero cambia la silueta y la inercia y obligaría a repetir el gate de sensación). |
| D73 | `Crew` como `CharacterBody3D` | `src/crew/crew_member.tscn` (cápsula de 1,75 m, cámara en primera persona a 1,65 m) + `crew_member.gd` + `crew_input.gd` separado (traduce acciones a `drive_move`, la misma API que usará la IA en M3). Velocidades y salto desde `TuningTable` (`player_walk_speed_mps`, `player_sprint_speed_mps`, `player_jump_height_m` — ya existían). | R2: los números del personaje ya están en datos; el personaje solo los consume. Separar input de lógica repite lo que funcionó con el bus. |
| D74 | Herencia del movimiento del bus, decidida por medición | **Medido (paso 1, evidencia 03):** `get_platform_velocity()` devuelve la velocidad del bus y la cápsula es transportada por el motor con deriva de milímetros en 64 s con giros y saltos. **Se USA la herencia del motor; NO se implementa el acarreo manual** (queda como plan B). | Es el riesgo #1 del proyecto; la decisión se tomó con la tabla delante, no adivinada (ADR-003). |
| D75 | Entrar y salir por la puerta lateral | Prueba de plano en el marco del bus (el hueco de la puerta lateral), sin animación y sin zonas Area3D (una zona en el hueco dispara enter+exit en un solo cruce). **Cambio de mecanismo medido:** el reparentado al bus quedó FUERA — una tripulante hija del bus Y transportada por la plataforma recibe el movimiento dos veces (deriva de 3,5 m por vuelta, medida); el `aboard` es un flag con señales (`boarded`/`exited`). | La letra de D75 decía reparentar; la medición lo contradijo y manda. Reportado, no resuelto en silencio. |
| D76 | Asiento del conductor mínimo | Al interactuar (E) con el puesto `driver`: la tripulante se oculta y congela, la cámara pasa a la de cabina y el input de conducción se habilita; al levantarse, lo contrario. Un ocupante, sin red, sin `CoopInteractable` (M3). | Es lo mínimo que hace jugable lo que el dueño pidió, sin invadir M3. |
| D77 | El gate duro de M2 no se juega aquí | El gate con dos instancias es de T2.3 (con paquetes). T2.2 entrega el criterio de un jugador: caminar dentro a ~70–80 km/h sobre los saltos durante un minuto sin atravesar nada ni salir despedido (medido: deriva máxima 0,30 m, cero atravesamientos, cero expulsiones). | Adelantar el gate duro sin paquetes lo dejaría vacío. |

| D78 | El chasis del bus es un casco hueco (r1.1 de M2-T2.2) | Ya no hay caja maciza: la colisión del cuerpo es una **placa de piso de 2,5 × 0,2 × 8,0 m** (`BoxShape3D` en `bus.tscn`) y paredes, techo y escalones son las formas convexas del interior (D66), todas hijas del mismo `RigidBody3D`. El habitáculo es un hueco real por el que se camina. Lo que el bus barre contra el mundo, su tensor de inercia y su silueta son los de ESTA forma; la sensación se re-midió tras el cambio y se mantuvo (D70, D72). | El registro de T2.2 lo llamó “interior encogido”, que no es lo que ocurrió, y la frase “ninguna forma sobresale del chasis” dejó de significar algo (el chasis es una placa de 0,2 m). Toda tarea posterior razona sobre esta forma: esta entrada es la referencia. |
| D79 | Una sola cámara activa, mirada con ratón y captura del puntero (M2-T2.2 r3) | `CameraArbiter` es el único sitio que decide la cámara (`apply()`/`toggle_bus_view()`): a pie manda la `EyeCamera` de la tripulante (grupo `eye_camera`), sentado la del bus (cabina por defecto, persecución con C), en demo la persecución. Al sentarse se pasa a la cabina y **al levantarse se recupera la vista del personaje** (ese retorno era el bug de la ronda 2). Mirada §9.2: `MouseLook` puro y testeable (guiñada al cuerpo, cabeceo a la cámara, ±89° a pie; cono del conductor ±120°/±45° en cabina, ADR-004); sensibilidad desde `TuningTable.player_mouse_sensitivity`; siempre `screen_relative`, nunca `.relative` (con stretch `canvas_items` el motor lo escala ×18 — medido (10,−4)→(180,−72)). Captura: al jugar capturado, **Escape libera** y un clic recaptura; con la demo (F1) el ratón queda libre. La verdad de la captura la posee `CrewInput` porque el DisplayServer headless no retiene `Input.mouse_mode` (medido 2026-09-14). | El dueño lo pidió textual tras jugar la ronda 2. Un solo punto de decisión hace afirmable por test que hay exactamente una cámara activa en cada estado, y la captura con salida evita el "puntero secuestrado". |
| D80 | Conductor a la izquierda, puerta a la derecha (r3.1, decisión del dueño 2026-09-14) | `Positions/driver` a x = −0,6 y `CabinCamera` sobre él (−0,6, 0,75, −2,9); `Positions/copilot` pasa a x = +0,6. La puerta lateral no se mueve (sigue a la derecha). Pines en el test de escena: ambas x < 0. | En Chile se conduce por la derecha, así que el conductor va a la izquierda; el maestro no lo fijaba. Barato hoy (un marcador y una cámara), caro tras T2.3, cuando los amarres se asienten sobre ese lado. |
| D81 | `Package` es un `RigidBody3D` con tres estados físicos | `src/cargo/package.tscn` + `package.gd`: caja greybox de 0,4 m, masa desde `TuningTable.package_mass_kg`, capa 2 `cargo` (máscara mundo+carga+tripulante), CCD encendido, fricción por defecto. Estados `FREE` (rigid real), `HELD` (cinemático sin colisión), `STRAPPED` (congelado en un anclaje). Sin contenido, tipos ni integridad (M3). | La letra de ADR-003; los nombres de §5.1 evitan renombrar en M3. La configuración de fricción/CCD no se adivinó: viene de la tabla del paso 1 (03). |
| D82 | En mano: cinemático, sin colisión, siguiendo el `HandAnchor` | Un objeto a la vez; cada tick de física el paquete se coloca en el `HandAnchor` (hijo de la `EyeCamera`, así que la carga cabecea con la mirada) con `global_transform`, congelado y con la colisión APAGADA. | Es la lección de la ronda 4 aplicada antes de tropezar: un cinemático con colisión dentro del casco patea el bus (medido: una caja en mano a 40 km/h no cambia ω ni altura del bus). |
| D83 | Soltar y lanzar posicionan antes de encender colisión | `Package.release(at, velocity)` hace la pareja completa (colocar, descongelar, velocidad, encender diferido) en un solo sitio. Soltar: al punto de la mano si cabe (prueba de solape), si no a los pies; velocidad = la del punto del bus (v + ω × r). Lanzar: adelante de la cámara × `package_throw_speed_mps` + la del punto del bus, composición como función pura. Un clic con el puntero libre solo recaptura, nunca lanza. | Soltar dentro de una pared es el fallo del techo con otro disfraz; heredar la velocidad del bus es lo que hizo funcionar levantarse en marcha. |
| D84 | Amarrado: hijo congelado del bus en el anclaje | Mantener E `strap_hold_seconds` con un paquete en mano y un anclaje libre al alcance: el paquete se reparenta al bus en la posición del anclaje (pose fijada antes), congelado `FREEZE_MODE_STATIC`, colisión encendida (la tripulante choca con él como con un estante). Un paquete por anclaje; E sobre un amarrado con la mano vacía lo devuelve a la mano. Deriva 0,000004 m medida en una vuelta. **Nota de mecanismo (medida 2026-09-15):** se probó `FREEZE_MODE_KINEMATIC` por el acarreo de quien se suba encima y el reparent **duplica el movimiento** (88,5 m de deriva en una vuelta); con pin local queda un tick de retardo (0,3 m). Y una tripulante de pie sobre una caja de 0,4 m dentro del casco atraviesa el techo (1,2 + 1,75 > 1,30), así que el acarreo sobre carga no tiene consumidor dentro del casco hoy — queda STATIC+reparent, y la medida del acarreo KINEMATIC (sí funciona) en la evidencia 03. | ADR-003 literal; y cuando la medición contradice la letra (reparent), manda la medición, reportada. |
| D85 | Los sueltos se miden antes de escribirse | Paso 1: cuatro cajas libres en el piso del bus, la vuelta de la demo; tabla de despegues, alturas, atravesamientos y escapes en tres condiciones (60 Hz, 120 Hz, amortiguador). Con la tabla: 120 Hz descartado para T2.3 (empeora, número sin explicar en BACKLOG), amortiguador SOLO VERTICAL con k=160 en `TuningTable`, fricción por defecto, CCD encendido. **Corrección (revisión 01, r1.1):** el amortiguador oponía TODA la componente vertical, también la caída libre — una caja de 8 kg alcanzaba velocidad terminal m·g/k = 8·9,8/160 = **0,49 m/s** ("caen como en agua", medido: 0,48–0,49 m/s constantes; una caída de mano de 0,73 m tardaba 1,5 s; caída libre: 0,39 s). El amortiguador existe para que un bache no lance la carga al techo: eso es velocidad relativa HACIA ARRIBA. Desde la ronda 2 solo se opone la componente vertical positiva; la caída queda libre (test: 1,0 m toca el piso en 27 ticks ≈ caída libre; antes: 123). (c'') y (d) re-medidos con la corrección en la 03. | Adivinar aquí es lo que ADR-003 prohíbe; la tabla está en `docs/evidencia/M2-T2.3/03_free_cargo_measurement.txt` con (c'), (c'') y (d). |
| D86 | Entradas: `throw`, `drop`, y E como detector de duración | Acciones `throw` (clic izquierdo) y `drop` (clic derecho) en `project.godot`; `interact` (E) como detector de duración en ticks: toque = retícula (asiento/agarrar/desamarrar, lo más alineado con la mirada), mantenido = amarrar con `strap_progress`. El test estático de acciones (T2.2) caza cualquier acción sin registrar. | El mapa de §9.2; la retícula salió de la jugada medida (en un bus con carga, agarrar por distancia ganaba siempre al asiento). |
| D87 | Campos de carga en `TuningTable` | Grupo `cargo`: `package_mass_kg` 8, `package_throw_speed_mps` 6, `package_hold_distance_m` 0,7, `strap_hold_seconds` 1,5, y `package_relative_damping_ns_per_m` 160 (este último fijado por la medición, no por el plan). Espejo JSON y `.tres` por el pipeline de T0.2; ningún número en código. | R2; los valores son v0 para tunear en playtest. |
| D88 | El gate duro de M2 es la tarea siguiente (`M2-GATE`), no parte de T2.3 | T2.3 entrega el criterio de un jugador con carga (la vuelta con dos amarrados, dos sueltos y la tripulante caminando: cero atravesamientos, cero fuera por paredes/parabrisas, deriva de amarrados 0,000, 60+ fps). El gate con dos instancias necesita replicar tripulantes y paquetes (subconjunto de ADR-006 que nada antes de M4 construye): su propio plan, justo después. **Decisión del dueño.** | Meter la replicación aquí triplica la ronda y mezcla dos preguntas: "¿funcionan los tres estados?" y "¿sobreviven a la red?". |
| D89 | El parabrisas no es una abertura | `WallFront` mide 0,8 m (de −0,6 a 0,2) y encima no había colisión: omisión del greybox de T2.1, no una puerta. `BoxShape3D` 2,5 × 1,1 × 0,1 en (0, 0,75, −3,85) añadida como texto con malla translúcida y pin en el test de escena. La puerta lateral y el hueco trasero se quedan abiertos (son puertas; abrir y cerrar es M3). La métrica de escapes se parte: "fuera por paredes o parabrisas" = cero; "fuera por puertas" = se cuenta y se reporta, no se prohíbe. | Las salidas medidas en el paso 1 eran balísticas por las aberturas; cerrar la que no es puerta y contar las que sí lo son. |
| D90 | Esquema de capas físicas y el rayo de suspensión solo lee `world` (enmienda de alcance autorizada por el dueño 2026-09-15, tras diagnóstico medido) | Capa 1 `world` (suelo, rampa, baches, el propio bus), capa 2 `cargo`, capa 3 `crew`, en `project.godot [layer_names]`. Tripulante: `collision_layer = 4`, máscara 3 (mundo+carga). Paquete: capa 2, máscara 7 (mundo+carga+tripulante). Bus: capa 1, máscara 1 (sin cambio). El rayo de suspensión (`bus.gd`) fija `query.collision_mask = WORLD_LAYER` (constante nombrada con el porqué en comentario). **Medido:** sin máscara, el rayo leía una caja en la columna de una rueda (la delantera derecha cae en el hueco de la puerta) como suelo a 0,2 m → ~54 kN de muelle en una esquina y el bus parado voló a y = 3,84 m; la vuelta con carga daba exit 100. Con la línea: 19/19 en verde y la vuelta limpia. La regla "colisionan si la máscara de CUALQUIERA incluye la capa del otro" quedó demostrada sin querer: las cajas con máscara 3 descansan sobre el bus (máscara 1) — va citada, no asumida. | La columna de rueda cae en la puerta: la tripulante la pisa a cada abordaje y las cajas ruedan por ella; la línea sola no bastaba. Con capas no hace falta excluir RIDs por tick ni ahora ni con cuatro peers en M4. |
| D98 | **La carga puede empujar a la tripulación: es característica del juego, no defecto** (decisión del dueño, 2026-09-17, sobre [revisión 06](docs/revisiones/M2-GATE_revision_06.md)) | No se construye inmunidad: ni réplicas de carga dinámicas, ni supresión de la colisión tripulante–carga, ni opción B. **Lo que sí era defecto y se arregló: la pérdida de inercia en el aire.** `drive_move()` sobrescribía cada tick `velocity.x/z` con la de paseo, así que al despegar la tripulante conservaba la velocidad del bus **un tick** y al siguiente se quedaba a ~1,5 m/s en el mundo mientras el bus se iba a 62 km/h; por eso **las cinco expulsiones de M2-GATE fueron definitivas**, y no era de red: afectaba igual al anfitrión y a un jugador. Ahora, al perder el suelo se suma una vez la velocidad de la última plataforma apoyada (`platform_on_leave = DO_NOTHING`, reserva propia) y el input añade el paseo encima; aterrizar o sentarse la borra. Medido: residuos de 0,0317 m de pie, 0,0032 m caminando y 0,0121 m en baches, saltando a 80 km/h, con los controles sin carga en cero ticks en el aire. `slide_on_ceiling = false` recupera 0,32 m que se perdían al rozar el techo. **Asimetría declarada y aceptada:** la carga rígida del anfitrión no puede mover a su tripulante (un `CharacterBody3D` es inamovible para cuerpos rígidos) y las réplicas cinemáticas del cliente sí mueven a la suya; el empujón simétrico es tarea de M3. **Límite declarado ([evidencia 21](docs/evidencia/M2-GATE/21_estimulo_del_banco.md)):** sostenido el tiempo suficiente, un empujón saca a la tripulante del bus — en el banco con déficit permanente, 695 ticks fuera. En lo medido no ocurre: los doce empujones reales de 120 s se recuperan en 1–3 ticks. | El dueño decide qué es juego. El revisor decide qué es medible: que te empuje una caja es juego; quedarte en la carretera a 60 km/h es física mal hecha. |
| D99 | **El fondo abierto del bus es la PUERTA DE CARGA, y perder carga suelta por ahí es juego** (decisión del dueño, 2026-09-17: «sí es una puerta, no la detallé en el plan») | **Geometría medida, que hasta hoy no estaba escrita en ningún sitio:** el suelo mide 2,5 × 7,8 y termina en `z = 3,9`; atrás hay dos paredes de **0,55 m de ancho** en `x = ±0,975`, `z = 3,85`, altura 1,9 desde la superficie del suelo. Entre ellas queda un **hueco de 1,4 m de ancho por 1,9 m de alto**, y la caja mide 0,4 m: pasa holgada. **Consecuencia, comprobada en la sesión del dueño del 2026-09-17:** cuando el bus acelera, una caja suelta se queda atrás respecto al suelo —física correcta— y se desliza hacia el fondo hasta salir. Las cuatro salieron por el mismo sitio, a ras de suelo, con `z` entre 4,5 y 5,8; una a los **5,4 s**, sin que nadie la tocara. No se añade bordillo, panel ni rampa: **no es una omisión del greybox como el parabrisas de D89**, es la puerta. **Lo que sí queda establecido:** que la carga suelta se pierda es la razón de ser de las correas (maestro §5.1, amarrar es el bucle central), así que es contenido, no defecto; el criterio del gate **no** cuenta salidas de carga por la puerta; y el hueco trasero se documenta en `src/vehicle/bus_interior.tscn` para que nadie lo «arregle» creyendo que falta una pared. Si alguna vez se quiere una puerta cerrable, es mecánica de M3, no una corrección. | Lo había diagnosticado como posible omisión por analogía con D89. El dueño aclara que es intencional; lo que faltaba era escribirlo, y eso sí era un defecto — del plan, no del juego. |
| D97 — CUMPLIDA | **Gate humano superado el 2026-09-17** | El dueño jugó la instancia cliente con ventana mientras el anfitrión conducía. Veredicto literal: «se siente bien al estar en el auto manejando». La única objeción que planteó —cajas que «saltan lejos»— se midió sobre su propio rastro y resultó ser la puerta de carga (D99), no un fallo del acarreo: `Package_3` salió a los 5,4 s sin que nadie la tocara. **Limitación declarada:** cerró la ventana antes de los 300 s, así que la sesión no fue de cinco minutos completos; el dueño dio el gate por bueno igualmente. Con esto **ADR-003 queda validado en su opción A** y M2 cierra. | El riesgo #1 del proyecto se valida con una persona sintiendo, no con una tabla; y la tabla sola nunca habría encontrado que la queja era una puerta. |
| D100 | **Enmienda a D16: el bus deja de tener ventanas corridas y pasa a ser un furgón largo de reparto** (decisión del dueño, 2026-09-17: «parece un bus escolar, la idea es repartir paquetes»; eligió la variante v3) | **Lo que cambia de D16:** fuera la «franja de ventanas corridas» — ventanas de pasajero a lo largo de todo el lateral, más amarillo, es un autobús escolar y no puede leerse de otra forma. **Queda:** carrocería única sin cabina separada, morro plano cab-over, **lateral ciego** con una fila de ventanas pequeñas altas bajo el techo, puerta lateral corredera con estribo a media carrocería, **puertas traseras dobles de carga** (la puerta de D99), batalla larga, librea amarilla con una franja roja y techo crema. **Lo que NO se puede cambiar, y por qué se descartaron las otras variantes:** la carrocería tiene que ser **continua**. Un camión con caja (variante A) tiene cabina separada y el conductor no puede levantarse y caminar hacia la carga — que es el bucle central que M2 tardó dos semanas en validar (T2.2, D76). Y tiene que ser **largo**: el casco real son 7,8 m de suelo para seis posiciones útiles, estanterías en ambos muros, dos asientos, banco y camilla (§4.3); la variante retro corta no los alberga. **Láminas canónicas:** `docs/referencias/bus_exterior_canonico_v2.png` y `bus_exterior_trasera_v2.png`. Las descartadas se conservan (`v2a_furgon`, `v2b_expedicion`, `v2c_retro`, `v3_sintesis`) porque el motivo del descarte es información de diseño, no basura. | El style board existe para cazar esto con cinco láminas y no con veinte assets. Fue lo primero que hizo al existir. |
| D101 | **El generador 3D del proyecto es TRELLIS.2 nativo de ComfyUI; la DA4 del plan M-ART queda resuelta sin instalar nada aparte** (2026-09-21) | **Lo que cambió:** el dueño actualizó el servidor de ComfyUI 0.33.4 a **0.37.0** (respaldo previo en `/srv/vms/comfy-ui/backup-pre-update-20260921`, 19 custom nodes importan sin error). Desde 0.34.0 ComfyUI trae TRELLIS.2 **en el núcleo**: verificado en `/object_info` con los nodos `Trellis2Conditioning`, `Trellis2ShapeStage`, `Trellis2UpsampleStage`, `Trellis2TextureStage` y los decodificadores `VaeDecode*Trellis*`. Pesos descargados y visibles en los cargadores: `trellis_2_int8_convrot` (5,0 GB), los dos VAE (1,0 + 0,9 GB), `dino_v3_L_naf_fp32` (1,2 GB) y `birefnet` (0,4 GB). **Por qué la ruta nativa y no el custom node `ComfyUI-Trellis2`:** el TRELLIS original y el wrapper dependen de `nvdiffrast`/`nvdiffrec`, bajo licencia NVIDIA **solo no comercial**; la implementación nativa los reescribió en PyTorch/SciPy. Es la única ruta cuya cadena completa es limpia para Steam: TRELLIS.2 MIT, BiRefNet MIT, DINOv3 con licencia Meta mundial que exige el aviso «Built with DINOv3» (CREDITS.md). **Consecuencias:** la decisión de la sesión del 17 de «modelar en Blender por script porque Trellis no está» deja de ser forzada; Blender sigue siendo la herramienta de retopología y colisión (§10.1 punto 3, el mesh generado nunca es colisión). El host del servidor pasa a `https://comfy.areum.cl` tras Cloudflare Access; las credenciales se leen del entorno (`CF_ACCESS_ID`, `CF_ACCESS_SECRET`) y **no se escriben en el repo**. Herramientas: `tools/comfy_api.py` (cliente común), `tools/comfy_mesh.py` (imagen → .glb), `tools/comfy_generate.py` migrado al cliente común. Hunyuan3D sigue instalado y sigue prohibido para publicar. | Lo que el 17 era un bloqueo con dos salidas malas — instalar un repo de investigación con dependencias no comerciales, o renunciar al 3D generado — se resolvió con una actualización de versión. Conviene mirar el changelog antes de construir alrededor de una carencia. |
| D102 | **M-ART-VAN queda aprobado como tres assets con tres atlases externos y geometría final autorada en Blender:** exterior, interior y rueda; un material y cero imágenes embebidas por GLB. El neumático será genérico de competición, sin Pirelli, logos, lettering ni trade dress, por la regla sin marcas reales. El arte queda separado de la física: ningún GLB aporta colisión. Copiloto solo local con `drives_bus=false`, `BusInput` apagado y cámara fija propia hija del bus; asiento elegido por retícula. Las ruedas son solo visuales y `NetBusSync` replica `steer` al final del snapshot sin escribirlo en la física cliente. Los greyboxes se ocultan, no se borran. **R12:** solo pueden ocultarse/reemplazarse nodos visuales en `bus.tscn` y `bus_interior.tscn`; colisiones, marcadores, masa/capas y tuning quedan intactos. | El plan aprobado cierra alcance y separación visual/física antes de producir assets. La ocupación de asientos por red sigue en M4 y el gate humano M-ART-VAN sigue pendiente; esta decisión no afirma implementación. |
| D103 | **Puertas funcionales y asientos automotrices, por pedido explícito del dueño** (2026-09-24: «no existe la puerta para subirse, falta la opcion de abrir y cerrar la puerta de atras y se vea con animacion»; eligió que la lateral también cierre) | **Enmienda D90/D99/D102 solo donde era imposible cumplirlo visualmente:** el hueco lateral se mueve 1,0 m atrás, de z [-3,2,-2,3] a [-2,2,-1,3], y deja de estar sobre la rueda delantera derecha. Se reubican `WallRightFront`, `WallRightRear` y los escalones; ruedas, masa, COM, capas y tuning no cambian. `BusDoors` es host-authoritative: lateral corredera y dos traseras con bisagras, fases OPEN/CLOSING/CLOSED/OPENING, ambas abiertas al iniciar. E por retícula; con paquete en mano no opera. Dos blockers viven en un `AnimatableBody3D` separado, capa `cargo`, para no cambiar inercia ni tocar el rayo; solo se habilitan en CLOSED y se deshabilitan antes de abrir. El cierre consulta vano y barrido de la hoja; carga o tripulante lo revierte desde el progreso actual, nunca empuja. Clientes piden por RPC confiable; el host valida alcance/fase y replica estado, con snapshot al entrar tarde. Con trasera abierta la carga suelta sigue pudiendo caer (D99); cerrada queda dentro. Asientos: una malla por asiento con bucket, bolsters, respaldo reclinado hacia atrás, cabezal con postes, rieles y cinturón. | El pedido del dueño convierte las puertas en juego real; una hoja sobre la rueda sería falsa o cortaría el arco. Medido: 288/288, red 1+3, inercia idéntica abierta/cerrada y vuelta idéntica con puertas cerradas. El gate humano decide si se ve y se siente bien. |

**D32 — hash del commit clonado:** `08ffc7c65b61b1b2edd545616061a99973c13ce1` (tag `v6.2.1` del repo `godot-gdunit-labs/gdUnit4`).

# Prompt para el agente ejecutor — M0-T0.2 Ronda 3 (cierre)

Copiar desde `CONTEXTO:` hasta el final y pegarlo tal cual. No hay decisiones pendientes del dueño.

---

CONTEXTO: Proyecto "Ruta Frágil" (Godot 4.7.2, GDScript tipado). Obedece R1–R14: están textuales en `AGENTS.md` §0.3. Repo local: `C:\Users\Leo\Documents\RutaFragil`, rama `m0/t0.2-game-config` (HEAD `6656a8c`; `main` = `e42f80b`, no se toca). Tu ronda 2 cerró el código (M4 y mc1–mc7), M2, M3, mt1–mt7 y md1–md8; el revisor lo reprodujo todo en verde (53/53, sin datos → código 1, mod de dos secciones rechazado entero, ida y vuelta byte-estable). NO pasa por tres huecos de prueba sobre lo nuevo y por evidencias que afirman lo que sus comandos no demuestran. Hay tres archivos del revisor sin commitear en el árbol (`docs/revisiones/M0-T0.2_revision_02.md`, `docs/planes/M0-T0.2_ronda3_prompt.md`, `docs/planes/M0-T0.2_plan.md` actualizado a v1.1): cométealos tal cual primero (paso 0).

TAREA: **M0-T0.2 — Ronda 3 (cierre).** Cerrar MR1–MR3, e1–e6 y d1–d4 de `docs/revisiones/M0-T0.2_revision_02.md`. Sin cambios de diseño; un único cambio de firma compatible en el autoload.

ARCHIVOS RELEVANTES (lee completos, en este orden): `docs/revisiones/M0-T0.2_revision_02.md` · `tests/core/data/game_data_loader_test.gd` · `tests/autoloads/game_config_autoload_test.gd` · `tests/core/data/resource_json_codec_test.gd` · `src/autoloads/game_config.gd` · `src/core/data/game_data_loader.gd` · `docs/datos/esquema_v1.md` · `DECISIONS.md` · `README.md` · `docs/evidencia/M0-T0.2/` (02, 03, 04, 05, 06, 07, 08).

REGLAS DE TIPADO (vigentes; ya las cumples): sin `:=`, todo con tipo, `-> tipo` en todo `func`, fronteras `Variant` con `is` y variable tipada, sin `@warning_ignore`. Herramientas `-s` con `quit()` en todas las rutas y `timeout 120`.

QUÉ HACER:

A. Tests (mayores)
   1. **MR1:** `test_empty_dirs_fail_with_one_error_per_document` en `game_data_loader_test.gd`: dos `create_temp_dir` vacíos, `load_all()`, afirmar `is_ok()` falso, `errors.size() == 2`, `applied_files` vacío, `errors[0]` empieza por `doc:game_config:` y `errors[1]` por `doc:tuning:` (el orden de `DOCS`).
   2. **MR2:** en `test_tres_plus_truncated_json…` (M1c, líneas ~203–216): escribir también la base de `game_config` (`_write_base_game_config`), afirmar `errors.size() == 1`, `errors[0]` empieza por `base:` + ruta del JSON truncado, y `starting_money == 321`.
   3. **MR3:** en `src/autoloads/game_config.gd`, `func reload(base_dir: String = "res://data", mods_dir: String = "user://mods") -> DataLoadReport` (el `_ready` sigue llamando `reload()` sin argumentos; la señal y `last_report` igual). En `game_config_autoload_test.gd`, dos tests con una instancia propia del script del autoload (`auto_free(load("res://src/autoloads/game_config.gd").new())`, sin añadirla al árbol) y directorios temporales: (a) base válida + mod inválido → `await assert_error(func() -> void: node.reload(base, mods)).is_push_warning(<texto exacto "REJECTED mod:…">)`; (b) directorios vacíos → `is_push_error(<primera línea del summary()>)`. Documentar la firma en el esquema y README ("los argumentos existen para pruebas; el juego usa los defaults").
   4. **e1:** tests de mc1 (`{}` como base → `is_ok()` falso con error "missing object section"; `{}` como mod → aviso "no sections" y el archivo en `applied_files`), mc2 (`validate` con `1e400` en un `int` → error "finite"), mc3 (`ResourceSaver.save(GameConfigData.new(), base + "/tuning.tres")` → error "is not a").

B. Documentos (menores)
   1. **d1:** en `esquema_v1.md` §5, viñetas para: mod `{}` (aviso, entra en `applied_files`), mod con solo secciones desconocidas (igual), `.tres` de clase equivocada (error), base `{}` o sin sección (error "missing object section"), rechazo por clave de sección no-string o sección no-objeto. Reformular "un archivo aplica completo o no aplica" → "un archivo con errores no aplica nada; un archivo sin errores aplica todas sus secciones conocidas (aunque sean cero)".
   2. **d2:** D45 en `DECISIONS.md` y el docstring de `game_data_loader.gd`: "orden alfabético" → "orden de bytes (ASCII); ver esquema §5".
   3. **d3:** una sola frase en D44, README y esquema §5.1: "`.tres` autoritativo para el editor/Inspector; en caliente gana la última capa; el test de deriva mantiene ambos iguales".
   4. **d4:** esquema: "el `push_error` **del reporte** vive en el autoload (los demás `push_error` del código son de rutas de bug)"; README: `print_game_data` sale con código 1 si `!is_ok()`; cabecera de la tabla D30–D47: "(D44/D45/D47 precisadas en la ronda 2)".
   5. BACKLOG: nota "editor con ventana: un segmentation fault al cerrar observado una vez bajo `timeout` (2026-09-10), no reproducido en dos intentos; bug de apagado de 4.7 con plugins, fix en 4.8; sin acción".

C. Evidencia
   1. **e6:** cabecera en 03, 04 y 05: "capturada en `e5fa554` (ronda 1); `bus_wear_per_contract_max` se renombró a `bus_integrity_max_loss_per_contract` y `fuel_consumption_l_per_km` pasó a float en la ronda 2; los valores efectivos actuales están en 02/06/09". O recapturar las tres.
   2. **e2:** recapturar 06 sobre árbol commiteado: `git hash-object data/*` antes → `write` → `import` → `write` → `git hash-object data/*` después → idénticos; suite de deriva corrida al final.
   3. **e4:** capturar la "verificación final" que la nota de 06 cita, o retirar la frase.
   4. **e5:** 08: los `.gitkeep` se borraron en `e5fa554` (ronda 1); añadir la rama `push_warning` como verificada por test (tras MR3) o declararla.
   5. **e1:** corregir la cabecera de 02 para que liste solo lo que los tests cubren; recapturar 02 con el arnés final (≥ 58 tests).
   6. **e3:** 07 en **dos partes**: captura pre-commit dentro del commit de código, y una **sección post-commit** capturada después y commiteada en un commit de docs aparte (`docs: evidence 07 post-commit (M0-T0.2)`), como en T0.1. El `git log main..HEAD` de esa sección debe listar el commit de código de esta ronda.

CRITERIOS DE ACEPTACIÓN: ambos arneses en verde con ≥ 58 tests; MR1–MR3 con los tests descritos; e1 con los tres tests de mc1–mc3; 06 con hashes idénticos antes/después; 07 con sección post-commit que liste el commit de código; 02 recapturada; 03/04/05 con cabecera o recapturadas; esquema/D44/D45/README alineados; `git status` limpio; nada fuera de los archivos listados (más `src/autoloads/game_config.gd` por MR3 y `BACKLOG.md`).

INSTRUCCIONES:
0. Commit de los tres archivos del revisor tal cual: `docs: review 02, plan v1.1 and round-3 prompt (M0-T0.2)`.
1. Responde PRIMERO con un plan corto (archivos, orden, riesgos). No toques nada aún.
2. Tras la aprobación del dueño, implementa A → B → C en `m0/t0.2-game-config`. Itera código → `run_tests` → código hasta verde.
3. Corre ambos arneses y adjunta la salida completa.
4. Actualiza `08_no_verificado.txt`.
5. Tres intentos fallidos en cualquier punto = detenerse y reportar.
6. Reporte final: hashes de los commits (código y post-commit), ítems cerrados por código (MR1–MR3, e1–e6, d1–d4), conteo de tests, lo que quedó abierto y por qué, ruta de cada evidencia. Sin resúmenes de "todo verde": el revisor reproduce.

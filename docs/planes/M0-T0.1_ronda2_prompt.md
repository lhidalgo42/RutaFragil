# Prompt para el agente ejecutor — M0-T0.1 Ronda 2

Copiar desde la línea `CONTEXTO:` hasta el final y pegarlo tal cual al agente ejecutor. **Antes de enviar, el dueño completa el campo marcado `[DUEÑO COMPLETA]`.**

---

CONTEXTO: Proyecto "Ruta Frágil" (Godot 4.7.2, GDScript tipado). Obedece R1–R14 (abajo). Repo local: `C:\Users\Leo\Documents\RutaFragil`, rama `m0/t0.1-foundations` (HEAD `62fa3a3`; `main` = `633d4ba`, no se toca). Tu ronda 1 de M0-T0.1 fue revisada y NO pasó el gate. Esta es la ronda 2. Hay archivos sin commitear en el árbol de trabajo escritos por el revisor con autorización del dueño: no los reescribas, solo cométealos (ver INSTRUCCIONES, paso 0).

```
REGLAS DEL PROYECTO RUTA FRÁGIL — OBLIGATORIAS EN TODA SESIÓN (v0.2 Godot)
R1  Nunca hardcodear el número de jugadores. Leer siempre GameConfig.max_players.
    Toda UI de lobby/HUD se genera dinámicamente para N jugadores.
R2  Todo contenido (paquetes, biomas, contratos, herramientas, sistemas del bus)
    se define en Resources (.tres) con espejo JSON en res://data/ y carga desde
    user://mods/. Prohibido definir contenido en código.
R3  GDScript con tipado estático en TODAS las declaraciones (variables, parámetros,
    retornos). Prohibido C#, GDExtension o addons nuevos sin ADR aprobado.
R4  Autoridad de red: el peer con autoridad sobre el bus (ADR-006) simula el bus y
    los paquetes sueltos en su interior; el host es dueño de economía, contratos,
    spawns y estado de sistemas. Nadie simula física de lo que no le pertenece.
R5  Prohibido agregar addons/dependencias sin aprobación humana explícita.
R6  Una tarea por sesión. No tocar archivos fuera del alcance declarado en el plan.
R7  Antes de declarar una tarea terminada: (a) el proyecto abre sin errores en
    consola, (b) los tests gdUnit4 pasan en headless, (c) los criterios de
    aceptación se probaron con evidencia adjunta (salida de tests, log, screenshot).
    Si un criterio no se puede probar desde CLI/editor, decirlo; nunca marcarlo.
R8  Scripts de máximo 400 líneas; escenas de máximo 60 nodos. Si crece, dividir.
R9  Código, nodos y archivos en inglés, snake_case. Comentarios solo para el porqué.
R10 No dejar TODOs silenciosos: todo pendiente se registra en BACKLOG.md.
R11 No inventar APIs. Verificar en la documentación de Godot 4.7 / GodotSteam antes
    de escribir; si hay duda, pedirla.
R12 Ediciones destructivas (borrar archivos, reescribir escenas existentes)
    requieren confirmación humana previa.
R13 Todo sistema de gameplay nuevo entrega con al menos un test gdUnit4 que lo
    ejercite sin editor abierto.
R14 Escenas y recursos siempre en formato texto (.tscn/.tres). Prohibido .scn/.res
    binarios. Prohibido cifrar scripts (moddabilidad, D11).
```

TAREA: **M0-T0.1 Fundaciones — Ronda 2.** Texto original (maestro, Parte 3): "Proyecto Godot 4.7.2, Forward+, Jolt, estructura §20, Git+LFS, `AGENTS.md`, gdUnit4 corriendo headless con un test trivial, puente MCP elegido y documentado. ✅ Acepta: `run_tests` pasa en ambas máquinas; commit inicial." La ronda 1 dejó el proyecto Godot funcionando; esta ronda corrige lo que la revisión 01 marcó como FALLA. Ejecuta el plan **v1.1** (no la v1.0 que usaste antes) y cierra cada hallazgo de la revisión listado abajo.

ARCHIVOS RELEVANTES (lee en este orden, completos):
1. `docs/revisiones/M0-T0.1_revision_01.md` — veredicto, hallazgos B1, M1–M5, m1–m8, notas, checklist PASA/FALLA.
2. `docs/planes/M0-T0.1_plan.md` — plan **v1.1**. Cambios frente a v1.0 en la cabecera. Secciones 5 (pasos), 7 (evidencia), 8 (checklist).
3. `docs/RUTA_FRAGIL_documento_maestro_v0.2_godot.md` — ahora completo (51 KB). Necesitas Parte 3 (M1–M8) y §15–18 (ADR-000..008).
4. `docs/CONTEXTO_RUTA_FRAGIL_v0.2.md` — ahora completo. Necesitas §3 (D1–D29).
5. `AGENTS.md` — ya tiene §0.3, §0.4 y §0.8 textuales. **No toques esas tres secciones.** Puedes actualizar "Comandos verificados" y "Rutas clave" si cambias el comando del arnés.
6. `tools/run_tests.ps1`, `tools/run_tests.sh`, `tests/smoke_test.gd`, `project.godot`, `README.md`, `DECISIONS.md`, `BACKLOG.md`, `CREDITS.md`, `docs/evidencia/M0-T0.1/`.

CONFIRMACIÓN R12 DEL DUEÑO PARA BORRAR `addons/gdUnit4/test/`: **[DUEÑO COMPLETA: SÍ / NO]**. Si este campo no dice SÍ, no borres nada: haz todo lo demás, detente en ese punto y pide la confirmación.

QUÉ CORREGIR (cada ítem cita el hallazgo de la revisión 01):

A. Arnés (`tools/run_tests.ps1` y `tools/run_tests.sh`, misma lógica en ambos) — hallazgo M1, plan v1.1 paso 6:
   1. Antes de correr gdUnit4, borrar `reports/` (o registrar la hora de inicio y aceptar solo un `results.xml` posterior). Verificado: cuando gdUnit4 no descubre tests imprime `No test cases found, abort test run!`, sale 0 y no escribe reporte; hoy tu guardia de ≥3 lee el reporte de la corrida anterior y aprueba.
   2. Fallar también si la salida del runner contiene `No test cases found`.
   3. Crear `reports/.gdignore` si no existe (Godot importa los PNG del HTML en cada `--import`; hallazgo m2).
   4. Decidir sobre `-d --remote-debug tcp://127.0.0.1:0` (hallazgo m1): probar la corrida sin ambos flags y repetir la prueba de la evidencia 05; si los errores de script siguen saliendo con traza, adoptar la forma sin flags; si no, conservarlos y documentar las dos líneas `ERROR` como salida esperada en `AGENTS.md` → "Comandos verificados". Registrar la decisión en `DECISIONS.md`.
   5. Etiqueta del código 101: "orphans (nodos huérfanos)", no "warnings".
   6. PowerShell 5.1 (hallazgo m4): fijar `$ErrorActionPreference = "Continue"`; no canalizar stderr nativo con `2>&1 | Out-String`; tolerar `tools/godot_bin.local` vacío, con BOM o con comillas. Funciones en snake_case (`write_step`, `fail`) o registrar la excepción PowerShell en `AGENTS.md` (hallazgo m6).

B. Addon (hallazgos M2 y M3, plan v1.1 paso 4) — solo si la confirmación R12 dice SÍ:
   1. Borrar `addons/gdUnit4/test/` completo (559 archivos: auto-tests del addon; la distribución oficial los excluye por `export-ignore`).
   2. `--import` ×2, `run_tests`, y verificar que `.godot/global_script_class_cache.cfg` no contiene ninguna ruta `res://addons/gdUnit4/test/`.
   3. Actualizar D32 en `DECISIONS.md`, `CREDITS.md` y la evidencia 08: "carpeta `addons/gdUnit4/` sin `test/`, igual que la distribución oficial; `src/dotnet/GdUnit4CSharpApi.cs` es parte de la distribución y queda inerte en build estándar". Retirar la "excepción R14": ya no hay `.scn`.
   4. Anotar en `AGENTS.md` → "Rutas clave" que los `CLAUDE.md`/`AGENTS.md` bajo `addons/` son de terceros y no aplican al proyecto (queda `addons/gdUnit4/src/asserts/CLAUDE.md`).

C. Documentos (hallazgos B1 resto, M5, m3, m7, m8):
   1. `DECISIONS.md`: D1–D29 copiados del briefing §3; resúmenes de ADR-000..008 desde el maestro §15–18 (ADR-000 ya no va marcado como reconstruido: cítalo del maestro). Anotar en D35 que los documentos fueron escritos por el revisor desde el chat con autorización del dueño el 2026-09-08.
   2. `BACKLOG.md`: M1–M8 con sus tareas desde el maestro Parte 3; añadir los pendientes propios: corrección del plan por formato de versión (`4.7.2-stable (official)`), 17 `.gd` sin `.uid` (irrelevante si se borra `test/`), decisión `--remote-debug`, `reports/` reimportado, y "al subir de parche 4.7.x tocar `tests/smoke_test.gd`, `tools/run_tests.ps1`, `tools/run_tests.sh`, `README.md`".
   3. `README.md`: receta Windows con `git lfs install` y `git config --global core.autocrlf input`; macOS ≥ 11 solo Intel, **≥ 13 en Apple Silicon**; comandos en la forma verificada (`powershell -ExecutionPolicy Bypass -File tools/run_tests.ps1`, `bash tools/run_tests.sh`); nota de que el remoto debe tener LFS antes del primer push (ya existen 13 punteros LFS por los PNG del addon); lista de archivos a tocar al subir de parche.
   4. `CREDITS.md`: Jolt sin "(c)" (la licencia upstream dice "Copyright 2021 Jorrit Rouwe").

D. Evidencia (hallazgos M4, m5; plan v1.1 sección 7), todo en `docs/evidencia/M0-T0.1/`, volcados completos, cabecera correcta en cada archivo:
   1. Recapturar `03_run_tests_pass.txt` (PowerShell) sobre el `project.godot` ya normalizado y con el arnés corregido; incluir también la corrida bash.
   2. Recapturar `04` y `05` con el arnés corregido. En `05` añadir una prueba con `:=` (`var y := 1`) para ejercitar `inferred_declaration=2`.
   3. Nuevo `09_no_verificado.txt`: mínimo `run_tests.sh` en la Mac con hash `ed1daf0bf`; remoto + LFS + primer push; carga del plugin gdUnit4 en el editor con ventana (dock visible).
   4. Nuevo `10_zero_tests_fail.txt`: corrida del arnés con `tests/` temporalmente vacío. **Debe fallar** por el guardia. Restaurar `tests/` después.
   5. Corregir la cabecera de `03a_hallazgo_version_format_fail.txt` (hoy dice `03_run_tests_pass.txt`).
   6. `07_git.txt` con volcados completos (`git status --short`, `git ls-files | grep -E '\.(uid|import)$'`, `git lfs ls-files`, `git ls-files -s | grep 100755`), no conteos.
   7. Si borraste `test/`: `08_gdunit4_pin.txt` actualizado con `ls addons/gdUnit4/` sin `test/` y el conteo 0 de rutas `res://addons/gdUnit4/test/` en la caché global.

CRITERIOS DE ACEPTACIÓN (checklist §8 del plan v1.1, incluidos los ítems marcados v1.1): todo PASA salvo los dos ítems "Solo el dueño" (Mac y remoto), que se declaran NO PROBADO en `09_no_verificado.txt`. En particular: evidencia 10 muestra la corrida con 0 tests fallando; `addons/gdUnit4/test/` no existe (si hubo SÍ); ningún "PENDIENTE" en `DECISIONS.md` ni `BACKLOG.md`; `git status` limpio tras el último commit.

INSTRUCCIONES:
0. Primero, sin tocar nada más: `git status` mostrará modificados `AGENTS.md`, `docs/RUTA_FRAGIL_documento_maestro_v0.2_godot.md`, `docs/CONTEXTO_RUTA_FRAGIL_v0.2.md`, `docs/planes/M0-T0.1_plan.md` y sin seguimiento `docs/revisiones/` y `docs/planes/M0-T0.1_ronda2_prompt.md`. Son del revisor. Cométealos tal cual en `m0/t0.1-foundations` con el mensaje `docs: review 01, plan v1.1, master and briefing from owner text (M0-T0.1)`. No los edites.
1. Responde PRIMERO con un plan (archivos a crear/modificar, enfoque, riesgos, y qué harás con el punto B según el campo R12). No toques nada aún.
2. Tras la aprobación del dueño, implementa en el orden A → B → C → D. Todo en `m0/t0.1-foundations`; nunca en `main`.
3. Corre `tools/run_tests.ps1` y `tools/run_tests.sh` en headless y adjunta la salida completa. Corre la prueba de 0 tests (D.4) y adjunta su salida.
4. Lista qué criterios NO pudiste verificar desde CLI/editor (`09_no_verificado.txt`) para que el dueño los valide (§0.7).
5. Tres intentos fallidos en cualquier punto = detenerse y reportar, no forzar.
6. Reporte final: hash del commit, lista de hallazgos cerrados (por su código B1/M1…/m8), lista de lo que quedó abierto y por qué, y la ruta de cada evidencia. Sin resúmenes de "todo verde": el revisor reproduce.

# Prompt para el agente ejecutor — M2-GATE Ronda 1 (gate duro de M2: dos instancias)

Copiar desde `CONTEXTO:` hasta el final. La ronda empieza **secuencial y para en el paso 1**: la medición decide si se construye la réplica o se habla de la opción B. Después, enjambre de dos agentes con interfaces fijadas.

---

CONTEXTO: Proyecto "Ruta Frágil" (Godot 4.7.2, GDScript tipado). Obedece R1–R14: están textuales en `AGENTS.md` §0.3. Repo local: `C:\Users\Leo\Documents\RutaFragil`. `main` tiene T0.1–T0.4, T1.1, T2.1, T2.2 y T2.3 mergeadas (180 tests). Crea la rama `m2/gate-two-instances` desde `main`. Hay dos archivos del revisor sin commitear (`docs/planes/M2-GATE_plan.md`, `docs/planes/M2-GATE_ronda1_prompt.md`): cométealos tal cual primero.

**Lee entero `docs/planes/M2-GATE_plan.md` antes de nada.** D91–D97 mandan, los hechos de su §3 son restricciones, su §6 es el orden. Esto es el resumen operativo.

TAREA: **M2-GATE — el gate duro de M2** (maestro Parte 3 y ADR-003 §16): con dos instancias locales, caminar, agarrar y amarrar dentro del bus en marcha sobre baches, 60 fps con ventana, sin jitter ni atravesamientos durante cinco minutos. **Es una medición con lo mínimo construido para medirla**, no M4: sin autoridad del conductor, sin reconexión, sin cuatro peers.

LOS HECHOS QUE CONDICIONAN TODO:

1. **El bus del cliente hoy se congela en `STATIC`** (`playground.gd`, `_freeze_bus_for_client`). Un cuerpo `STATIC` no transporta nada (T2.3: 22,2 m de resbalón por vuelta); `KINEMATIC` sí (0,185 m). Una tripulante local sobre el bus del cliente se resbalaría en la primera curva.
2. **La réplica va a 30 Hz y la física a 60**: el bus del cliente salta cada dos ticks y la velocidad de plataforma alterna 2·v y 0. Eso es jitter por construcción; la interpolación entre las dos últimas fotos es la candidata. **Se mide.**
3. El escenario de red corre **en tiempo real** (sin `--fixed-fps` con varios procesos), todas las instancias comparten `user://` (logs por `--log-file`, resultados por JSON), los IDs de peer son aleatorios (anfitrión 1), los RPC exigen rutas idénticas, puerto 47810 con cuatro alternativas.
4. Todo lo de T2.2 y T2.3: posición antes de entrar al árbol o de encender colisión; un cinemático con colisión dentro del casco patea el bus; capas D90; el rayo de suspensión solo lee mundo.
5. La demo tope en 64,6 km/h en el circuito; el gate habla de 80 km/h. Mide qué alcanza el bus sobre los baches a fondo; si 80 no se alcanza aquí, el gate corre a la máxima alcanzable **y lo dice**.

PASOS SECUENCIALES DEL COORDINADOR:

**Paso 0 — cierres, un commit cada uno, con test:** (a) archivos del revisor; (b) **r2.1**: `tools/run_tests.ps1` y `.sh` afirman el **conteo exacto** de tests (constante `EXPECTED_TESTS`, hoy 180) y fallan si difiere; demuéstralo quitando un test y viendo el arnés fallar, y restaurando; (c) **r2.2**: partir `tests/cargo/restraint_test.gd` en dos suites por tema, mismo conteo de tests; (d) BACKLOG: "gdUnit en un clon fresco ha omitido tres veces el último test de una suite (revisiones 04/05 de T2.2 y 02 de T2.3); causa desconocida; el conteo exacto del arnés lo caza".

**Paso 1 — medir antes de escribir (D92), y PARAR:** con el guion de red actual (anfitrión conduce la vuelta completa, `++ waypoints=16`; un cliente), pon una tripulante **local del cliente** de pie en el pasillo de su bus congelado (posición antes de `add_child`, `aboard = true`, sin réplica de nada nuevo) y mide 60 s en tres configuraciones del bus del cliente: (a) `STATIC` como hoy; (b) `freeze_mode = KINEMATIC`; (c) `KINEMATIC` + interpolación a 60 Hz del `global_transform` recibido (posición y rotación entre las dos últimas fotos, una foto de latencia). Columnas por configuración: deriva de la tripulante en el marco del bus (máx y p95), ticks sin suelo, violaciones del casco (bajo el piso, fuera por paredes), velocidad máxima del bus sobre los baches. **Reporta la tabla y una propuesta de tres líneas antes de tocar nada más.** Si la mejor configuración deriva más de 1 m o atraviesa, eso es la **iteración 1 fallida** de D96 y se discute antes de construir.

**Paso 2 — fase 0 del enjambre, un commit (tras la aprobación):** rutas de spawn fijas (`Crews/Crew_<peer>` para tripulantes por `MultiplayerSpawner` con `add_spawnable_scene`; `Cargo/<nombre>` para la carga), los RPC con sus firmas (`request_hold(path)`, `request_release(path, at, velocity)`, `request_strap(path, anchor_path)`, `request_unstrap(path)`, todos fiables hacia el anfitrión, que valida), la configuración de los sincronizadores (tripulantes `global_transform` a 30 Hz; carga a 20 Hz; interpolación en el receptor), el criterio de autoridad local que consultan `CameraArbiter`, `CrewInput` y `CrewHands` (`is_multiplayer_authority()` de la tripulante del grupo `crew`), y el esquema del JSON de métricas de D95. **Estas firmas no las cambia ningún agente.**

ENJAMBRE — dos agentes, propiedad exclusiva:

**Agente A — tripulantes (D93).** Posee `src/net/net_crew_sync.gd` (nuevo), los cambios mínimos de gating por autoridad en `crew_member.gd`, `crew_input.gd`, `crew_hands.gd`, `camera_arbiter.gd`, la retirada de la tripulante autorada en roles de red dentro de `playground.gd` (sección propia, coordinada con B), y `tests/net/crew_sync_test.gd`. Hace: una tripulante por peer con nombre legible; la local se simula, las remotas reciben transformada interpolada y no llaman a `move_and_slide`; **exactamente una cámara activa por instancia**, medido; input solo en la local.

**Agente B — carga (D94).** Posee `src/net/net_cargo_sync.gd` (nuevo), `package.gd` (no simular sin autoridad; aceptar autoridad cedida), los cuatro RPC y su validación en el anfitrión, y `tests/net/cargo_sync_test.gd`. Hace: sueltos con autoridad del anfitrión y clientes cinemáticos interpolados a 20 Hz; agarrar cede la autoridad al peer y soltar/lanzar la devuelve con `at` y `velocity` del que soltó; amarrar y desamarrar validados y replicados; ninguna instancia simula una caja que no le pertenece, **medido** (una caja del anfitrión empujada en el cliente no se mueve en el cliente hasta que llega la foto).

INTEGRACIÓN Y GATE (coordinador): `++ mode=gate` en `run_net_scenario`: anfitrión a fondo 300 s por el circuito con dos amarradas y dos sueltas; tripulantes guionizadas en las dos instancias caminando el pasillo y agarrando, soltando y amarrando en bucle; métricas por tick a JSON (D95: deriva local máx y p95, ticks sin suelo, violaciones del casco de tripulantes y cajas, error remoto de tripulantes y cajas en cada foto, fps); `tools/run_gate.ps1` y `.sh` que lanzan todo con una orden **y que el dueño puede usar con `++ human=client`** para jugar él la instancia cliente con ventana mientras el anfitrión corre sin ventana. **Primera iteración completa de 300 s, tabla entera.** Criterio de paso propuesto en D95 (cero violaciones; deriva p95 < 0,30 m y máx < 0,60 m; sin suelo < 1 %; error remoto p95 < 0,25 m; fps ≥ 60 con ventana). Si falla, corrección con causa medida y **siguiente iteración numerada; tres como máximo** (D96); a la tercera fallida, parar y escribir el plan de la opción B.

EVIDENCIA (`docs/evidencia/M2-GATE/`): `01_versions` · `02_run_tests` (tres corridas más la sonda `unsafe_*=2`) · `03_client_carry_measurement` (la tabla del paso 1, **antes** de construir) · `04_gate_iteration_N` (una por iteración, tabla completa y fps) · `05_windowed` (la vista del cliente con la tripulante remota y las cajas; el anfitrión en persecución) · `06_regression` (`run_demo`, red corta y larga, seis cifras de sensación) · `07_git` en dos partes · `08_no_verificado` · `09_limits` · `10_windowed_clean_log` (**los logs de las dos instancias**, por `--log-file`, cero ERROR y cero WARNING) · `11_paso0` (con la demostración de que el conteo exacto caza un test quitado).

CRITERIOS DE ACEPTACIÓN: paso 0 con tests; tabla del paso 1 con tres configuraciones y D92 citándola; una cámara activa por instancia (medido); autoridad de carga cedida y devuelta (medido en ambas instancias); ninguna instancia simula carga ajena (medido); al menos una iteración completa del gate con su tabla, y el veredicto A/B según D95–D96; `run_demo`, red corta y larga y sensación intactos; tres arneses en código 0 con el conteo exacto; logs de las dos instancias limpios; `git status` limpio; **nada fuera del alcance de §5 del plan** (`bus.gd`, la suspensión y `tuning.json` no se tocan, salvo campos nuevos de red declarados).

INSTRUCCIONES:
0. Commit de los archivos del revisor tal cual. Rama `m2/gate-two-instances` desde `main`.
1. Paso 0 y paso 1 en secuencia, y **para en el paso 1**: responde con la tabla de las tres configuraciones, la velocidad máxima alcanzada sobre los baches, tu propuesta de tres líneas para D92, y la lista de interfaces de la fase 0 tal cual las vas a commitear. **No repartas nada hasta la aprobación del dueño.**
2. Tras la aprobación: fase 0 en un commit, y los dos agentes en paralelo. Cada agente termina con un reporte corto: qué hizo, qué test lo prueba, qué firma le sobró o le faltó.
3. Integración, la primera iteración de 300 s con su tabla, y **antes de dar nada por bueno, juega tú la instancia cliente con ventana mientras el anfitrión conduce.**
4. Reporte final: hashes, los tres códigos de salida, la tabla del paso 1, la tabla de cada iteración, los logs limpios, las capturas, el veredicto A o B con la iteración en que se decidió, y lo que quede abierto.

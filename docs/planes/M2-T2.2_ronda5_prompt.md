# Prompt para el agente ejecutor — M2-T2.2 Ronda 5 exprés (el acelerador se queda pisado al levantarse)

Copiar desde `CONTEXTO:` hasta el final. Ronda corta y secuencial: un solo agente, sin enjambre. Estimación: veinte minutos de código, el resto es evidencia.

---

CONTEXTO: Proyecto "Ruta Frágil" (Godot 4.7.2, GDScript tipado). Obedece R1–R14: están textuales en `AGENTS.md` §0.3. Repo local: `C:\Users\Leo\Documents\RutaFragil`, rama `m2/t2.2-crew` (HEAD `39c2743`, tu commit de la revisión 04 y el prompt; el código es el de `9f19089`; `main` en `3c8eca8`). Los dos archivos del revisor (`docs/revisiones/M2-T2.2_revision_04.md`, `docs/planes/M2-T2.2_ronda5_prompt.md`) tienen **cambios sin commitear** posteriores a tu commit: la aprobación de tu plan con precisiones y el punto F. Cométealos tal cual primero.

**LA RONDA 4 PASA.** El revisor se sentó, condujo a 40 km/h y se levantó en marcha midiendo el bus: ω 0,0000 rad/s al sentarse, posición restaurada en el marco actual del bus, velocidad heredada, log limpio, y el test nuevo falla sobre `98b5994` con los mismos números que tu evidencia 13. Bien hecho. Pero al medir encontró un defecto **anterior a esta ronda** que ahora es alcanzable y que el dueño verá en el primer minuto de su gate. Se cierra antes del gate.

**TU PLAN DE TRES LÍNEAS ESTÁ APROBADO** (BusInput dueño del neutro vía setter; test primero con la acción real; punto de pasillo `(0, −0,55, −2,0)` verificado con sonda de solape; forma por tipo), con las cinco precisiones que el revisor escribió en el chat y que se repiten abajo dentro de A–D. **Y EL DUEÑO DECIDIÓ r3.1 el 2026-09-14: conductor a la IZQUIERDA.** Entra en esta ronda como punto F.

DIAGNÓSTICO DEL REVISOR (r4.6), medido sobre la escena real:

- `Seat.vacate()` pone `BusInput.enabled = false`, y `BusInput._physics_process` con `enabled` falso **retorna sin escribir nada**. El último `set_drive` queda latente en `Bus.drive_throttle` y el bus lo aplica cada tick.
- En llano: sentada, acelerador pisado dos segundos hasta 21,6 km/h; entonces la tecla se suelta y la tripulante se levanta **en el mismo tick**. Resultado: `drive_throttle` sigue en **1,00** y el bus acelera solo hasta **30,5 km/h** con la tripulante de pie en el pasillo.
- Control: la misma secuencia soltando la tecla **sin levantarse**: `BusInput` escribe el cero al tick siguiente y el bus frena solo de 21,6 a **13,2 km/h** en tres segundos. La diferencia es el pedal pegado.
- Viene del cableado de D76 en la ronda 1; en las rondas 1 a 3 nadie podía levantarse en marcha, así que nunca se vio.

TAREA: **M2-T2.2 — Ronda 5 exprés.** Que el bus vuelva a neutro cuando el conductor deja el asiento, con un test que lo garantice, más dos endurecimientos de tres líneas en los mismos archivos.

QUÉ HACER:

A. **Neutro al dejar el asiento (r4.6), dueño: `BusInput`.** Setter sobre `enabled`: cada vez que se asigne a `false` escribe `_bus.set_drive(0.0, 0.0, 0.0)` una vez, **idempotente** (no solo en la transición) y **con guarda `_bus != null`**, porque `_find_bus` es diferido y el `@export` puede asignarse antes de resolverlo; el bus arranca en neutro, así que saltarlo ahí no cuesta nada. Sin freno de mano: un bus sin conductor rueda libre y frena por su propia resistencia; que siga rodando es el caos que el concepto quiere, que siga acelerando, no. **Orden en los tests de la ronda 4:** ahora `_park_bus_input()` escribe ceros; los dos tests existentes ya aparcan **antes** de `set_drive(1, 0, 0)`, compruébalo y déjalo dicho en el docstring de la suite: aparcar después de mandar tracción borra el mando.

B. **El test primero (falla sobre `9f19089`).** En `seat_physics_test.gd`: cargar el Playground, subir a la tripulante al pasillo, ocupar, **acelerar con la acción real** (`Input.action_press("drive_accelerate")`, con `BusInput` habilitado, que es el camino del jugador; **este test NO aparca `BusInput`**) durante 120 ticks, que en llano son unos 6 m y no llegan a la rampa; soltar la acción y `vacate()` en el mismo tick; afirmar en el tick siguiente `bus.drive_throttle == 0.0` y `bus.drive_steer == 0.0` (exacto, es el literal que escribes), y 120 ticks después que la velocidad **no ha subido** (`speed_mps()` ≤ la velocidad al levantarse + 0,15 m/s). Enséñame los números del fallo con el código actual antes de tocar A. Referencia del revisor: sobre `9f19089` queda `1,00` y el bus pasa de 21,6 a 30,5 km/h; soltando sin levantarse queda `0,00` y baja a 13,2 km/h.

C. **Forma por tipo, no por nombre (r4.2).** `CrewMember.set_collision_disabled` busca `"CollisionShape3D"` por nombre de nodo. Recorre los hijos y actúa sobre los que sean `CollisionShape3D`, para que renombrar la forma al poner el arte no reactive en silencio la colisión sentada.

D. **Punto de restauración validado (r4.3), con su test.** En `Seat.vacate()`, antes de mover a la tripulante al punto guardado, comprueba que ese punto cae dentro del interior en coordenadas locales del bus (`|x| ≤ 1,15`, `z ∈ [−3,8, 3,8]`, `y ∈ [−0,65, 1,30]`, los mismos límites que ya usa el test). Si no, restaura al pasillo en local `(0, −0,55, −2,0)`, **verificado libre con tu sonda de solape** (cápsula de la tripulante, radio 0,3 y altura 1,75 con origen en los pies, contra las formas del interior real). Motivo: el revisor midió que levantarse con el punto guardado sobre el techo patea el bus a 16 rad/s; hoy no es alcanzable jugando, pero no dependemos de eso. **Un camino de código que nadie recorre no existe** (tu lección de la ronda 1): tercer test en la suite: ocupar, poner `seat.saved_local_position = Vector3(0.0, 1.4, -1.4)` (el techo donde se midió la patada; el campo es visible para tests para esto), `vacate()`, y afirmar que la tripulante cae en `(0, −0,55, −2,0)` con tolerancia 0,1, que está en el suelo tras 30 ticks, y que el bus sigue con ω < 0,05.

F. **Conductor a la izquierda (r3.1, decisión del dueño).** En Chile se conduce por la derecha: el conductor va a la izquierda y la puerta a la derecha, que es donde ya está. Cambios: en `bus_interior.tscn` intercambia la `x` de `Positions/driver` y `Positions/copilot` (conductor a `(−0,6, 0,4, −2,9)`, copiloto a `(+0,6, 0,4, −2,9)`); en `bus.tscn` mueve `CabinCamera` a `(−0,6, 0,75, −2,9)`, porque hoy está sobre el asiento derecho. **Edita los `.tscn` a mano con el editor de texto, no con el editor de Godot**, que reescribe la escena (el incidente de la ronda 3). El `seat_marker` del asiento no cambia: sigue apuntando a `Positions/driver`. **Pin:** en el test de escena existente, afirma que `Positions/driver.position.x < 0` y `CabinCamera.position.x < 0`, para que un arrastre accidental en el editor no lo devuelva a la derecha en silencio. **Decisión:** entrada nueva en `DECISIONS.md` (la siguiente a D79): "conductor a la izquierda, puerta a la derecha; razón: tránsito por la derecha en Chile; el maestro no lo fijaba". Recaptura la vista de cabina (`14_cabin_driving.png` o una `15_`): ahora la pared que llena un tercio del encuadre será la izquierda.

E. **Evidencia:** los tres arneses seguidos; la demostración del fallo del test de B antes de A con los números, en una sección nueva al estilo de la 13; una corrida **con ventana** en la que la tripulante acelere sentada, suelte la tecla y se levante en marcha, con la velocidad bajando después y el log de `user://logs/godot.log` limpio; la captura de cabina desde la izquierda; `git status` limpio.

CRITERIOS DE ACEPTACIÓN: tras `vacate()` el mando está en neutro en el tick siguiente y la velocidad no sube; el test de B existe, falla sobre el código viejo y pasa sobre el nuevo; C hecho; D hecho con su test; F hecho con su pin y su decisión; tres arneses en código 0 (esperados **145 tests**, con los pines de F dentro del test de escena existente); log con ventana limpio; `git status` limpio; **nada fuera de** `bus_input.gd`, `crew_member.gd`, `seat.gd`, `seat_physics_test.gd`, el test de escena (pines), `bus_interior.tscn` y `bus.tscn` (solo las tres posiciones dichas), `DECISIONS.md` y la evidencia.

INSTRUCCIONES:
0. Commit de los archivos del revisor tal cual.
1. Tu plan ya está aprobado. Confirma en dos líneas que asumes las precisiones de A–D y el punto F, y empieza. Si algo de F no cuadra con lo que ves en las escenas, dilo antes de tocarlo.
2. B antes que A. El test de D también antes que D.
3. **Antes de dar nada por bueno, siéntate, acelera, suelta y levántate con ventana.** El bus tiene que ir perdiendo velocidad con la tripulante de pie, y la vista de cabina tiene que salir del asiento izquierdo.
4. Reporte final: hashes, los tres códigos de salida, los números del test antes y después, el log limpio, la captura, y lo que quede abierto.

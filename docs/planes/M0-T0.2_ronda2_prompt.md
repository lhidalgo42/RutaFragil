# Prompt para el agente ejecutor — M0-T0.2 Ronda 2

Copiar desde `CONTEXTO:` hasta el final y pegarlo tal cual. No hay decisiones pendientes del dueño.

---

CONTEXTO: Proyecto "Ruta Frágil" (Godot 4.7.2, GDScript tipado). Obedece R1–R14: están textuales en `AGENTS.md` §0.3. Repo local: `C:\Users\Leo\Documents\RutaFragil`, rama `m0/t0.2-game-config` (HEAD `73305e8`; `main` = `e42f80b`, no se toca). Tu ronda 1 de M0-T0.2 fue revisada: la implementación es sólida (40/40 tests, 0 avisos `unsafe_*` con los cuatro elevados a error, 39/39 valores de §8.1, criterios 1–3 probados), pero NO pasa por cuatro mayores: tres huecos de prueba y un comportamiento silencioso en producción. Hay dos archivos del revisor sin commitear en el árbol (`docs/revisiones/M0-T0.2_revision_01.md`, `docs/planes/M0-T0.2_ronda2_prompt.md`): cométealos tal cual primero (paso 0).

TAREA: **M0-T0.2 — Ronda 2.** Cerrar M1–M4, los menores mc/mt/md y llevar las notas al BACKLOG, según `docs/revisiones/M0-T0.2_revision_01.md`. Sin cambios de diseño: D43–D47 se mantienen; D44/D45 se precisan.

ARCHIVOS RELEVANTES (lee completos, en este orden): `docs/revisiones/M0-T0.2_revision_01.md` · `docs/planes/M0-T0.2_plan.md` (§3, §4, §6, §9) · `src/core/data/game_data_loader.gd` · `src/core/data/resource_json_codec.gd` · `src/core/data/data_mirror.gd` · `src/autoloads/game_config.gd` · `src/tooling/import_data_mirror.gd` · `tests/core/data/game_data_loader_test.gd` · `tests/core/data/resource_json_codec_test.gd` · `tests/core/data/data_mirror_test.gd` · `docs/datos/esquema_v1.md` · `DECISIONS.md` · `BACKLOG.md` · `README.md`.

REGLAS DE TIPADO (siguen vigentes; ya las cumples): sin `:=`, todo declarado con tipo, `-> tipo` en todo `func`, fronteras `Variant` con `is` y variable tipada, sin `int(variant)`, sin `@warning_ignore`. Herramientas `-s` con `quit()` en todas las rutas y ejecutadas con `timeout 120`.

QUÉ HACER:

A. Mayores
   1. **M4 (código, decisión del revisor dentro del plan):** en `game_data_loader.gd`, un documento sin ninguna capa aplicada (`.tres` ni `.json` base) es `add_error`, no aviso. En `game_config.gd`, emitir `push_warning` por cada entrada de `last_report.warnings` en `_ready` y en `reload()`; `push_error(summary)` sigue solo si `!is_ok()`. `print_game_data` ya sale 1 con `!is_ok()`: verificar que ahora lo hace sin datos. Test nuevo en `game_data_loader_test.gd`: directorios vacíos → `is_ok()` falso y un error por documento. Actualizar D44/D45 en `DECISIONS.md` y la política de errores en `esquema_v1.md` (md1: la API es `is_ok()`, el `push_error` vive en el autoload).
   2. **M1 (tests):** en `game_data_loader_test.gd`, (a) `.tres` inyectado: `TuningTable` con `starting_money = 321` guardado con `ResourceSaver.save` en el `base_dir` temporal, sin JSON → 321; (b) `.tres`(321) + JSON(500) → 500 (orden de capas); (c) `.tres`(321) + JSON base truncado → `is_ok()` falso **y** 321; (d) reescribir el `.tres` (321 → 654) y recargar con el mismo loader → 654 (prueba `CACHE_MODE_IGNORE`).
   3. **M2 (tests):** mod con dos secciones donde una falla: `{"tuning": {"starting_money": "mucho", "rent_quota": 1}, "game_config": {"max_players": 2}}` sobre base `rent_quota 900`, `max_players 6` → 900, 6, `applied_files` solo base, un `REJECTED` en avisos. En `resource_json_codec_test.gd`: modo laxo (`unknown_is_error=false`) + error de tipo + clave hermana válida → `{}`.
   4. **M3 (tests):** nueva suite `tests/core/data/data_load_report_test.gd` (`is_ok` verdadero/falso; formato `"%s: %s"` de `add_error`/`add_warning`; `summary()` con cabecera `N applied, N warning(s), N error(s)` y una línea `[applied]`/`[warning]`/`[error]` por entrada). En el codec, test de campo `String` válido e inválido usando `label_text` del fixture.

B. Menores de código
   1. mc1: sustituir todo `if raw.is_empty(): return` por comprobación de errores nuevos en el reporte (`report.errors.size() != errors_before`) en `game_data_loader.gd` (base y mod), `data_mirror.gd` e `import_data_mirror.gd`; un `{}` base es entonces "sección faltante" (error) y un `{}` mod es aviso "sin secciones".
   2. mc2: en `_convert_int`, rechazar no finitos y `absf(f) > 9.0e18` con mensaje.
   3. mc3: en `_load_tres_layer`, `add_error` si `loaded` no es de la clase esperada; en `apply()`, `push_error` en el `continue` de clave fuera de esquema.
   4. mc4: `const DOCS: Dictionary[String, GDScript] = {"game_config": preload("res://src/core/data/game_config_data.gd"), "tuning": preload("res://src/core/data/tuning_table.gd")}` como única fuente; `_resource_for` y `_new_document` la usan; id desconocido → `push_error` + `null`. Ajustar `DOC_IDS` si queda redundante.
   5. mc5: en `_get_max_players`, `push_error("GameConfig.max_players read before data loaded")` cuando `data == null`.
   6. mc6: documentar en `esquema_v1.md` y README que el orden de mods es por bytes (ASCII); recomendar minúsculas.
   7. mc7: `fuel_consumption_l_per_km` pasa a `float` (4.0) en `tuning_table.gd`; regenerar `.tres` y `.json` con las herramientas; actualizar `esquema_v1.md` y D47.

C. Menores de tests y evidencia
   1. mt1: orden alfabético: escribir `zz.json` antes que `aa.json`; `aa` con `rent_quota`, `zz` con `starting_money`; afirmar ambos valores y `applied_files` en orden `[base, aa, zz]`.
   2. mt2: `test_real_res_data_loads_without_errors`: afirmar `applied_files.size() == 4` y `warnings.is_empty()`.
   3. mt3: mod parcial con hermano de nivel superior (`starting_money 500` en base, mod solo `base_pay`) → 500 intacto.
   4. mt4: afirmar `is_ok()` en las dos cargas del test de recarga.
   5. mt5: en `json_file_test.gd`, comprobar que la línea del error es entero.
   6. mt6: registrar en `DECISIONS.md` (nota a D44) y en `docs/planes/M0-T0.2_plan.md` §3 el hecho verificado "`Dictionary ==` es estricta `int`/`float` también en valores; `check_mirror` compara semánticamente vía ida y vuelta JSON". Reforzar `test_real_data_mirror_is_in_sync`: además de `check_mirror`, escribir con `DataMirror.write_mirror` a `create_temp_dir` y afirmar que el texto es idéntico al de `res://data/<doc>.json`.
   7. mt7/mt8: al final, recapturar 02 (arnés completo), 06 (con la suite de deriva corrida **después** de regenerar) y 07 (HEAD final, listados completos). Añadir a 08 el hallazgo de `Dictionary ==` y los dos `.gitkeep` borrados (R12: placeholders sin contenido).

D. Documentos y BACKLOG
   1. md2: en `esquema_v1.md`, "los archivos base son mono-documento; claves raíz adicionales se ignoran sin aviso" (o implementar el aviso y documentarlo).
   2. md3: marcar "arrays se reemplazan" y "escalar sobre diccionario" como propiedades de `JsonMerge` para documentos futuros; en v1 ningún campo admite arrays y un tipo distinto rechaza el archivo.
   3. md5: `schema_version` "informativo en v1, sin comprobación ni migración".
   4. md6: renombrar `bus_wear_per_contract_max` → `bus_integrity_max_loss_per_contract` (magnitud positiva que se resta a `bus_integrity_max`; §8.1 "−5") en `.gd`, `.tres`, `.json`, esquema y D47; o, si prefieres conservar el nombre, explicar signo y semántica en la tabla. Elige y justifica en el plan de respuesta.
   5. md8: README: `src/tooling/` en el mapa de carpetas; enlace al plan de T0.2; ejemplo de archivo de mod (`user://mods/zz_mi_mod.json` → `{"tuning": {"starting_money": 999}}`); nota "`.tres` autoritativo para el editor; en caliente gana la última capa; el test de deriva mantiene ambos iguales"; nota "los `.tres` se regeneran solo con la herramienta, no guardar desde el Inspector".
   6. BACKLOG (R10): codec sin `Array` ni otros `Dictionary[K,V]` (antes de T3.1); validación semántica (`max_players ≥ 1`, `≤ hard_limit`, `min ≤ max`, fracción en [0,1]; destino lobby T4.1); `*.json` en el filtro de export (M4); "flotador ×4" y reglas cualitativas de §8.1 sin parámetro; `schema_version` sin comprobar; contradicción §13/§19 sobre `base_pay` y claves con namespace (T3.1); `reload()` reemplaza objetos, consumidores deben reengancharse en `reloaded` (T0.3+).
   7. Fixture: añadir `tests/core/data/fixtures/` a la lista de archivos del plan (nota al pie en `docs/planes/M0-T0.2_plan.md` §7) o declararlo en 07.

CRITERIOS DE ACEPTACIÓN: checklist §9 del plan en PASA incluido R13; ambos arneses en verde con **≥ 50** tests; `print_game_data` con `data/` renombrado temporalmente → código 1 y error por documento (evidencia nueva `09_no_data.txt`; restaurar y `git status` limpio); evidencias 02, 06, 07 recapturadas con el HEAD final; `git status` limpio; ningún archivo fuera de los listados.

INSTRUCCIONES:
0. Commit de los dos archivos del revisor tal cual: `docs: review 01 and round-2 prompt (M0-T0.2)`.
1. Responde PRIMERO con un plan corto (archivos, orden, la decisión sobre md6 y cualquier contradicción que veas). No toques nada aún.
2. Tras la aprobación del dueño, implementa A → B → C → D en `m0/t0.2-game-config`. Itera código → `run_tests` → código hasta verde.
3. Corre ambos arneses y adjunta la salida completa; corre la evidencia 09.
4. Actualiza `08_no_verificado.txt`.
5. Tres intentos fallidos en cualquier punto = detenerse y reportar.
6. Reporte final: hash del commit, ítems cerrados por código (M1–M4, mc, mt, md), conteo de tests, lo que quedó abierto y por qué, ruta de cada evidencia. Sin resúmenes de "todo verde": el revisor reproduce.

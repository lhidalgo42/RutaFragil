# Prompt para el agente ejecutor — M2-T2.2 Ronda 2 (arreglo bloqueante)

Copiar desde `CONTEXTO:` hasta el final.

---

CONTEXTO: Proyecto "Ruta Frágil" (Godot 4.7.2, GDScript tipado). Obedece R1–R14: están textuales en `AGENTS.md` §0.3. Repo local: `C:\Users\Leo\Documents\RutaFragil`, rama `m2/t2.2-crew` (HEAD `5637655`; `main` sigue en `fc34b16`). Hay dos archivos del revisor sin commitear (`docs/revisiones/M2-T2.2_revision_01.md`, con la corrección del veredicto, y `docs/planes/M2-T2.2_ronda2_prompt.md`), más `docs/revisiones/M2-T2.1_revision_01.md` modificado con una retractación: cométealos tal cual primero.

**EL DUEÑO NO PUDO JUGAR.** Intentó conducir y el juego le dio errores. Diagnóstico del revisor sobre su propio registro, concluyente:

- **Las seis acciones de andar no existen.** `crew_input.gd` lee `walk_forward`, `walk_back`, `walk_left`, `walk_right`, `walk_sprint` y `walk_jump`. En `project.godot` solo están registradas `drive_accelerate`, `drive_brake`, `drive_steer_left`, `drive_steer_right`, `drive_handbrake`, `toggle_camera` y `toggle_demo`. Su registro acumula **11.232 líneas de error**, seis por fotograma.
- **No existe ninguna acción de interactuar.** `seat.gd` expone ocupar y dejar el asiento, pero nada en `crew_input.gd` lee una tecla para llamarlos. La tecla E que anunciaste en tu reporte no está registrada ni leída en ninguna parte.

Consecuencia: no se puede andar, no se puede llegar al asiento y no se puede conducir desde él. La tarea entrega al dueño una funcionalidad que no funciona. Tu evidencia 08 declara que las teclas no se probaron porque headless no entrega input, y eso es cierto y honesto, pero declarar algo como no probado no es lo mismo que haberlo construido. El revisor también se equivocó al dar APTA sin hacer la comprobación estática obvia; está reconocido por escrito en la revisión.

TAREA: **M2-T2.2 — Ronda 2.** Que el dueño pueda jugar, y que esto no pueda repetirse.

QUÉ HACER:

A. **Registrar las seis acciones de andar** en la sección `[input]` de `project.godot`, con el mapa de §9.2: W adelante, S atrás, A izquierda, D derecha, Shift correr, Espacio saltar.

B. **Añadir la acción de interactuar** (`interact`, tecla E) y **conectarla en `crew_input.gd`**: al pulsarla, buscar el asiento más cercano dentro de un alcance razonable (`interaction_reach_m` ya existe en `TuningTable`) y ocuparlo; estando sentado, la misma tecla levanta. Nada de esto existe hoy; no es solo configuración.

C. **El test que impide la recaída, y es el punto importante de esta ronda:** un test estático que recorra los scripts de `src/`, extraiga todos los nombres de acción usados con `Input.is_action_*` y afirme que **cada uno existe en el mapa de entrada**. Sin ventana, sin input simulado, corre en el arnés de siempre. Si alguien añade mañana una acción y olvida registrarla, el arnés lo caza.

D. **Cerrar los tres hallazgos de la revisión**, cada uno en su commit: r1.1, una entrada de decisión que describa lo que el bus es ahora, un casco hueco con placa de piso de 0,2 m en vez de caja maciza, porque toda tarea posterior razona sobre esa forma; r1.2, leer la gravedad del ajuste del proyecto en vez de la constante 9,8 en `crew_member.gd`; r1.3, conservar `src/tooling/probe_handling.gd` o explicar con qué se sustituye, porque es la herramienta que mide las cifras de sensación que el dueño aprobó.

E. **Recapturar la evidencia 02** y añadir una **06 nueva**: una corrida con ventana de al menos veinte segundos cuyo registro **no contenga ni una línea de error**. Ese es el criterio que faltaba: hoy el juego escupía seis errores por fotograma y la evidencia no lo reflejaba.

CRITERIOS DE ACEPTACIÓN: los tres arneses seguidos en código 0; el test de C existe y falla de verdad si se le quita una acción del mapa (demuéstralo quitando una temporalmente y enseñando el fallo); una corrida con ventana de veinte segundos sin una sola línea de error en el registro; andar, entrar, sentarse, conducir, levantarse y salir funcionan de verdad con teclado; `run_demo`, la red y las cifras de sensación intactos; `git status` limpio.

INSTRUCCIONES:
0. Commit de los archivos del revisor tal cual.
1. Responde PRIMERO con un plan de cuatro líneas. No toques nada aún.
2. Tras la aprobación del dueño, implementa A a E.
3. **Antes de dar nada por bueno, abre el juego con ventana y comprueba el registro.** El defecto de esta ronda era visible en la primera línea del log y nadie lo miró.
4. Reporte final: hashes, los tres códigos de salida del arnés, la demostración de que el test de C falla cuando debe, el registro limpio de la corrida con ventana, y lo que quede abierto. Sin resúmenes de "todo verde": el dueño ya comprobó lo que vale eso.

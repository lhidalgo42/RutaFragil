# Prompt para el agente ejecutor — M1-T1.1 Ronda 1

Copiar desde `CONTEXTO:` hasta el final y pegarlo tal cual. Las decisiones D59–D65 del plan quedan aprobadas por el dueño al entregar este prompt, incluido el borrado autorizado de `placeholder_bus.*` (R12).

---

CONTEXTO: Proyecto "Ruta Frágil" (Godot 4.7.2, GDScript tipado). Obedece R1–R14: están textuales en `AGENTS.md` §0.3; también §0.4 y §0.8. Repo local: `C:\Users\Leo\Documents\RutaFragil`, `main` = `fc34b16` con M0 completa (96 tests, arnés con bloque de red, Playground con circuito de 17 waypoints, `DemoDriver`, `run_demo`, arnés de red multi-instancia). Crea la rama `m1/t1.1-bus` desde `main`; nunca trabajes en `main`. Hay dos archivos del revisor sin commitear (`docs/planes/M1-T1.1_plan.md`, `docs/planes/M1-T1.1_ronda1_prompt.md`): cométealos tal cual primero (paso 0a).

TAREA: **M1-T1.1 — Bus con suspensión por raycast y cámaras del conductor.** El pliego completo es `docs/planes/M1-T1.1_plan.md`: léelo entero antes de planificar. Es la primera tarea del proyecto que produce algo jugable, y su criterio de aceptación no es un test verde: es que el dueño conduzca tres minutos y diga si se siente controlable, con peso y sin tumbarse solo.

Decisiones ya tomadas: D59 (`Bus` sustituye a `PlaceholderBus` conservando su interfaz pública, y las herramientas lo buscan por el grupo `bus` en vez de por nombre de nodo), D60 (cuatro ruedas por raycast con resorte y amortiguador, fuerzas aplicadas en el punto de cada rueda), D61 (los doce parámetros de sensación viven en `TuningTable` y en `data/`, nunca en código), D62 (primera sección `[input]` del proyecto, subconjunto de conducción de §9.2), D63 (cámara de cabina en primera persona más la de persecución que ya existe, con conmutador), D64 (freno de mano derrapante), D65 (la vuelta automática del Playground y el bloque de red son la prueba de regresión).

HECHOS MEDIDOS POR EL REVISOR QUE NO DEBES REDESCUBRIR (plan §3). Los dos primeros cuestan una ronda entera si se adivinan mal:
- **`apply_force(force, position)`: `position` es un desplazamiento desde el ORIGEN del cuerpo en orientación GLOBAL, no local.** Con el cuerpo girado 90° en Y, pasar el desplazamiento crudo hace que el par caiga sobre el eje X global (producto punto 1,0000) y no sobre el del cuerpo (0,0000); pasar `global_basis * desplazamiento` lo pone donde toca (0,9999). **Multiplica siempre el desplazamiento de la rueda por `global_basis`.** Si te equivocas, el par cae sobre el eje de alabeo, que tiene mucho menos inercia: la velocidad angular medida fue seis veces mayor y el bus se revuelca al girar.
- **El raycast desde `_physics_process` funciona headless** con `get_world_3d().direct_space_state.intersect_ray(PhysicsRayQueryParameters3D.create(desde, hasta))` y `query.exclude = [cuerpo.get_rid()]`; devuelve posición, normal, colisionador y distancia exacta. No necesitas nodos `RayCast3D`.
- Vigentes de M0: la física headless corre en tiempo real a 60 Hz, así que los tests esperan `physics_frame` y nunca frames de proceso; headless **no entrega `InputEvent`**, así que el input no se prueba sin ventana y los tests son de lógica; escribir `linear_velocity` dentro de `_physics_process` pisa lo que el solver integró ese tick.

ARCHIVOS RELEVANTES (lee completos, en este orden): `docs/planes/M1-T1.1_plan.md` · `AGENTS.md` · `src/vehicle/placeholder_bus.gd` (lo que vas a sustituir, y su interfaz) · `src/vehicle/demo_driver.gd` · `src/vehicle/chase_camera.gd` · `scenes/playground.tscn` · `src/biomes/playground/playground.gd` · `src/tooling/run_demo.gd` · `src/tooling/run_net_scenario.gd` · `src/net/net_scenario_util.gd` · `src/core/data/tuning_table.gd` · `data/tuning.json` · maestro §4.1, §4.5, §9.1, §9.2, §18 y Parte 3.

REGLAS DE TIPADO (vigentes): sin `:=`, todo con tipo, `-> tipo` en todo `func`, `for x: T in ...`, fronteras `Variant` con `is` y variable tipada, `preload` tipado, sin `@warning_ignore`. Herramientas `-s` con `quit()` en todas las rutas y `timeout` externo.

QUÉ HACER: los pasos 0 a 8 del plan §6, en orden. El paso 0b cierra un residuo de T0.4: que el JSON de abandono del cliente incluya la marca de conectado, para que el resumen no acuse a los clientes cuando el que se cayó fue el host.

Dos cosas que quiero subrayar del plan. La primera: **los parámetros de sensación van a `data/`, no a `@export` ni a constantes**, porque el dueño va a querer ajustarlos editando un JSON sin abrir el editor, que es justo para lo que se construyó T0.2. La segunda: **el paso 6 exige medir antes de ajustar**. Tiempo de 0 a 60, velocidad máxima, radio de giro, distancia de frenado, balanceo máximo y altura de reposo, anotados antes y después de cada cambio. Sin números no hay ajuste, hay opinión, y el revisor va a pedir la tabla.

CRITERIOS DE ACEPTACIÓN (plan §7–§8): los tres arneses seguidos en código 0 con el bloque de red; `run_demo` termina en vuelta completa con el bus nuevo; el guion de red pasa en ruta corta y larga; la suspensión es de verdad por rueda, comprobable porque con una rueda al aire el bus se inclina en vez de flotar; ningún número de sensación en `.gd`; las dos cámaras funcionan con su conmutador y hay captura de ambas; la tabla de medidas del paso 6 con antes y después; sonda de accesos inseguros en verde con su diff dentro de la 02; `git diff main..HEAD -- project.godot` solo con la sección `[input]`; `git status` limpio; nada fuera del alcance del plan §5.

INSTRUCCIONES:
0. Commits de arranque: (a) los dos archivos del revisor tal cual, `docs: plan and round-1 prompt (M1-T1.1)`; (b) el residuo r2.1 de T0.4 en un commit aparte.
1. Responde PRIMERO con un plan corto: archivos, orden, cómo estructuras el bucle de suspensión, y qué valores de partida vas a usar. No toques nada aún.
2. Tras la aprobación del dueño, implementa los pasos 1 a 8 iterando código y arnés.
3. Si tras tres ciclos de medir y ajustar el bus no completa la vuelta automática, **detente y reporta con la tabla de medidas**, no sigas improvisando. Si el campo de saltos resulta intratable para un bus con suspensión, ajusta la geometría y repórtalo con números; nunca aflojes el criterio del demo ni toques el conductor automático para que pase.
4. Si algo te obliga a salirte del alcance del plan §5, detente y repórtalo antes de escribirlo.
5. Reporte final: hashes de los commits, los tres códigos de salida del arnés, la tabla de medidas antes y después, los valores finales de los doce parámetros, qué tuviste que tocar de la escena y por qué, y lo que quede abierto. Sin resúmenes de "todo verde": el revisor reproduce y mide.

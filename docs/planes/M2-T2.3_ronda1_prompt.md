# Prompt para el agente ejecutor — M2-T2.3 Ronda 1 (agarrar, soltar, lanzar y amarrar)

Copiar desde `CONTEXTO:` hasta el final. Ronda en **enjambre**: un coordinador hace los pasos secuenciales (0, 1 y la fase 0 de interfaces), tres agentes trabajan en paralelo con **propiedad exclusiva de archivos**, y el coordinador integra. Nadie toca un archivo que no posee.

---

CONTEXTO: Proyecto "Ruta Frágil" (Godot 4.7.2, GDScript tipado). Obedece R1–R14: están textuales en `AGENTS.md` §0.3. Repo local: `C:\Users\Leo\Documents\RutaFragil`. `main` está en `6df7824` con T1.1, T2.1 y T2.2 mergeadas (145 tests). Crea la rama `m2/t2.3-cargo` desde `main`. Hay dos archivos del revisor sin commitear (`docs/planes/M2-T2.3_plan.md`, `docs/planes/M2-T2.3_ronda1_prompt.md`): cométealos tal cual primero.

**Lee entero `docs/planes/M2-T2.3_plan.md` antes de nada.** Las decisiones D81–D88 mandan, los hechos medidos de su §3 son restricciones, y su §6 es el orden de trabajo. Lo que sigue es el resumen operativo.

TAREA: **M2-T2.3 — Agarrar, soltar, lanzar y amarrar, con los tres estados físicos de ADR-003:** suelto (`RigidBody3D` real), en mano (cinemático, sin colisión, sigue la mano), amarrado (hijo congelado del bus en un anclaje). Paquete = caja greybox de 0,4 m y 8 kg; nada de contenido, tipos ni `PackageDefinition` (M3). Un objeto en mano a la vez. El gate duro de dos instancias es la tarea siguiente (D88), no esta.

LOS HECHOS QUE CONDICIONAN TODO (medidos en T2.2, no negociables):

1. **Un cuerpo cinemático con colisión activa dentro del casco despenetra el bus cada tick** (la ronda 4 del asiento: 0,4 m y 8°; sobre el techo, 16 rad/s). Un paquete en mano es cinemático y va dentro del casco: **su colisión va apagada mientras esté en la mano**.
2. **Posicionar antes de entrar al árbol o de encender la colisión.** Siempre. Un tick en el sitio equivocado lanza a los dos cuerpos.
3. **`call_deferred` en un tick de física se ejecuta al final de ese tick**, antes del paso del servidor: por eso la pareja "apagar y mover" / "mover y encender" funciona.
4. **Un `RigidBody3D` suelto sobre el piso del bus se mueve por fricción**, no por el acarreo de plataforma que usa la tripulante. **No está medido: es el paso 1.**
5. El input headless funciona por acciones (`Input.action_press/release`); la pulsación mantenida se mide en ticks.

PASOS SECUENCIALES DEL COORDINADOR (antes de repartir nada):

**Paso 0 — cierres, un commit cada uno, con test:** (a) archivos del revisor; (b) **r4.1**: mientras la tripulante está sentada, `Seat` la arrastra al marcador cada tick de física; aserción en `seat_physics_test`: tras 5 s conduciendo sentada, su posición local es la del marcador (hoy queda 30 m detrás del bus); (c) **r4.5**: test "ocupar el asiento con el bus a 40 km/h", bus estable con los umbrales del asiento; (d) **F1**: al salir de la demo, `bus_input.enabled` sigue a `crew.seated` (hoy queda `true` con la tripulante a pie y W acelera el bus aparcado), con test; (e) **pines de los 16 waypoints** del circuito en el test de escena, tolerancia 0,01.

**Paso 1 — medir antes de escribir (D85):** sonda temporal: cuatro `RigidBody3D` caja de 8 kg sobre el piso del bus, posicionadas antes de `add_child`, la vuelta de la demo con `run_demo`, registrando por caja y por segundo: posición en el marco del bus, base bajo el piso (sí/no), fuera del casco (sí/no), dormida (sí/no). Dos corridas: `continuous_cd` apagado y encendido. **Reporta la tabla al dueño ANTES de escribir `Package`.** Si algo atraviesa el piso, dilo con el segundo y la caja. Evidencia `03_free_cargo_measurement`.

**Paso 2 — fase 0, interfaces fijadas, un commit:** crea con firmas vacías y docstrings: `src/cargo/package.gd` (`class_name Package extends RigidBody3D`; `enum Restraint { FREE, HELD, STRAPPED }`; `var restraint: Restraint`; `signal restraint_changed(from: Restraint, to: Restraint)`; `func hold(by: CrewMember) -> bool`; `func release(velocity: Vector3) -> void`; `func strap(anchor: RestraintAnchor) -> bool`; `func unstrap() -> void`), `src/cargo/restraint_anchor.gd` (`class_name RestraintAnchor extends Marker3D`; `var occupant: Package`; `func is_free() -> bool`; grupo `restraint_anchor`), `src/crew/crew_hands.gd` (`class_name CrewHands extends Node`; `var held: Package`; `signal strap_progress(fraction: float)`; `func try_grab() -> bool`; `func drop() -> void`; `func throw() -> void`; `func begin_strap() -> void`; `func cancel_strap() -> void`), los cuatro campos de `TuningTable` con espejo JSON y `.tres` (`package_mass_kg` 8, `package_throw_speed_mps` 6, `package_hold_distance_m` 0,7, `strap_hold_seconds` 1,5), las acciones `throw` (botón izquierdo) y `drop` (botón derecho) en `project.godot`, y el `HandAnchor` (Marker3D) en `crew_member.tscn`. **Estas firmas no las cambia ningún agente.** Si una no sirve, el agente lo dice en su reporte y solo el coordinador la cambia, para todos.

ENJAMBRE — tres agentes en paralelo, propiedad exclusiva:

**Agente A — `Package` y los sueltos.** Posee: `src/cargo/package.gd`, `src/cargo/package.tscn`, `tests/cargo/package_test.gd`, `tests/cargo/free_cargo_ride_test.gd`. Hace: transiciones con precondiciones (no se agarra un amarrado sin desamarrar; no se amarra sin estar en mano); `hold` apaga la colisión y congela en modo cinemático; `release` posiciona, fija velocidad y **enciende la colisión después**, como pareja diferida; capa 2 `cargo`, máscara mundo + cargo + tripulante; lo que la tabla del paso 1 pida para los sueltos (fricción, amortiguador de velocidad relativa, CCD), **citando la tabla en el commit**. Tests: unitarios de transiciones; integración con cuatro sueltos en la vuelta de la demo: cero bases bajo el piso, cero fuera del casco por las paredes.

**Agente B — Manos.** Posee: `src/crew/crew_hands.gd`, `src/crew/crew_input.gd`, `src/crew/crew_member.tscn` (solo el `HandAnchor`), `tests/crew/crew_hands_test.gd`. Hace: elegir objetivo al alcance (`interaction_reach_m`, el `Package` más cercano al rayo de la mirada); seguir el `HandAnchor` cada tick de física con `global_transform`; soltar con prueba de solape (si la caja no cabe en la mano, a los pies sobre el piso) y velocidad del punto del bus `v + ω × r`; lanzar con velocidad = adelante de la cámara × `package_throw_speed_mps` + la del punto del bus, **la composición como función pura** probada sin física; E como detector de duración en ticks (toque = agarrar/desamarrar/asiento, mantenido = amarrar, con `strap_progress` emitido); **un clic con el puntero libre solo recaptura, nunca lanza**. **El test que habría cazado la ronda 4:** con una caja en la mano dentro del bus a 40 km/h, el bus no cambia ω (< 0,05) ni altura (< 0,05 m) durante 120 ticks.

**Agente C — Amarres.** Posee: `src/cargo/restraint_anchor.gd`, las 12 líneas de `src/vehicle/bus_interior.tscn` que dan script y grupo a `Restraints/restraint_*` (editadas como texto, no con el editor), `tests/cargo/restraint_test.gd`. Hace: ocupación (un anclaje, un paquete); progreso del amarre como función pura (ticks mantenidos → fracción, cancela al soltar E); `strap` reparenta al bus en el anclaje **con la posición fijada antes**, congelado cinemático, colisión encendida; `unstrap` devuelve a la mano. Integración: dos amarrados en la vuelta de la demo, **deriva respecto al anclaje 0,000**, y la tripulante choca con ellos como con un estante.

INTEGRACIÓN (coordinador, tras los tres reportes): nodo `Cargo` en el Playground con cuatro paquetes sobre estantes y piso, posicionados antes de `add_child`; cableado `CrewInput → CrewHands → Package/RestraintAnchor`; pines de escena (cuatro paquetes en capa 2, doce anclajes con script y grupo); **el criterio de un jugador con carga**: la vuelta a ~70–80 km/h sobre los saltos con dos amarrados, dos sueltos y la tripulante caminando: cero atravesamientos, cero fuera del casco por las paredes, deriva de los amarrados 0,000, fps con ventana. **Y una jugada con ventana antes de dar nada por bueno:** agarrar, llevar por el pasillo, soltar, lanzar, amarrar manteniendo E, desamarrar, conducir con carga, levantarse en marcha con una caja en la mano.

EVIDENCIA (`docs/evidencia/M2-T2.3/`): `01_versions` · `02_run_tests` (tres corridas más la sonda `unsafe_*=2` con su diff) · `03_free_cargo_measurement` (la tabla del paso 1, dos corridas, **antes** de implementar) · `04_cargo_ride` (la vuelta con carga y sus números, más fps) · `05_windowed` (capturas: caja en la mano en primera persona, amarre en curso con progreso, cajas saltando en un bache) · `06_regression` (`run_demo`, red, seis cifras de sensación) · `07_git` en dos partes · `08_no_verificado` · `09_limits` · `10_windowed_clean_log` (log de `user://logs/godot.log`, cero ERROR y cero WARNING) · `11_paso0` (los cinco cierres con sus números).

CRITERIOS DE ACEPTACIÓN: paso 0 cerrado con tests; la tabla del paso 1 existe y lo implementado la cita; una caja en mano dentro del bus en marcha no mueve el bus (medido); soltar y lanzar posicionan antes de encender colisión, y soltar contra una pared cae a los pies; amarrado con deriva 0,000 en una vuelta, desamarrar devuelve a la mano, un anclaje un paquete; sueltos sin atravesar piso ni paredes en una vuelta; clic con puntero libre no lanza; los cuatro números en `TuningTable`/JSON/`.tres` y ninguno en código; cada `add_child` y cada reactivación de colisión con la posición antes; `run_demo`, red y sensación intactos; tres arneses en código 0; log con ventana limpio; `git status` limpio; **nada fuera del alcance de §5 del plan** (en particular: `bus.gd`, la suspensión y `camera_arbiter.gd` no se tocan).

INSTRUCCIONES:
0. Commit de los archivos del revisor tal cual. Rama `m2/t2.3-cargo` desde `main`.
1. Paso 0 y paso 1 en secuencia, y **para en el paso 1**: responde con la tabla de las dos corridas y tu propuesta de tres líneas para los sueltos (qué haces con fricción, amortiguador y CCD, y por qué según la tabla), y con la lista de interfaces de la fase 0 tal cual las vas a commitear. **No repartas nada a los agentes hasta la aprobación del dueño.**
2. Tras la aprobación: fase 0 en un commit, y entonces los tres agentes en paralelo. Cada agente termina con un reporte corto: qué hizo, qué test lo prueba, qué firma le sobró o le faltó.
3. Integración, criterio con carga, jugada con ventana.
4. Reporte final: hashes, los tres códigos de salida, la tabla del paso 1 y la del paso 5, el log limpio, las capturas, lo que quede abierto, y qué firma de la fase 0 cambiaste y por qué, si alguna.

# Prompt para el agente ejecutor — M2-T2.1 Ronda 1

Copiar desde `CONTEXTO:` hasta el final. Requiere que T1.1 esté mergeada en `main` antes de empezar.

---

CONTEXTO: Proyecto "Ruta Frágil" (Godot 4.7.2, GDScript tipado). Obedece R1–R14: están textuales en `AGENTS.md` §0.3; también §0.4 y §0.8. Repo local: `C:\Users\Leo\Documents\RutaFragil`. **M1-T1.1 pasó el gate del dueño**, que condujo el bus y lo aprobó: "se ve bien, se siente bien, la amortiguación se ve bien". Crea la rama `m2/t2.1-interior` desde `main` con T1.1 ya mergeada; nunca trabajes en `main`. Hay dos archivos del revisor sin commitear (`docs/planes/M2-T2.1_plan.md`, `docs/planes/M2-T2.1_ronda1_prompt.md`) y también `docs/revisiones/M1-T1.1_revision_01.md` si aún no está commiteado: cométealos tal cual primero.

TAREA: **M2-T2.1 — Interior greybox del bus según §4.3**, seis posiciones útiles y pasillo de doble ancho. El pliego completo es `docs/planes/M2-T2.1_plan.md`: léelo entero antes de planificar.

**QUÉ NO ES ESTA TAREA, y es importante porque el dueño ha pedido justo lo siguiente:** aquí no hay personaje, no se camina, no se entra ni se sale, nadie se sienta y nadie conduce desde el asiento. Eso es T2.2, y agarrar y amarrar es T2.3. Esta tarea construye el espacio y sus anclajes para que T2.2 tenga dónde ocurrir. No la adelantes.

Decisiones ya tomadas: D66 (el interior es escena propia instanciada como hija del bus, con sus formas de colisión colgando del `RigidBody3D`), D67 (medidas exactas: caja útil 2,30 × 7,60 × 2,05 libres, pasillo central de 1,20, piso a 0,35 del fondo del chasis, puerta lateral de 0,90 con dos escalones de 0,25, puertas traseras de 1,40 en total), D68 (seis `Marker3D` en un nodo `Positions`, nombrados `driver`, `copilot`, `bench`, `shelf_left`, `shelf_right`, `stretcher`), D69 (anclajes de carga como `Marker3D` en un nodo `Restraints`, seis por muro), D70 (**el interior no cambia ni un dato de sensación**: si la inercia altera las cifras, se corrige repartiendo las formas, nunca tocando `data/tuning.json`).

RESTRICCIONES TÉCNICAS (plan §3):
- **Un cuerpo dinámico no admite colisión cóncava.** El interior hueco se construye con **varias formas convexas** hijas del mismo `RigidBody3D`: losa de piso, muros, techo, escalones. Nada de una malla única.
- **Añadir formas cambia el tensor de inercia**, y con ello la sensación que el dueño acaba de aprobar. Por eso hay que volver a medir las seis cifras de T1.1 y compararlas: altura de reposo 0,979 m, 0–60 en 6,93 s, tope 90,0 km/h, radio de giro 12,4 m, frenado desde 50 en 6,7 m, balanceo máximo 14°.
- **Los rayos de la suspensión excluyen solo el propio bus.** Configura capas y máscaras para que el interior no sea golpeado por los rayos de rueda ni empuje al chasis.
- Vigentes: física headless en tiempo real a 60 Hz, los tests esperan `physics_frame` y nunca frames de proceso; headless no entrega input; escenas ≤ 60 nodos, que es parte de por qué el interior va en escena aparte.
- **Aviso del revisor, sin diagnosticar:** al poner un `CharacterBody3D` de pie sobre el bus, el bus salió despedido. El montaje de la sonda se verificó correcto sobre suelo plano, así que el efecto es real pero no se conoce su causa. **No te afecta en T2.1**, que no tiene personajes, pero será el primer paso obligatorio de T2.2. No asumas que se arreglará solo.

QUÉ HACER: los pasos 0 a 7 del plan §6. El paso 0 son cuatro commits separados: (a) los archivos del revisor; (b) la normalización que el editor hizo de la sección `[input]` de `project.godot`, que es la esperada desde T0.1; (c) **r1.1 de T1.1**, que el campo `bus_accel_0_60_kmh_s` valga 5,0 y el bus tarde 5,0 s ± 0,2, calibrando la tracción, o bien renombrar el campo a lo que de verdad representa y anotar el 0–60 real: elige una salida y justifícala, pero no dejes un campo que dice una cosa y hace otra; (d) **r1.2 de T1.1**, dejar F5 conduciendo y añadir una forma de lanzar la demo automática sin consola, registrándolo como decisión.

CRITERIOS DE ACEPTACIÓN (plan §7–§8): el interior existe con las medidas de D67 y **el ancho libre del pasillo y la altura libre se miden sobre las formas, no se declaran**; las seis posiciones y los anclajes existen con los nombres exactos; ninguna malla cóncava en el cuerpo dinámico; los rayos de suspensión no ven el interior; las seis cifras de sensación siguen dentro del 10 % y **ningún dato de tuning se tocó para lograrlo**; r1.1 y r1.2 cerrados; `run_demo` y el bloque de red siguen pasando; tres arneses seguidos en código 0; sonda de accesos inseguros en verde con su diff dentro de la 02; captura con ventana del interior desde dentro y desde fuera; `git status` limpio; nada fuera del alcance del plan §5.

INSTRUCCIONES:
0. Los cuatro commits del paso 0, en ese orden.
1. Responde PRIMERO con un plan corto: archivos, cómo repartes las formas convexas, qué haces con r1.1 y por qué. No toques nada aún.
2. Tras la aprobación del dueño, implementa los pasos 1 a 7.
3. Si al añadir el interior alguna cifra de sensación se mueve más del 10 %, arréglalo repartiendo las formas y vuelve a medir; **no toques `data/tuning.json` para compensar**.
4. Si algo te obliga a salirte del alcance, detente y repórtalo antes de escribirlo. En particular: si te ves tentado de añadir un personaje para probar el interior, detente, eso es T2.2.
5. Reporte final: hashes de los commits, los tres códigos de salida del arnés, la tabla de las seis cifras antes y después, las medidas reales del pasillo y la altura libre, qué hiciste con r1.1 y por qué, y lo que quede abierto. Sin resúmenes de "todo verde": el revisor reproduce y mide.

# Prompt para el agente ejecutor — M0-T0.3 Ronda 1

Copiar desde `CONTEXTO:` hasta el final y pegarlo tal cual. Las decisiones D48–D52 del plan quedan aprobadas por el dueño al entregar este prompt.

---

CONTEXTO: Proyecto "Ruta Frágil" (Godot 4.7.2, GDScript tipado). Obedece R1–R14: están textuales en `AGENTS.md` §0.3; también §0.4 (protocolo) y §0.8 (autonomía). Repo local: `C:\Users\Leo\Documents\RutaFragil`, `main` = `1f021cb` (M0-T0.1 y M0-T0.2 cerradas y mergeadas: arnés `tools/run_tests.*` con 61 tests en verde, `GameConfig` autoload con `tuning: TuningTable` y señal `reloaded`). Crea la rama `m0/t0.3-playground` desde `main`; nunca trabajes en `main`. Hay dos archivos del revisor sin commitear en el árbol (`docs/planes/M0-T0.3_plan.md`, `docs/planes/M0-T0.3_ronda1_prompt.md`): cométealos tal cual primero (paso 0).

TAREA: **M0-T0.3 — Escena `Playground` (plano, rampas, baches, agua) + modo demo automático (§21).** ✅ *Acepta:* existe, carga, el bus placeholder recorre el circuito solo. El pliego completo es `docs/planes/M0-T0.3_plan.md`: léelo entero antes de planificar. Decisiones ya tomadas: D48 (bus placeholder "disco con motor": RigidBody3D sin suspensión, `set_drive(throttle, steer, brake)`, masa y dos campos nuevos de tuning desde `GameConfig`, el resto `@export` greybox), D49 (`Circuit` de `Marker3D` + `DemoDriver` + `DemoSteering` estática; `steer > 0` = izquierda), D50 (greybox con primitivas, sin CSG; el circuito atraviesa rampa, baches y agua; ≤ 60 nodos), D51 (`demo_mode` en la raíz + herramienta `src/tooling/run_demo.gd` con `--fixed-fps 60`), D52 (`run/main_scene` = Playground). No es T1.1: sin suspensión, sin ruedas, sin input de jugador, sin feel.

ARCHIVOS RELEVANTES (lee completos, en este orden): `docs/planes/M0-T0.3_plan.md` · `AGENTS.md` · `src/autoloads/game_config.gd` · `src/core/data/tuning_table.gd` · `data/tuning.json` · `docs/datos/esquema_v1.md` · `README.md` (sección "Datos y mods", para regenerar el `.tres`) · `tests/autoloads/game_config_autoload_test.gd` (idioma de tests vigente) · `addons/gdUnit4/src/GdUnitSceneRunner.gd` (API de `scene_runner`; solo lectura) · maestro §4.5, §9.1, §18, §20, §21, Parte 3 (T0.3, T0.4, T1.1).

HECHOS QUE NO DEBES REDESCUBRIR (plan §3, medidos por el revisor): la física corre headless con Jolt en tiempo real a 60 Hz (600 ticks = 10 s de reloj) mientras el bucle de proceso va a ≈144 fps: **nunca cuentes frames de proceso ni uses `simulate_frames` para física; espera `physics_frame` o señales con timeout explícito.** `--fixed-fps 60` da 1 tick por frame y corre ≈150× más rápido (úsalo en `run_demo`). `Engine.time_scale` solo no acelera; `runner.set_time_factor(k)` sí (pone `time_scale` y `physics_ticks_per_second × k`). Los autoloads existen en `-s` pero **después** de `SceneTree._init()`: instancia el Playground en `_initialize()` o diferido. Un `.tscn` escrito a mano sin `uid` carga bien y ni `--import` ni el editor lo reescriben.

REGLAS DE TIPADO (vigentes): sin `:=`, todo con tipo, `-> tipo` en todo `func`, `for x: T in ...`, fronteras `Variant` con `is` y variable tipada, `const X: GDScript = preload(...)` / `const S: PackedScene = preload(...)`, sin `@warning_ignore`. Herramientas `-s` con `quit()` en todas las rutas y `timeout`.

QUÉ HACER: los pasos 0–11 del plan §6, en orden: (1) dos campos `bus_max_speed_kmh` = 90.0 y `bus_accel_0_60_kmh_s` = 5.0 en `TuningTable` + `data/tuning.{json,tres}` + esquema, con el `.tres` regenerado por las herramientas de espejo; (2) `PlaceholderBus` + escena + tests; (3) `DemoSteering` + ≥ 8 tests; (4) `Circuit`, `DemoDriver` + tests; (5) `WaterZone` + test; (6) `ChaseCamera` + test; (7) `scenes/playground.tscn` + `Playground` + test de escena (≤ 15 s de reloj); (8) `src/tooling/run_demo.gd` + comandos en README y `AGENTS.md` "Comandos verificados"; (9) `run/main_scene`, editor con ventana e idempotencia; (10) `DECISIONS.md` D48–D52, `BACKLOG.md` (seis entradas del paso 10), README "Playground y demo"; (11) sonda `unsafe_*=2` con el `git diff -- project.godot` dentro del volcado.

CRITERIOS DE ACEPTACIÓN (plan §8–§9): ambos arneses en verde con ≥ 81 tests y ≤ 60 s de reloj; `run_demo` termina con `result=lap_completed` y código 0 tanto con `--headless --fixed-fps 60` (dos corridas) como en tiempo real; el circuito atraviesa rampa, baches y agua y `bus_entered` se emite; `rolled_over` no se emite en la vuelta; `playground.tscn` carga headless sin `ERROR`; escenas ≤ 60 nodos y scripts ≤ 400 líneas (evidencia 09); `git diff main..HEAD -- project.godot` muestra solo `run/main_scene`; sonda `unsafe_*=2` en verde; evidencias 01–09 completas con 07 en dos partes y la captura 05 como puntero LFS; `git status` limpio; nada fuera del alcance del plan §5.

INSTRUCCIONES:
0. Commit de los dos archivos del revisor tal cual: `docs: plan and round-1 prompt (M0-T0.3)`.
1. Responde PRIMERO con un plan corto (archivos, orden, parámetros iniciales del bus placeholder que vas a probar, riesgos). No toques nada aún.
2. Tras la aprobación del dueño, implementa en `m0/t0.3-playground` iterando código → `run_tests` → código hasta verde. Para afinar el bus usa `run_demo` con `--fixed-fps 60` como criterio (vuelta completa sin `rolled_over` ni `stuck`); máximo tres ajustes de parámetros greybox antes de detenerte y reportar; nunca quites un elemento del ✅.
3. Corre ambos arneses y la sonda `unsafe_*=2`; adjunta salidas completas.
4. Entrega las evidencias 01–09 del plan §8 con cabeceras exactas (comando, HEAD, qué demuestra). La 07 en dos partes: pre-commit dentro del commit de código y post-commit en un commit de docs aparte que liste el commit de código.
5. Tres intentos fallidos en cualquier punto = detenerse y reportar con la salida del último intento.
6. Reporte final: hashes de los commits, criterios cumplidos con la evidencia que lo prueba, conteo de tests y duración del arnés, parámetros finales del bus placeholder, lo que quedó abierto y por qué. Sin resúmenes de "todo verde": el revisor reproduce.

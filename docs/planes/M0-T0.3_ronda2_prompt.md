# Prompt para el agente ejecutor — M0-T0.3 Ronda 2 (exprés)

Copiar desde `CONTEXTO:` hasta el final y pegarlo tal cual. Un commit de código/docs y uno de evidencia 07 post-commit.

---

CONTEXTO: Proyecto "Ruta Frágil" (Godot 4.7.2, GDScript tipado). Obedece R1–R14: están textuales en `AGENTS.md` §0.3. Repo local: `C:\Users\Leo\Documents\RutaFragil`, rama `m0/t0.3-playground` (HEAD `f3b5802`; `main` = `1f021cb`, no se toca). Tu ronda 1 está bien construida: alcance limpio, 88 tests, sonda `unsafe_*=2` en verde con su `git diff` dentro del volcado, evidencia 07 en dos partes, tests no vacuos, `run_demo` con `quit()` en todas las rutas. El revisor reprodujo todo en verde. Hay dos archivos del revisor sin commitear (`docs/revisiones/M0-T0.3_revision_01.md`, `docs/planes/M0-T0.3_ronda2_prompt.md`): cométealos tal cual primero (paso 0).

LO QUE MIDIÓ EL REVISOR (sondas sobre la escena real, no opiniones):
- El impulso de escalón dispara **una vez por vuelta** y **cero veces dentro del campo de baches**: ocurre en (36,1 · 1,13 · −14,2), en la aproximación, fuera del campo.
- Con `climb_hop_up_speed` y `climb_hop_fwd_speed` puestos a cero en la instancia, **la vuelta nunca se completa**: el bus se detiene en (36,1 · 1,09 · −14,2) a los 24,9 s y sigue exactamente ahí a los 120 s. `stuck` se emite correctamente a los 31,9 s.
- Durante ese bloqueo el cuerpo está despierto (`sleeping_ticks=0`), `grounded=true` con 6 contactos, `thr=0.92`, `brake=0.00`, velocidad lineal y angular exactamente cero, `up=1.000`. Su morro queda contra la esquina del primer bache: el bus va a x ≈ 32–36 cuando la línea WP9→WP10 es x = 30, y la fila de baches termina en x = 36.
- Cruzando el campo de baches de frente el bus **no necesita ningún impulso**, pero tampoco se sacude: altura constante 1,25 m (suelo + los 0,15 m del bache) e inclinación máxima 3,3°, porque una caja de 8 m siempre apoya sobre dos o tres crestas separadas 3 m.

TAREA: **M0-T0.3 — Ronda 2 (exprés).** Cerrar r1.1, r1.2, r1.3 y r1.5 de `docs/revisiones/M0-T0.3_revision_01.md`. Sin cambios de alcance: nada de suspensión ni de input (eso es T1.1).

ARCHIVOS RELEVANTES: `docs/revisiones/M0-T0.3_revision_01.md` · `src/vehicle/placeholder_bus.gd` · `src/vehicle/demo_driver.gd` · `scenes/playground.tscn` · `src/tooling/run_demo.gd` · `BACKLOG.md` · `docs/evidencia/M0-T0.3/` (02, 03, 04, 05, 07).

QUÉ HACER:

A. **r1.1 — la vuelta debe completarse sin rescate.** Objetivo: `run_demo --headless --fixed-fps 60` termina en `lap_completed` con **cero impulsos de escalón**, y un atasco provocado a propósito sigue dando `result=stuck` con código 1.
   1. Arregla la causa, que es de trazado y geometría, no de la ley de control: añade un waypoint de aproximación que alinee al bus con la fila de baches antes de entrar (o estrecha la fila para que su borde no quede en la trayectoria, o bisela su cara de ataque). Ajusta el `reach_radius_m` o los waypoints del tramo WP9→WP10 si hace falta para que el bus no se desvíe 6 m de la línea.
   2. **Borra `_update_climb_hop` y sus tres `@export`** (`climb_hop_up_speed`, `climb_hop_fwd_speed`, `climb_wedge_seconds`) junto con `_wedge_time_s` y `_has_moved` si quedan sin uso. Si tras el arreglo de trazado sigues necesitando una asistencia, entonces **detente y repórtalo antes de escribirla**; si el dueño la aprueba, tendrá que estar condicionada a un contacto realmente bloqueante (normal casi horizontal con velocidad ≈ 0 pese a fuerza aplicada) y emitir una señal propia que `run_demo` cuente e imprima en su línea final.
   3. Añade un test que fije el contrato: con el bus colocado a propósito contra un obstáculo insuperable, `DemoDriver` emite `stuck` (ya existe `test_stuck_emitted_when_bus_cannot_move` con `freeze = true`; añade uno con un obstáculo real, sin `freeze`, para que el caso que el impulso ocultaba quede cubierto).

B. **r1.2 — que los baches sacudan.** En `scenes/playground.tscn`, rehaz el campo de baches para que un chasis de 8 m caiga entre ellos: separación mayor que el largo del bus (> 8 m) o alturas alternadas, o baches que no crucen todo el ancho. Criterio medible: durante la travesía la altura del bus deja de ser constante y la inclinación mínima (`min_upright` de `run_demo`) baja de forma apreciable respecto al 0,96 actual **sin** llegar a `rolled_over`. Reporta los números antes y después.

C. **r1.3 — corregir el registro.** La entrada de `BACKLOG.md` sobre el impulso y cualquier frase equivalente en la evidencia deben decir lo medido: el impulso no ayudaba a subir los baches; disparaba una vez por vuelta en la esquina de aproximación y ocultaba un atasco real. Si el impulso desaparece (A.2), la entrada pasa a "cerrado en la ronda 2" con esa explicación.

D. **r1.5 — cabeceras.** En 02, 03, 04 y 05, "HEAD: 5c6d1d2" → "árbol de trabajo sobre 5c6d1d2".

E. **Recapturar** 02 (con la sección de la sonda `unsafe_*=2` y su `git diff` dentro, como en la ronda 1), 03 (dos corridas), 04 (tiempo real) y 07 en dos partes. La 05 solo si la escena cambió lo que se ve.

CRITERIOS DE ACEPTACIÓN: ambos arneses en verde con ≥ 89 tests y ≤ 60 s de reloj; `run_demo --fixed-fps 60` con `result=lap_completed`, código 0 y **sin impulsos de rescate**; `run_demo ++ seconds=1` sigue dando `timeout` con código 1; un atasco real sigue produciendo `stuck`; `min_upright` de la vuelta mejorado respecto a 0,96 por los baches nuevos, sin `rolled_over`; sonda `unsafe_*=2` en verde; `git diff main..HEAD -- project.godot` solo con `run/main_scene`; escenas ≤ 60 nodos y scripts ≤ 400 líneas; `git status` limpio; nada fuera de los archivos listados.

INSTRUCCIONES:
0. Commit de los dos archivos del revisor tal cual: `docs: review 01 and round-2 prompt (M0-T0.3)`.
1. Responde PRIMERO con un plan de cuatro líneas: qué cambias del trazado, qué geometría de baches propones y qué números esperas. No toques nada aún.
2. Tras la aprobación del dueño, implementa A → B → C → D → E iterando código → `run_tests` → código.
3. Corre ambos arneses, la sonda y las cuatro variantes de `run_demo`; adjunta salidas completas.
4. Reporte final: hashes de los commits, ítems cerrados (r1.1–r1.5), conteo de tests, `min_upright` antes y después, cuántos impulsos de rescate quedan (debe ser cero), lo que quedó abierto. Sin resúmenes de "todo verde": el revisor reproduce y mide.

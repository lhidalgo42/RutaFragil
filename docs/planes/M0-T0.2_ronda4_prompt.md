# Prompt para el agente ejecutor — M0-T0.2 Ronda 4 (exprés, cierre)

Copiar desde `CONTEXTO:` hasta el final y pegarlo tal cual. No hay decisiones pendientes del dueño. Un commit de código/docs y uno de evidencia post-commit.

---

CONTEXTO: Proyecto "Ruta Frágil" (Godot 4.7.2, GDScript tipado). Obedece R1–R14: están textuales en `AGENTS.md` §0.3. Repo local: `C:\Users\Leo\Documents\RutaFragil`, rama `m0/t0.2-game-config` (HEAD `17a788a`; `main` = `e42f80b`, no se toca). La revisión 03 cerró los 13 ítems de la ronda 3 y reprodujo todo en verde (60/60, sin datos → 1, mods, hashes idénticos, `WARNING: REJECTED` del autoload a nivel de proceso). Falta un pulido: dos tests nuevos usan un acceso inseguro que rompe la propiedad "cero `unsafe_*`" (r3.1) y hay tres cabeceras de evidencia imprecisas. Hay dos archivos del revisor sin commitear (`docs/revisiones/M0-T0.2_revision_03.md`, `docs/planes/M0-T0.2_ronda4_prompt.md`): cométealos tal cual primero (paso 0).

TAREA: **M0-T0.2 — Ronda 4 (exprés).** Cerrar r3.1–r3.6 (y r3.7 si cabe) de `docs/revisiones/M0-T0.2_revision_03.md`. Sin cambios de producto.

ARCHIVOS RELEVANTES: `docs/revisiones/M0-T0.2_revision_03.md` · `tests/autoloads/game_config_autoload_test.gd` · `tests/core/data/json_file_test.gd` · `tests/core/data/game_data_loader_test.gd` · `DECISIONS.md` · `docs/datos/esquema_v1.md` · `docs/evidencia/M0-T0.2/` (02, 03, 04, 05, 07, 08, 09).

QUÉ HACER:

A. Tests
   1. **r3.1:** en `game_config_autoload_test.gd`, `const AUTOLOAD_SCRIPT: GDScript = preload("res://src/autoloads/game_config.gd")` y `auto_free(AUTOLOAD_SCRIPT.new())` en los dos tests de MR3. Verificar con la sonda: poner temporalmente `unsafe_property_access`, `unsafe_method_access`, `unsafe_cast`, `unsafe_call_argument` = 2 en `project.godot`, correr `powershell -ExecutionPolicy Bypass -File tools/run_tests.ps1` → debe dar 60/60 y código 0 (hoy da 105); revertir `project.godot` con `git checkout -- project.godot`. Dejar la salida de la sonda como sección en la 02.
   2. **r3.4:** en `json_file_test.gd::test_list_json_files_is_sorted_and_filtered`, añadir `B.json` y `a.json` (además de los actuales) y afirmar el orden por bytes `["B.json", "a.json", …]`. Renombrar `test_two_mods_apply_in_alphabetical_order` → `test_two_mods_apply_in_byte_order` y corregir los comentarios "alphabetical" de `game_data_loader_test.gd:4,95`.
   3. **r3.6:** docstring de `game_config_autoload_test.gd` actualizado: describe también los dos tests con instancia propia y directorios inyectados.
   4. **r3.7 (opcional, recomendado):** en MR3-a afirmar `last_report.is_ok()` verdadero y en MR3-b `warnings` vacío, leyendo `node.get("last_report")` con `is DataLoadReport`; cambiar `assert_bool(errors[0].begins_with(...))` por `assert_str(...).starts_with(...)` en `game_data_loader_test.gd:218`; test de 8 líneas para "mod con solo secciones desconocidas → aviso y en `applied_files`".

B. Documentos
   1. **r3.5:** `DECISIONS.md` D46: añadir "`reload(base_dir: String = "res://data", mods_dir: String = "user://mods")`; los argumentos existen para pruebas, el juego usa los defaults (ronda 3)"; cabecera de la tabla: "(D44/D45/D47 precisadas en la ronda 2; D46 en la ronda 3)". `esquema_v1.md` §7: misma firma que §1.

C. Evidencia
   1. **r3.2:** cabecera de 02: `test_reload_push_error_on_empty_dirs` compara el **summary completo** (la API de gdUnit4 solo admite cadena exacta o `any()`).
   2. **r3.3:** cabeceras de 03/04/05: "HEAD 0645a87; `data/` sin cambios desde 6656a8c".
   3. r3.7: nota en 09 (o en 08) de que los números de línea de sus backtraces son de `6656a8c` (`:35/:24`; en HEAD `:37/:26`).
   4. Recapturar 02 con el arnés final (≥ 60 tests) incluyendo la sección de la sonda `unsafe_*=2` (código 0).
   5. 07 en dos partes como en la ronda 3: pre-commit dentro del commit de código; post-commit en un commit de docs aparte que liste el commit de código.

CRITERIOS DE ACEPTACIÓN: ambos arneses en verde; **sonda `unsafe_*=2` en verde** y capturada en 02; `git diff main..HEAD -- project.godot` sin cambios; `B.json` < `a.json` fijado por test; D46 y esquema §7 con la firma nueva; cabeceras de 02/03/04/05 corregidas; 07 post-commit listando el commit de código; `git status` limpio; nada fuera de los archivos listados.

INSTRUCCIONES:
0. Commit de los dos archivos del revisor tal cual: `docs: review 03 and round-4 prompt (M0-T0.2)`.
1. Responde PRIMERO con un plan de tres líneas. No toques nada aún.
2. Tras la aprobación del dueño, implementa A → B → C en `m0/t0.2-game-config`.
3. Corre ambos arneses y la sonda; adjunta salidas completas.
4. Reporte final: hashes de los dos commits, ítems cerrados (r3.1–r3.7), conteo de tests, salida de la sonda, lo que quedó abierto. Sin resúmenes de "todo verde": el revisor reproduce.

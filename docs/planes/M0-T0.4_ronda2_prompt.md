# Prompt para el agente ejecutor — M0-T0.4 Ronda 2

Copiar desde `CONTEXTO:` hasta el final y pegarlo tal cual. Un commit de código/docs y uno de evidencia 07 post-commit.

---

CONTEXTO: Proyecto "Ruta Frágil" (Godot 4.7.2, GDScript tipado). Obedece R1–R14: están textuales en `AGENTS.md` §0.3. Repo local: `C:\Users\Leo\Documents\RutaFragil`, rama `m0/t0.4-net-harness` (HEAD `77fb441`; `main` = `5234c17`, no se toca). Tu ronda 1 es buena en lo difícil: el guion de red pasa, el fallo provocado falla por la razón correcta con 55,987 m de desviación contra medio metro de tolerancia, no quedan procesos huérfanos, `run_demo` no cambió y los seis bugs de red que documentaste son reales y están bien diagnosticados. Hay dos archivos del revisor sin commitear (`docs/revisiones/M0-T0.4_revision_01.md`, `docs/planes/M0-T0.4_ronda2_prompt.md`): cométealos tal cual primero (paso 0).

TAREA: **M0-T0.4 — Ronda 2.** Cerrar r1.1 a r1.4 de `docs/revisiones/M0-T0.4_revision_01.md`. Sin cambios de diseño ni de alcance.

LO QUE MIDIÓ EL REVISOR (no son opiniones):
- `tools/run_tests.ps1`, `tools/run_tests.ps1 -SkipNet` y `tools/run_tests.sh` dan **código 100** en su máquina, tres corridas seguidas: fallan `test_host_game_makes_us_host_and_emits_hosted` y `test_leave_returns_to_idle_and_frees_the_port`.
- La causa exacta: `netsh int ipv4 show excludedportrange protocol=udp` muestra el rango **49152-49251 excluido**, y tus puertos de prueba son 49211 y 49212. Sonda directa: `create_server(49211)` y `create_server(49212)` devuelven error 20 "Can't create"; `create_server(47950)` y `create_server(47810)` devuelven OK. Esas reservas de Hyper-V/WinNAT **se vuelven a sortear al reiniciar Windows**, por eso tu evidencia 02 es auténtica (a las 16:19 UTC pasaban) y aun así el arnés falla ahora.
- Ruta larga: `++ waypoints=17` da código 1; `++ waypoints=16` con el resto por defecto da código 1; `++ waypoints=16 seconds=60` pasa con 0,000 m. El circuito tiene índices 0 a 16, así que 17 no se cumple nunca, y el tiempo máximo del host sale del parámetro de segundos, que por defecto es menor que los 36,8 s de la vuelta.

QUÉ HACER:

A. **r1.1 (bloqueante) — puertos de prueba robustos.** En `tests/net/network_backend_test.gd`, deja de fijar puertos del rango dinámico. Busca en tiempo de ejecución un puerto que acepte `create_server` entre varios candidatos fuera del rango dinámico de Windows (la vecindad de 47810 está probada), ciérralo y úsalo en el test. Si ningún candidato sirve, el test debe fallar con un mensaje que diga que no hay puerto libre, no con un "esperaba 1 y hubo 0". Aplica el mismo criterio al puerto del test de conexión fallida.

B. **r1.2 (bloqueante) — que la ruta larga funcione.** Tres cosas: (1) el tiempo máximo que el host se da para alcanzar el waypoint debe derivarse de la ruta pedida, no del parámetro de segundos; (2) el cliente debe sacar su foto **cuando el host se lo indique** por RPC, después de frenar y asentar, en vez de esperar un tiempo fijo de reloj, y solo entonces escribir su JSON; (3) arregla el desfase de uno, o documenta el parámetro como "índice en el que parar" y corrige D56 y el README para que digan 16. Criterio: `++ waypoints=16` **sin más parámetros** termina en paso, y la ruta corta por defecto sigue costando lo mismo que ahora.

C. **r1.3 — resumen honesto cuando el host falla.** Si falta el JSON del host, toma el número de clientes conectados de los JSON de los clientes y omite la desviación en vez de calcularla contra un vector cero.

D. **r1.4 — corregir el registro.** La evidencia 08, punto 5, dice que el guion soporta la vuelta completa. No la soportaba. Sustituir por lo medido y por lo que quede tras el arreglo.

E. **Recapturar** las evidencias 02 (ambos arneses con y sin el bloque de red, más la sonda de accesos inseguros con su diff dentro), 03 (añadiendo una corrida de ruta larga que pase) y 08. La 07 en dos partes como siempre.

CRITERIOS DE ACEPTACIÓN: `tools/run_tests.ps1`, `tools/run_tests.ps1 -SkipNet` y `tools/run_tests.sh` en **código 0** con 96 tests o más; el guion de red pasa en ruta corta y en `++ waypoints=16` sin parámetros extra; el fallo provocado sigue terminando en código 1 por el assert de convergencia; sin procesos huérfanos tras una corrida fallida; `run_demo` con los mismos números; sonda de accesos inseguros en verde con su diff dentro de la 02; `git diff main..HEAD -- project.godot` solo con el autoload; `git status` limpio.

INSTRUCCIONES:
0. Commit de los dos archivos del revisor tal cual: `docs: review 01 and round-2 prompt (M0-T0.4)`.
1. Responde PRIMERO con un plan de cuatro líneas: cómo eliges el puerto, cómo coordinas la foto del cliente, qué haces con el desfase de uno. No toques nada aún.
2. Tras la aprobación del dueño, implementa A a E iterando código y arnés hasta verde.
3. Antes de dar nada por bueno, **corre el arnés completo tres veces seguidas** y adjunta los tres códigos de salida: el defecto de esta ronda fue justamente un fallo que no aparece en una sola corrida.
4. Si algo te obliga a salirte del alcance, detente y repórtalo antes de escribirlo.
5. Reporte final: hashes de los commits, ítems cerrados, los tres códigos de salida del arnés, cómo quedó la elección de puerto, los números de la corrida de ruta larga, y lo que quede abierto. Sin resúmenes de "todo verde": el revisor reproduce y mide.

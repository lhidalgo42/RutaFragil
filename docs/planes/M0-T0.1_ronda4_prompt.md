# Prompt para el agente ejecutor — M0-T0.1 Ronda 4 (cierre)

Copiar desde `CONTEXTO:` hasta el final. No requiere ninguna decisión del dueño: no hay borrados ni ADRs nuevos.

---

CONTEXTO: Proyecto "Ruta Frágil" (Godot 4.7.2, GDScript tipado). Obedece R1–R14: están textuales en `AGENTS.md` §0.3. Repo local: `C:\Users\Leo\Documents\RutaFragil`, rama `m0/t0.1-foundations` (HEAD `410b449`; `main` = `633d4ba`, no se toca). La revisión 03 declaró M0-T0.1 apto para el gate del dueño; esta ronda 4 cierra cinco menores y unas notas que no bloquean. Hay dos archivos sin commitear del revisor (`docs/revisiones/M0-T0.1_revision_03.md`, `docs/planes/M0-T0.1_ronda4_prompt.md`): cométealos tal cual primero (paso 0).

TAREA: **M0-T0.1 Fundaciones — Ronda 4 (cierre).** Cerrar m-3.1 a m-3.5 y las notas del arnés de `docs/revisiones/M0-T0.1_revision_03.md`. Nada más.

ARCHIVOS RELEVANTES: `docs/revisiones/M0-T0.1_revision_03.md` (secciones "Hallazgos nuevos" y "Notas") · `tools/run_tests.ps1` · `tools/run_tests.sh` · `README.md` · `CREDITS.md` · `BACKLOG.md` · `DECISIONS.md` · `docs/evidencia/M0-T0.1/` (02, 09, 11). No tocar `AGENTS.md` §0.3/§0.4/§0.8, `project.godot`, `tests/`, `addons/`.

QUÉ HACER:

A. Arnés:
   1. m-3.1: `-Encoding UTF8` en la lectura de `tools/godot_bin.local` (`run_tests.ps1` ~línea 136).
   2. Nota `[int]`: antes del cast, `if ($null -eq $process.ExitCode) { fail "Process '$exe' returned no exit code" }` y corregir el comentario: el `catch` cubre el proceso que no arranca; el `null`-check cubre la falta de código; el cast es solo garantía de tipo.
   3. Nota cabecera `.sh`: reformular el contrato de códigos: "se propaga tal como lo entrega el shell; POSIX lo reduce a 0–255 (un `exit(444)` del runner llega como 188; bajo Git Bash los crashes de Windows aparecen como 128+señal). Solo el 1 está reservado al arnés". En el `.ps1` una línea: "Windows conserva el código de 32 bits".
   4. Nota pasada 3: imprimir la línea `FINDING:` cuando la tercera pasada de `--import` sale ≠ 0, igual que las pasadas 1–2, en ambos scripts.
   5. Mantener ≤400 líneas y snake_case.

B. Documentos:
   1. m-3.2: `README.md` — corregir el conteo de LFS ("9 punteros, 8 objetos únicos, todos PNG del addon gdUnit4") o sustituirlo por "ver `git lfs ls-files`".
   2. m-3.3: `CREDITS.md` — añadir la cláusula de D32: "`src/dotnet/GdUnit4CSharpApi.cs` es parte de la distribución oficial y queda inerte en build estándar".
   3. `DECISIONS.md` — convertir la nota `*.scn`/`*.res` en LFS en la decisión numerada **D41** (mantener la red de seguridad; R14 los prohíbe).
   4. `BACKLOG.md` — en "Correcciones al maestro para proponer al dueño" añadir: "§0.2, §20 y T0.1 dan por hecho un puente MCP; el proyecto decidió ninguno en M0 (D30/ADR-009)".
   5. **D42 (decisión del dueño en chat, 2026-09-09):** "Validación en Mac diferida. M0-T0.1 se acepta con `run_tests` verde solo en Windows. La Mac vuelve a ser obligatoria antes del gate M4 (red real Mac + Windows, D13). Residuo declarado: el arreglo de BOM POSIX de `run_tests.sh` (m-2.3) solo está probado bajo Git Bash; su prueba real en BSD sed queda para esa corrida." Registrar D42 en `DECISIONS.md`; en `09_no_verificado.txt` mover el ítem de la Mac a "diferido por D42 hasta antes de M4"; en `README.md` y `BACKLOG.md` (ítem "Cierre de M0-T0.1") reflejar que la corrida en la Mac ya no bloquea T0.1. Añadir a "Correcciones al maestro": "T0.1 'en ambas máquinas' → 'en Windows; Mac antes de M4' (D42)".

C. Evidencia (`docs/evidencia/M0-T0.1/`):
   1. m-3.4: sección bash en la 11a (guardia numérico `< 3` con un solo test) — debe fallar con código 1.
   2. m-3.5: ejercitar el abort por versión en la 11c con `GODOT_BIN` apuntando a un ejecutable ajeno (por ejemplo `C:\Windows\System32\cmd.exe`), ambos scripts; deben abortar con "Engine version must start with '4.7.2.stable'". Quitar el ítem 4 de la 09.
   3. Nota: ejercitar en la 11 la rama `catch` (`GODOT_BIN` apuntando a un archivo existente no ejecutable, por ejemplo `README.md`) en PowerShell; declarar en la 09 las ramas que no se pueden provocar de forma determinista (tercera pasada de import, caché inválida, `reports/` bloqueado).
   4. Nota: en la 11b, una línea que documente el percance de los escapes octales al escribir el archivo de prueba con `printf` (`\4` → octal), para que el siguiente agente no tropiece.
   5. Nota: cabecera en `02_import.txt` indicando que es la captura de la ronda 1, anterior al borrado de `test/`, y que las pasadas post-borrado están en 03/04/05/10.
   6. 09: actualizar a lo que quede realmente sin verificar (Mac, remoto, dock del editor, ramas no provocables).
   7. Recapturar 03 (ambas formas) con el arnés final.

CRITERIOS DE ACEPTACIÓN: ambos arneses en verde (03); 11a y 11c con ambas formas y los códigos esperados; 09 sin el ítem del abort por versión y con la Mac marcada como diferida por D42; README/CREDITS/DECISIONS/BACKLOG con los cinco cambios (incluida D42); `git status` limpio tras el último commit; nada fuera de la lista de archivos.

INSTRUCCIONES:
0. Commit de los dos archivos del revisor tal cual: `docs: review 03 and round-4 prompt (M0-T0.1)`.
1. Responde PRIMERO con un plan corto. No toques nada aún.
2. Tras la aprobación del dueño, implementa A → B → C en `m0/t0.1-foundations`.
3. Adjunta la salida completa de ambos arneses y de las nuevas ramas de la 11.
4. Reporte final: hash del commit, ítems cerrados por código (m-3.1…m-3.5, notas), lo que quedó abierto y por qué, ruta de cada evidencia.

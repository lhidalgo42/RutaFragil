# M5-T5.2a — Greybox de B0 Ciudad y ruta encadenada de biomas · Plan de ejecución y evidencia

**Versión:** 1.0 (2026-09-13) · **Proyecto:** Ruta Frágil (Godot 4.7.2, GDScript tipado) · **Ejecuta:** Claude (agente) · **Aprueba:** el dueño (§0.7)

**Jerarquía:** maestro v0.2 > briefing v0.2 > este plan > criterio del agente. Contradicciones se reportan, no se resuelven en silencio.

## 0. Cómo usar este documento

1. Leer `AGENTS.md`, la biblia de arte (`docs/biblia_arte_y_mundo.html`, "Los tres biomas del MVP"), el maestro §6.1–6.2, §20 y la Parte 3 (T5.2), y `docs/planes/M0-T0.3_plan.md` (D49–D51: circuito, conductor y herramienta de demo que esta tarea reutiliza).
2. La tarea está ejecutada en la rama `m5/t5.2a-biome-greybox-b0`; la evidencia vive en `docs/evidencia/M5-T5.2a/`.
3. El dueño valida con la sección 9 y decide el merge.

## 1. La tarea (instrucción del dueño, 2026-09-13)

No existe en la Parte 3. El dueño pidió, por instrucción explícita, **construir los biomas por donde pasará el bus y luego juntarlos para ver la interacción**, empezando por Ciudad. Se registra como **T5.2a**, pre-tarea de T5.2 ("B0 Ciudad grey-box con el guion de tutorial de §6.2 … la ruta mide 6–10 km").

**Contradicción reportada y resuelta por el dueño:** la petición original decía "mapa de bosque". Bosque está **eliminado** (D18, maestro §1.3). El dueño aceptó partir por B0 Ciudad; Bosque no se construye.

**Lo que T5.2a NO es:** ni el bus real (T1.1), ni la lógica de tutorial, NPCs, contratos o combustible (T5.2, T1.2, T1.3, M3). Es geometría recorrible por el bus placeholder, en primitivas planas, más la infraestructura para encadenar biomas.

## 2. Estado de partida (2026-09-13)

- `main` = `3c8eca8` (biblia de arte). 96 tests / 18 suites en verde en Windows con `4.7.2.stable.official.ed1daf0bf`.
- Existe el Playground (D50) con `Circuit`, `DemoDriver`, `DemoSteering`, `PlaceholderBus`, `ChaseCamera`, `WaterZone` y `src/tooling/run_demo.gd` (D51). `scenes/` solo tiene `playground.tscn`; `src/biomes/` solo `playground/`.
- Ramas remotas: `main`, `m2/t2.2-crew` (no se toca).

## 3. Hechos verificados que condicionan el diseño (R11)

Medidos con `run_demo` headless a `--fixed-fps 60` sobre la escena de la ruta (los seis intentos están en `docs/evidencia/M5-T5.2a/03_intentos_y_causas.txt`):

- **El `DemoDriver` solo frena cuando el siguiente waypoint está a más de 60° del morro** (`DemoSteering.BRAKE_TURN_ANGLE_RAD`); bajo eso toma la curva a velocidad y el bus placeholder **derrapa hacia afuera varios metros**. Corolario: todo giro entre tramos consecutivos del circuito se mantiene **≤ 54°**, y los giros en U se hacen en lágrima de 5–6 puntos.
- **Un bordillo de 0,15 m es una trampa de `stuck` para el bus placeholder** (caja con la base a 0,10 m del suelo y fricción 0,05): si una esquina lo pisa de frente, no sube ni resbala. Con calle de 18 m se trabó dos veces; con **24 m** y un waypoint de alineación antes del lomo, completa la vuelta.
- **El extremo de una pared paralela a la calle es un choque frontal** para un bus que entra en diagonal. Las paredes del túnel van **por fuera de los bordillos (±13 m)**: lo "bajo" del túnel es la altura (2,7 m de luz), no el ancho.
- **Escenas a mano** sin `uid` cargan bien; los `@export` tipados-Node necesitan `node_paths=[...]` (lección de T0.3, respetada en `route_mvp.tscn`).
- `RouteCircuit extends Circuit` es aceptado por `DemoDriver.circuit: Circuit` sin cambios en el conductor.

## 4. Decisiones de esta tarea (aprobadas por el dueño con el plan del chat, 2026-09-13)

| # | Decisión | Razón | Alternativa descartada |
|---|---|---|---|
| **D59** | **Bioma = `BiomeSegment`** (`src/biomes/biome_segment.gd`, `Node3D` raíz de una escena por bioma) con `biome_id`, `length_m`, marcadores `Entry`/`Exit` (el bus llega mirando a −Z) y un `Circuit` hijo con sus waypoints. **`RouteCircuit extends Circuit`** (`src/biomes/route_circuit.gd`) encadena los circuitos de todos los segmentos bajo `segments_root` en orden de árbol. **`RouteGreybox`** (`src/biomes/route_greybox.gd`) es la raíz de `scenes/route_mvp.tscn`, con la misma forma que `Playground` (`demo_mode`, `demo_driver`). **Escala de laboratorio:** ~300 m por segmento; la ruta de 6–10 km es de T5.2. **Sin `BiomeDefinition` todavía**: el bioma como contenido (R2, §19) entra con T5.2/T6/T7. | Una escena por bioma permite reordenarlos o cambiarlos por arte sin tocar a los demás; el `RouteCircuit` deja al `DemoDriver` intacto. La escala corta hace que la vuelta cueste 87 s de juego y quepa en un test. | Un solo `.tscn` con todo: supera los 60 nodos (R8) y mezcla biomas. `Path3D`: sin ventaja para 30 puntos (D49). |
| **D60** | **B0 Ciudad greybox** (`scenes/b0_city.tscn`, 39 nodos, 5 sub-escenas en `scenes/b0/`): calle recta de 24 m con bordillos de 0,15 m (`city_street`, con lomo de toro de 0,3 m y rampas a 8°, D50); **estación** con techo a 5 m, 4 postes, surtidor y tienda (`city_station`); **túnel bajo** de 2,7 m de luz con paredes a ±13 m (`city_tunnel`); **rama de combustible** a la izquierda con explanada, surtidor, 3 barriles y 2 bidones (`city_fuel_branch`); **casa de entrega** con timbre y receptor (`city_delivery`). Circuito de **30 waypoints** que visita estación, lomo, túnel, plaza de entrega, vuelve por el túnel y el lomo, entra a la rama de combustible y cierra por la plaza norte; giros ≤ 54°. Colisiones con `StaticBody3D` + primitivas, **no CSG**; explanadas y timbre son solo malla (sin escalón que trabe al bus). | Cada punto del guion de §6.2 que es *geometría* existe y el circuito pasa por todos; lo que es *lógica* (repostar, timbre, NPC) no se simula. | Curbs más bajos para "ayudar" al placeholder: falsea el tutorial; T1.1 (ruedas) debería superarlos. |
| **D61** | `src/tooling/run_demo.gd` acepta **`scene=<res://…>`** (por defecto el Playground) y la **`WaterZone` pasa a ser opcional**. Todo lo demás igual; comportamiento con el Playground idéntico. | Una sola herramienta para todas las escenas de demo; B0 no tiene agua. | Herramienta nueva por escena: duplica 150 líneas. |
| **D62** | `run/main_scene` **sigue siendo el Playground** (D52). La ruta se corre con `run_demo ++ scene=res://scenes/route_mvp.tscn` o abriéndola en el editor (F6). | El gate M1 sigue siendo el Playground. | Cambiar la escena principal: rompe D52 sin necesidad. |

## 5. Alcance (R6)

Archivos nuevos: `scenes/route_mvp.tscn`, `scenes/b0_city.tscn`, `scenes/b0/city_{street,station,tunnel,fuel_branch,delivery}.tscn`, `src/biomes/{biome_segment,route_circuit,route_greybox}.gd` (+ `.uid`), `tests/biomes/{biome_segment,route_circuit,b0_city_scene}_test.gd` (+ `.uid`), `docs/planes/M5-T5.2a_plan.md`, `docs/evidencia/M5-T5.2a/*`. Modificados: `src/tooling/run_demo.gd` (D61), `DECISIONS.md` (D59–D62), `BACKLOG.md`. **No se toca:** el bus, el conductor, el Playground, `project.godot`, `data/`, la rama `m2/t2.2-crew`.

## 6. Pasos ejecutados

1. Plan y contradicción (Bosque/D18) presentados al dueño en el chat; aprobación de B0 Ciudad.
2. Scripts D59 + escena de la ruta + calle con lomo; import limpio; 7 tests en verde.
3. Seis corridas de `run_demo` hasta `lap_completed` (sección 3 y evidencia 03): calle 18→24 m, lazos en lágrima, waypoint de alineación, paredes del túnel a ±13 m.
4. Estación, túnel, rama de combustible y entrega añadidos de a uno; la vuelta se cierra en 86,6 s con las 5 piezas (evidencia 01).
5. Suite completa del repo en la rama (evidencia 02) y capturas con ventana (evidencia 00).

## 7. Archivos (resumen)

Ver sección 5. Ninguna escena supera 60 nodos (máximo: `b0_city.tscn`, 39); ningún script supera 60 líneas.

## 8. Evidencia (`docs/evidencia/M5-T5.2a/`)

`00_capturas.txt` (dónde están los PNG y cómo regenerarlos) · `01_run_demo_vuelta_completa.txt` (30 waypoints, `lap_completed t=86.6s`, `max_speed_kmh=62.6`, `min_upright=0.81`) · `02_gdunit4_tests.txt` (suite completa del repo en la rama) · `03_intentos_y_causas.txt`.

## 9. Checklist de revisión (PASA / FALLA / NO PROBADO)

- [ ] `--import` ×2 sin errores y la suite completa en verde en Windows (evidencia 02).
- [ ] `run_demo ++ scene=res://scenes/route_mvp.tscn` termina en `lap_completed` con código 0 (evidencia 01).
- [ ] `run_demo` sin argumentos sigue completando la vuelta del Playground (D61 no rompe D51).
- [ ] Abrir `scenes/route_mvp.tscn` en el editor y F6: el bus recorre estación → lomo → túnel → entrega → túnel → lomo → combustible → plaza norte. **NO PROBADO por el agente con ojos humanos**: legibilidad y sensación quedan al gate (§0.8).
- [ ] Sin `.scn`/`.res` binarios; sin PNG nuevos en LFS.

## 10. Riesgos

- El bus placeholder derrapa: la geometría de B0 está calibrada para él. Con el bus de T1.1 (ruedas, suspensión) algunas holguras podrían sobrar y los bordillos dejar de ser trampa; **re-correr la evidencia 01 al cerrar T1.1**.
- El túnel bajo hoy no "hace" nada: los flotantes son M3.
- La escala 1:20 respecto de los 6–10 km de T5.2 es deliberada (D59); no extrapolar tiempos.

## 11. Fuentes primarias

Maestro §1.3, §6.1–6.2, §19, §20, Parte 3 (T5.2) · `docs/biblia_arte_y_mundo.html` · `AGENTS.md` (R1–R14, §0.4, §0.8) · `docs/planes/M0-T0.3_plan.md` (D48–D52) · `DECISIONS.md` D18, D50–D52.

## 12. Ronda 2 — B0 Ciudad v2 «real por fuera» (2026-09-13, aprobada por el dueño: «solo lo que se ve desde el bus»)

Fuente: `docs/referencias/B0_ciudad_referencias.html` (medidas chilenas con fuente oficial + reglas visuales de PEAK). Alcance aprobado: avenida, casas, lomos, paso bajo nivel y estación. **Fuera de esta ronda** (backlog): rotondas, plazas, mobiliario urbano, calle local hacia el sitio de combustible, casa de entrega v2.

| Elemento | v1 | v2 (D63) |
|---|---|---|
| Calle | calzada única de 24 m | avenida troncal: dos calzadas de 10,5 m, bandejón de 3 m con árboles **solo malla**, veredas de 3 m (malla), soleras de 15 cm en ±12 m; sentido sur por x = +5, regreso por x = −5 |
| Casas | solo la de la entrega | `HouseRow` (`src/biomes/house_row.gd`): dos filas de 10 casas de 8 m de frente, 1–2 pisos (3,5 / 7 m), tres colores, antejardín de 2,5 m con reja; una colisión por fila; MultiMesh, 3 nodos por fila |
| Lomo de toro | 1 de 30 cm (inclinaba el bus 36°) | 4 resaltos planos (2 por calzada, en serie a 30 m): 15 cm, meseta 5 m, rampas 1,8 m (4,8°), franjas amarillas; señales PG-8a + RR-1 a 35 m, solo malla |
| Paso bajo nivel | dintel a 2,7 m, 24 m de largo | tablero de 14 m con gálibo de **3,6 m** (cabe el bus real de ~3,2 m, no un flotante en el techo), muros a ±13 m, franjas de aviso, señales RR-6 «altura máxima» + PF-5 a 35 m |
| Estación | techo 16 × 18 a 5 m, 1 surtidor | marquesina 18 × 21 m a 6,4 m con 6 postes, 2 islas a **11 m** (real 9,5: la caja las roza a 9,5), tótem de precios, tienda de 12 × 8, punto de aire; salida ensanchada (bordillo derecho hasta z = 34) |

**Hechos medidos en esta ronda (R11):** (1) con las islas a 9,5 m el bus volcó una vez: la maniobra hacia la calzada empezaba entre las islas; (2) con la pista sur a 7 m del bordillo, el derrape de salida de la estación (~57 km/h) alcanzaba el extremo del bordillo en z = 44 → `stuck` dos veces; la salida ancha hasta z = 34 y una bajada en dos tramos ≤ 34° lo resuelven; (3) los resaltos planos dejan `min_upright` en 0,93 frente al 0,81 del lomo de 30 cm; (4) las copas de los árboles del bandejón deben quedar sobre 5,2 m o la `ChaseCamera` (a 5 m) queda dentro de ellas.

**Evidencia:** `docs/evidencia/M5-T5.2a/05_v2_run_demo.txt` (30 waypoints, `lap_completed`), `06_v2_gdunit4_tests.txt` (suite completa), `07_v2_intentos.txt`. Capturas en `capturas/biomas/b0v2_*.png` fuera del repo (LFS).

# Prompt para el agente ejecutor — M0-T0.1 Ronda 3

Copiar desde `CONTEXTO:` hasta el final. **El campo R12 ya está completado: SÍ**, autorizado por el dueño en chat el 2026-09-08 ("su autorizo", en respuesta a la pregunta "¿SÍ o NO al borrado de `addons/gdUnit4/test/`?"). No hace falta editar nada antes de enviar.

---

CONTEXTO: Proyecto "Ruta Frágil" (Godot 4.7.2, GDScript tipado). Obedece R1–R14: están textuales en `AGENTS.md` §0.3 (léelas antes de empezar; jerarquía maestro > briefing > plan > tu criterio). Repo local: `C:\Users\Leo\Documents\RutaFragil`, rama `m0/t0.1-foundations` (HEAD `41bea42`; `main` = `633d4ba`, no se toca). La ronda 2 cerró todo lo que dependía de ti. Esta ronda 3 es corta: el borrado pendiente de R12 más ocho menores nuevos de la revisión 02. Hay archivos sin commitear del revisor en el árbol de trabajo (`docs/revisiones/M0-T0.1_revision_02.md`, `docs/planes/M0-T0.1_ronda3_prompt.md`): cométealos tal cual primero (paso 0).

TAREA: **M0-T0.1 Fundaciones — Ronda 3.** Cerrar M2/M3 según la decisión R12 y los hallazgos m-2.1 a m-2.8 de `docs/revisiones/M0-T0.1_revision_02.md`.

ARCHIVOS RELEVANTES (lee completos, en este orden): `docs/revisiones/M0-T0.1_revision_02.md` · `docs/planes/M0-T0.1_plan.md` (v1.1, pasos 4 y 6, secciones 7 y 8) · `tools/run_tests.ps1` · `tools/run_tests.sh` · `AGENTS.md` (solo puedes tocar "Comandos verificados" y "Rutas clave") · `DECISIONS.md` · `BACKLOG.md` · `CREDITS.md` · `docs/evidencia/M0-T0.1/`.

CONFIRMACIÓN R12 DEL DUEÑO PARA BORRAR `addons/gdUnit4/test/`: **SÍ** (dada por el dueño en chat el 2026-09-08, tras la revisión 02). Ejecuta el bloque B completo. Registra la autorización en `DECISIONS.md` (fila D32: "borrado de `test/` autorizado por el dueño el 2026-09-08 por R12").

QUÉ HACER:

A. Arnés (`tools/run_tests.ps1` y `tools/run_tests.sh`, misma lógica):
   1. m-2.1: `-Encoding UTF8` en las tres lecturas `Get-Content` de `run_tests.ps1` (líneas ~72, ~73 y ~219). Godot escribe UTF-8; PowerShell 5.1 sin `-Encoding` decodifica Windows-1252 y corrompe los acentos en toda la evidencia.
   2. m-2.2: tras las dos pasadas de `--import`, exigir que la caché exista, **no esté vacía** y contenga las cadenas `"class": &"GdUnitTestCIRunner"` y `"class": &"GdUnitTestSuite"`; si falla, una tercera pasada de `--import`, re-verificar, y entonces fallar con un mensaje que apunte a la caché. No usar conteo de líneas. Actualizar el comentario "source of truth".
   3. m-2.3: en `run_tests.sh`, quitar el BOM sin `sed 's/^\xEF\xBB\xBF//'` (extensión GNU; BSD sed en macOS no la entiende). Usar `bom="$(printf '\357\273\277')"; line="${line#"$bom"}"` o equivalente POSIX.
   4. Notas: `catch` en `invoke_process` (o `[int]` en el código de salida) para que un proceso que no arranca no salga 0 tras imprimir FAILURE; verificar que `reports/` no existe tras borrarlo antes de recrearlo; comentar en el script que stdout y stderr se concatenan sin orden relativo; aclarar en el encabezado que los códigos no mapeados del runner se propagan crudos.
   5. Mantener ≤400 líneas y nombres snake_case (R8, R9).

B. Addon — **R12 = SÍ, se ejecuta** (plan v1.1 paso 4):
   1. Borrar `addons/gdUnit4/test/` completo (559 archivos).
   2. `--import` ×2, `run_tests` en ambas formas; verificar que `.godot/global_script_class_cache.cfg` no contiene ninguna ruta `res://addons/gdUnit4/test/` (hoy hay 148).
   3. `DECISIONS.md` D32 con el texto del plan v1.1: "Solo la carpeta `addons/gdUnit4/` sin su subcarpeta `test/` (auto-tests del addon; la distribución oficial la excluye por `export-ignore`); `src/dotnet/GdUnit4CSharpApi.cs` es parte de la distribución y queda inerte en build estándar". `CREDITS.md` igual. Retirar toda mención a la "excepción R14".
   4. Evidencia 08 actualizada: `ls addons/gdUnit4/` sin `test/`, conteo 0 de rutas `test/` en la caché, hash del tag sin cambios.
   5. `AGENTS.md` → "Rutas clave": los `CLAUDE.md`/`AGENTS.md` bajo `addons/` son de terceros y no aplican al proyecto (queda `addons/gdUnit4/src/asserts/CLAUDE.md`). Esta línea va **también si R12 = NO**.

C. Documentos:
   1. m-2.4: `AGENTS.md` "Comandos verificados" con la forma verificada: `powershell -ExecutionPolicy Bypass -File tools/run_tests.ps1` y `bash tools/run_tests.sh` (igual que el README).
   2. m-2.7: `DECISIONS.md`: cabecera "D30–D40"; D40 con nota "(añadida en la ronda 2, prompt A.4; no está en el plan v1.1)"; en ADR-000 separar lo citado del maestro de lo añadido por el proyecto (hash y test); ADR-009 marcado como "no proviene del maestro: decisión D30 del plan v1.1". D32 según el bloque B o, si R12 = NO, con la nota "test/ conservado por D41".
   3. m-2.8: `BACKLOG.md` T0.1–T0.4 con el texto completo del maestro Parte 3 (incluidos los criterios ✅), como ya están M1–M8.
   4. `.gitattributes`: añadir un comentario junto a `*.scn`/`*.res` indicando que R14 prohíbe esos formatos y que la línea LFS es una red de seguridad por si un archivo así entra por error (o quitar las dos líneas y anotarlo en `DECISIONS.md`). Registrar la elección.
   5. `BACKLOG.md`: mover m-2.2 (caché) a "Cerrados"; corregir la fila del transitorio de caché para decir "causa no determinada" en vez de atribuirla a PowerShell.

D. Evidencia (`docs/evidencia/M0-T0.1/`, volcados completos, cabeceras correctas):
   1. Recapturar las secciones PowerShell de 03, 04, 05 y 10 con la codificación corregida (los acentos deben leerse bien).
   2. m-2.5: sección bash en la 10 (corrida con `tests/` vacío bajo `bash tools/run_tests.sh`, debe fallar con el guardia). Restaurar `tests/` y evidenciar `git status` limpio después.
   3. m-2.6: nuevo `11_ramas_arnes.txt` que ejercite y volque: guardia numérico `< 3` (por ejemplo `-a` apuntando a una carpeta con un solo test temporal, o dejando un solo test), `tools/godot_bin.local` con BOM y comillas (ambos scripts), y abort por versión (por ejemplo `GODOT_BIN` apuntando a un ejecutable que no sea 4.7.2 si existe; si no existe, declararlo en la 09).
   4. 04: en el anexo del transitorio de caché, reemplazar la frase de causa por "causa no determinada" y añadir los volcados que faltan si aún existen (si no, decir que no se conservaron).
   5. 06: añadir una nota de que el diff original comparó contra una copia en `/tmp/` no conservada y que la idempotencia posterior está probada por `git status` limpio tras arranques con ventana (evidencia 07 y reproducción del revisor).
   6. 07: sección post-commit con listados completos (no conteos) y `git lfs ls-files`.
   7. 09: actualizar según lo que quede realmente sin verificar tras esta ronda.

CRITERIOS DE ACEPTACIÓN: checklist §8 del plan v1.1 con todo PASA salvo "Solo el dueño" (Mac y remoto, declarados en 09). `addons/gdUnit4/test/` no existe; 0 rutas `res://addons/gdUnit4/test/` en la caché; ningún `.scn` en `git ls-files`; acentos legibles en la evidencia PowerShell; evidencia 10 con sección bash; evidencia 11 presente; `git status` limpio tras el último commit.

INSTRUCCIONES:
0. `git status` mostrará sin seguimiento `docs/revisiones/M0-T0.1_revision_02.md` y `docs/planes/M0-T0.1_ronda3_prompt.md`. Cométealos tal cual, sin editarlos, con el mensaje `docs: review 02 and round-3 prompt (M0-T0.1)`.
1. Responde PRIMERO con un plan (archivos, enfoque, riesgos, y qué harás con el bloque B según el campo R12). No toques nada aún.
2. Tras la aprobación del dueño, implementa en el orden A → B → C → D, todo en `m0/t0.1-foundations`.
3. Corre ambos arneses y adjunta la salida completa. Corre las pruebas de la evidencia 10 (ambas formas) y 11.
4. Lista lo que no pudiste verificar (`09_no_verificado.txt`).
5. Tres intentos fallidos = detenerse y reportar.
6. Reporte final: hash del commit, hallazgos cerrados por código (M2, M3, m-2.1…m-2.8, notas), lo que quedó abierto y por qué, ruta de cada evidencia. Sin resúmenes de "todo verde": el revisor reproduce.

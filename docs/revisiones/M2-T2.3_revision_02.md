# Revisión 02 — M2-T2.3 Agarrar, soltar, lanzar y amarrar (ronda 2 exprés: la carga cae como carga)

**Fecha:** 2026-09-15 · **Revisa:** Claude · **Revisado:** rama `m2/t2.3-cargo`, HEAD `f2b4430` (7 commits sobre `1f689f4`: docs del revisor ×2, `70ffaef` amortiguador solo hacia arriba + puntos de aparición, `b4d51bc` padre original en `unstrap()`, `b2dfa57` el test de caída, `b7bdfca`/`f2b4430` evidencia); `main` en `6df7824` · **Pliego:** `docs/planes/M2-T2.3_ronda2_prompt.md` + plan del ejecutor aprobado con dos precisiones

## Veredicto: PASA. T2.3 está apta para tu gate con carga, y tras el gate, para mergear.

Solté una caja yo y cayó como una caja: 1,25 m en 0,50 s, que es exactamente la caída libre, con el bus parado y a 34 km/h. Sobre el código de la ronda 1 la misma caja **no había tocado el piso a los 2,5 s**, bajando a 0,49 m/s. Las cuatro cajas de aparición descansan donde el pin dice, al milímetro, desde el primer tick. Desamarrar devuelve al nodo `Cargo`. Los cuatro hallazgos de la revisión 01 están cerrados con consecuencia medida, no con bandera.

## Reproducción del revisor (Windows, 2026-09-15 17:30–17:56 UTC)

| Prueba | Resultado |
|---|---|
| `tools/run_tests.ps1`, tres corridas seguidas, con el escenario de red | código **0, 0, 0**; 180 tests descubiertos y verdes las tres veces; red `pass` las tres (1 anfitrión + 3 clientes) |
| `tools/run_tests.sh`, con el escenario de red | código **0**; 180 tests; red `pass` |
| Sonda `unsafe_*=2` (los cuatro avisos a error, arnés sin red, worktree de HEAD; `project.godot` restaurado) | código **0**; 180 tests |
| `run_demo` (`--fixed-fps 60`) | `lap_completed t=27.3s waypoints=17 max_speed_kmh=64.6 min_upright=0.97`, idéntico |
| Corrida **con ventana** en tu árbol (mi sonda: agarrar y soltar), log en `user://logs/godot.log` | 7 líneas, **cero ERROR, cero WARNING**; la caja toca el piso en el tick +30 (0,50 s), rebote 0,002 m: los mismos números que sin ventana |
| Alcance (`git diff 1f689f4 HEAD` sin docs) | 7 archivos, +116/−28: `package.gd`, `playground.gd` (los cuatro puntos), el test de escena (pines de posición), `package_test.gd`, `cargo_ride_test.gd` (anclajes `left_5`/`right_0` + aserción de separación), `restraint_test.gd`, `DECISIONS.md` (D85). **`bus.gd` y `tuning.json` intactos** |
| Estáticos | `:=` 0 · `@warning_ignore` 0 · **180** `func test_` (179 + la caída) · `restraint_test.gd` exactamente en 400 líneas (R8) |
| Árbol | limpio en `f2b4430` |

### Sondas propias sobre la escena real (worktree de HEAD), antes y después

| Medición | Ronda 1 (`1f689f4`, medido por mí hoy) | Ronda 2 (`f2b4430`) |
|---|---|---|
| Caja soltada de la mano con el bus parado (1,25 m de caída) | a los **150 ticks (2,5 s) aún no ha tocado el piso**, baja a 0,49 m/s | toca el piso en el tick **+30 (0,50 s)**; caída libre teórica 0,50 s; rebote 0,002 m |
| Caja soltada a **34 km/h** | — | toca en el tick **+28**, aterriza dentro, sin rebote, queda a `v_rel` 0,04 m/s |
| Las dos cajas "sobre los racks" | nacen en `z = ±1,5`, fuera del rack, y caen: `y = 1,0 → −0,40` y `→ −0,15` (camilla) | nacen en `(∓0,875, 1,0, ∓0,4)` y **no se mueven ni un milímetro** del tick 5 al 400 (`v_rel` 0,00); las dos del piso igual |
| Lanzar hacia el frente | 3,17 m | 3,17 m, aterriza a 2,6 m, dentro (la gracia de 30 ticks cubre el vuelo: sin cambio, como esperaba r1.6) |
| Amarrar y desamarrar | tras desamarrar, padre `Playground` | padre **`Cargo`**, el original |

**Mi verificación del "falla primero"** con `package.gd` de `1f689f4` en el worktree: la suite unitaria falla (`test_vertical_damping_force_is_pure_vertical`, código 100) y mi sonda de consecuencia da la fila de arriba. La misma arruga de siempre: gdUnit en mi worktree descubrió 9 casos y **no ejecutó el test de caída nuevo**, el último del archivo; en tu árbol el arnés cuenta 180 y la evidencia 03 muestra `contact_tick=123` sobre el código viejo. Por eso el falla-primero lo verifiqué con mi sonda, no con su test. Véase r2.1.

Las dos capturas nuevas son de verdad y distintas: `05_strapping.png` (objeto LFS `cc21e70c…`) enseña la caja en la mano frente a la tapa del rack con el anclaje delante; `10_drop_midair.png` (`56927a3f…`) la caja gris a media altura entre los racks.

## Lo que hay que reconocerles

- **Arreglaron la causa, no el síntoma**: la función pura devuelve cero para `v_rel` hacia abajo, y el docstring y D85 cuentan el porqué con el número (0,49 m/s = m·g/k). El test unitario muerde en las dos direcciones.
- **Volvieron a medir lo que el cambio tocaba** sin que hiciera falta insistir: (c'') con alturas contenidas (< 0,4 m) y caídas a gravedad, (d) golpeando a 3,65 m/s. Las tablas anteriores siguen en la 03.
- **Los puntos de aparición ahora tienen pin de posición, no de conteo**, y el test de la vuelta afirma la separación entre cajas y anclajes que pedí.
- **Declararon dos descuidos propios** (el test que se quedó fuera de su commit; la corrida 1 lanzada antes del recorte de comentarios) en vez de esconderlos.

## Hallazgos

**r2.1 — proceso, y ya son tres veces.** gdUnit, en un worktree recién importado, **no descubre el último caso de una suite**: ronda 4 de T2.2 (1 de 2), ronda 5 (3 de 4), y hoy (9 de 10 en `package_test.gd`). En tu árbol nunca ha pasado. No sé la causa y no la voy a adivinar, pero el riesgo es concreto: **un clon fresco (otra máquina, macOS por D42, un CI) puede correr menos tests en silencio**, y el arnés solo exige "al menos 3". **Arreglo barato, paso 0 de la siguiente tarea:** el arnés afirma el **conteo exacto esperado**, una constante que cada tarea actualiza (hoy 180), y falla si difiere. Y una línea en BACKLOG para investigar la discrepancia del worktree.

**r2.2 — menor.** `restraint_test.gd` está en 400 líneas justas. El siguiente test ahí obliga a partir la suite; mejor partirla en el paso 0 de la siguiente tarea que recortar comentarios otra vez.

**r2.3 — observación.** El test de caída corre sobre una escena de fixture (bus más piso estático), no sobre el Playground; mide la consecuencia correcta y falla primero, pero el interior real lo cubre solo el pin de aparición. Suficiente hoy; anotado.

**Siguen abiertos:** D88 (tu decisión sobre `M2-GATE`), daño por impacto (M3), 120 Hz sin explicar (BACKLOG), macOS (D42).

## Estado de la rama

`main` en `6df7824`. La rama `m2/t2.3-cargo` está publicada con 23 commits encima: T2.3 completa en dos rondas.

## Qué hace falta para cerrar

**Dueño, tu gate, ahora sí, cinco minutos:** F5, entra, apunta a una caja y E para agarrarla, llévala por el pasillo, **suéltala con clic derecho y mírala caer**, agárrala otra vez y lánzala con clic izquierdo, amárrala manteniendo E junto a la tapa de un rack, desamárrala con E, siéntate, conduce sobre los saltos con carga amarrada y suelta, levántate en marcha, agarra una caja con el bus rodando. Si algo se ve raro, dilo con el segundo.

**Después del gate:** commit de esta revisión en la rama, `git merge --no-ff m2/t2.3-cargo` en `main`, push. Yo lo hago si me lo pides.

**Revisor:** con tu respuesta a D88, el plan de `M2-GATE` (dos instancias, réplica mínima de tripulantes y carga, cinco minutos a 80 km/h) o su alternativa.

# Revisión 01 — M0-T0.1 Fundaciones

**Fecha:** 2026-09-08 · **Revisa:** Claude · **Revisado:** rama `m0/t0.1-foundations`, HEAD `62fa3a3` (main `633d4ba`) · **Pliego:** `docs/planes/M0-T0.1_plan.md` (v1.0; v1.1 incorpora las correcciones de esta revisión)

## Veredicto: NO PASA el gate. Vuelve al ejecutor para una ronda 2.

Lo que se puede reproducir, se reprodujo y pasa. Lo que bloquea no es el código de Godot: es (1) el reglamento de agentes incompleto porque el ejecutor nunca recibió el maestro, (2) un arnés cuya protección contra "0 tests = éxito" es burlable, y (3) la mitad del repo es la suite de auto-tests de gdUnit4, que no forma parte del addon distribuido y mete 154 nombres de clase globales en el juego.

Tres de los defectos nacen del plan, no del ejecutor. Están marcados como **[defecto del plan]** y corregidos en el plan v1.1.

## Reproducción del revisor (máquina Windows, 2026-09-08 13:38 UTC)

| Prueba | Resultado |
|---|---|
| `tools/run_tests.ps1` | código 0, import ×2 con código 0, 3 tests descubiertos y verdes |
| `tools/run_tests.sh` bajo Git Bash | código 0, 3 tests |
| Runner sobre carpeta sin tests (`-a res://src`) | `No test cases found, abort test run!`, **código 0, no escribe reporte nuevo** → el guardia de ≥3 leería el reporte anterior y aprobaría |
| `--editor --quit` con ventana, luego `git status --porcelain` | árbol limpio: normalización idempotente confirmada |
| Integridad de `addons/gdUnit4/` contra el árbol del tag v6.2.1 (`08ffc7c6`) vía API de GitHub | 604/604 archivos presentes; diferencias solo por punteros LFS (13 binarios), normalización LF (1 `.tscn`) y sidecars generados (462 `.uid`, 10 `.png.import`) |

Además: 6 revisores independientes por dimensión + 6 verificadores adversariales (12 agentes, solo lectura). Todo hallazgo de abajo fue confirmado contra el archivo citado.

## Hallazgos

### Bloqueante

**B1 — `AGENTS.md` no contiene R1–R14 textuales.** 5 reglas vacías (R1, R2, R4, R12, R13) y 9 reconstruidas con pérdidas: R3 omite "prohibido C#, GDExtension o addons nuevos sin ADR"; R8 omite el tope de 60 nodos por escena; R7 omite (a)(b)(c). §0.4 y §0.8 son "PENDIENTE". `docs/RUTA_FRAGIL_documento_maestro_v0.2_godot.md` y `docs/CONTEXTO_RUTA_FRAGIL_v0.2.md` son stubs de 360 y 337 bytes. `DECISIONS.md` ADR-001..008 y D1–D29 "PENDIENTE"; `BACKLOG.md` M1–M8 "PENDIENTE".
*Causa raíz:* el ejecutor no recibió el texto del maestro ni del briefing. Lo declaró con honestidad en su reporte.
*Impacto:* `CLAUDE.md` → `AGENTS.md` es el reglamento que carga cada sesión futura; hoy una sesión operaría sin R12 (confirmar antes de borrar) ni R13 (test por sistema).
*Fix:* el dueño entrega los originales al ejecutor, o autoriza a Claude a escribirlos desde su copia del chat. Después: §0.3, §0.4 y §0.8 textuales en `AGENTS.md`; D1–D29 y ADR-000..008 en `DECISIONS.md`; M1–M8 en `BACKLOG.md`. Criterio: `diff` vacío contra §0.3.

### Mayores

**M1 — El arnés acepta un `results.xml` viejo.** `run_tests.ps1:134-136` y `run_tests.sh:123` eligen el reporte más reciente sin atarlo a la corrida actual. gdUnit4 (`GdUnitTestCIRunner.gd:393-398`) sale 0 sin escribir reporte cuando no descubre tests. Reproducido por el revisor. La evidencia 05 del propio ejecutor lo muestra: tras un 105 leyó `report_5` (4 tests) de la corrida anterior.
*Fix:* antes del paso 3, borrar `reports/` (o registrar la hora de inicio y exigir `results.xml` más nuevo). Además, fallar si la salida del runner contiene `No test cases found`. Evidenciar con una corrida con `tests/` vacío que **falla**.

**M2 — `addons/gdUnit4/test/` vendorizada (559 archivos, 50 % del repo).** Contiene 154 `class_name` globales (`City`, `Person`, `BaseTest`, `Comparator`, `T2`, `T3`…) ya registrados en `.godot/global_script_class_cache.cfg`; 1 `.scn` binario; 22 `.resource`; 7 `.cs`; `update.zip`; 2 objetos LFS; y un segundo reglamento de agentes (`test/AGENTS.md`, `test/CLAUDE.md`) que contradice el del proyecto (permite `:=`, `class_name` en tests). La distribución oficial (zip/AssetLib) excluye `test/` por `export-ignore`. **[defecto del plan]**: el paso 4 dijo "clonar y copiar `addons/gdUnit4/`" sin excluir `test/`.
*Impacto:* colisiones garantizadas con clases futuras del juego (`City`, `Person`), import más lento, ruido en autocompletado, R14 violado, R3 (C#) en el árbol.
*Fix:* borrar `addons/gdUnit4/test/` (**R12: requiere el "sí" del dueño**), `--import` ×2, `run_tests`, recontar `.uid`, actualizar D32, `CREDITS.md` y evidencia 08 a "carpeta `addons/gdUnit4/` sin `test/`, igual que la distribución oficial". Verificar que la caché global ya no contiene `res://addons/gdUnit4/test/`.

**M3 — La "excepción R14" de `simple_scene.scn` no se sostiene.** El argumento "quitarlo rompería la paridad byte-exacta" es falso: el objeto en el árbol ya es un puntero LFS, y se agregaron 462 `.uid`. R14 no tiene cláusula de excepción; una excepción real sería una decisión numerada en `DECISIONS.md`, no una frase en un log. Se resuelve con M2.

**M4 — Falta la lista de criterios NO verificados (R7, plan §7 último punto).** No existe en `docs/evidencia/M0-T0.1/`. Mínimo: (1) `run_tests.sh` en la Mac con hash `ed1daf0bf`; (2) remoto + LFS + primer push; (3) carga del plugin gdUnit4 en el editor con ventana (dock visible), sin evidencia; (4) el guardia de ≥3 nunca ejercitado; (5) `inferred_declaration=2` nunca ejercitado (la prueba 05 usó `var x = 1`, no `:=`). *Fix:* `09_no_verificado.txt`.

**M5 — Pendientes de la ejecución fuera de `BACKLOG.md` (R10).** La sección "Pendientes" es copia byte-exacta del §9 del plan. Faltan: rellenar placeholders (B1); corrección del plan por formato de versión (`4.7.2-stable (official)`); excepción `.scn` (M3); 17 `.gd` sin `.uid`; los dos `ERROR` por `--remote-debug` (m1); `reports/` reimportado (m2).

### Menores

**m1 — Dos líneas `ERROR` en cada corrida** por `--remote-debug tcp://127.0.0.1:0` ("remote port number must be between 1 and 65535", "Unable to connect"). **[defecto del plan]**: R7(a) "sin errores en consola" no puede cumplirse literalmente con ese comando. *Fix:* probar sin `-d --remote-debug` (sin `-d` no existe el depurador interactivo local, que era lo que el puerto 0 neutralizaba); si los errores de script siguen saliendo con backtrace, quitar ambos flags; si no, documentar las dos líneas como salida esperada en `AGENTS.md`.

**m2 — `reports/` es escaneado e importado por Godot en cada `--import`** (reimport de `logo.png` visible en 03/04; `.godot/imported/` acumula 9 copias). *Fix:* el arnés crea `reports/.gdignore` si no existe antes de correr.

**m3 — `README.md`:** la receta Windows no incluye `git lfs install` ni `core.autocrlf input` (solo la de macOS); "macOS ≥ 11" es solo Intel, Apple Silicon exige macOS 13 (**[defecto del plan]**, verificado en system_requirements de 4.7); el comando documentado `tools/run_tests.ps1` no es el verificado (`powershell -ExecutionPolicy Bypass -File tools/run_tests.ps1`), y con política `Restricted` la forma corta falla.

**m4 — PowerShell 5.1:** sin `$ErrorActionPreference` fijado; `2>&1 | Out-String` (`run_tests.ps1:94`) convierte stderr nativo en `NativeCommandError`; lectura de `tools/godot_bin.local` sin guardia de vacío/BOM/comillas (`:51`; `.sh:46`).

**m5 — Evidencias:** 03/04/05 son anteriores a la normalización del paso 7 (el revisor sí corrió post-normalización y pasa, así que no bloquea); `03a` lleva la cabecera de `03`; `07` resume en vez de volcar (`git status` y listado de `.uid`); corridas `report_6` y un import entre 04 y 05 sin evidencia; `06` compara contra `/tmp/project.godot.pre_editor`, no reproducible.

**m6 — R9:** `Write-Step` y `Stop-WithError` en PascalCase. Convención PowerShell contra la regla del proyecto. Renombrar o registrar la excepción en `AGENTS.md`.

**m7 — Cabecera D35 mentirosa en los stubs:** dice "copia reconstruida desde chat" y no se reconstruyó nada. D35 en `DECISIONS.md` afirma algo que no ocurrió.

**m8 — ADR-000 "solo parches 4.7.x" vs. enforcement clavado a 4.7.2 + hash en 4 archivos** (`smoke_test.gd:9,12`, `run_tests.ps1:85`, `run_tests.sh:73`). Cuando llegue 4.7.3 hay que tocar los cuatro y ningún documento lo dice. Documentar en `AGENTS.md` la lista de archivos a subir de versión.

### Notas

- `addons/gdUnit4/src/asserts/CLAUDE.md` sobrevive al borrado de `test/`: es una guía interna de gdUnit4 que un agente podría cargar por error. Documentar en `AGENTS.md` que los `CLAUDE.md`/`AGENTS.md` bajo `addons/` no aplican al proyecto.
- `.gitattributes` manda `*.scn`/`*.res` a LFS: red de seguridad heredada del plan; R14 los prohíbe. Mantener o quitar, pero anotar.
- `addons/gdUnit4/src/dotnet/GdUnit4CSharpApi.cs` es parte de la distribución oficial y queda tras la limpieza: anotar en D32 como archivo inerte en build estándar.
- `test_rendering_method_is_forward_plus` prueba el default del motor (la clave se borró en la normalización, como el plan aceptó); no puede fallar en escritorio. Aceptable; el test de Jolt sí verifica un valor no-default.
- Código 101 de gdUnit4 significa "orphans", no "warnings" (`GdUnitTestCIRunner.gd:468-476`). Etiqueta heredada del plan.
- CREDITS: Jolt no lleva "(c)" en su licencia. Cosmético.

## Checklist §8 — PASA / FALLA / NO PROBADO

**Alcance y reglas**
- FALLA — Ningún archivo fuera del alcance / ningún addon salvo gdUnit4: el addon sí, pero con `test/` (M2).
- FALLA — `addons/gdUnit4/` del tag v6.2.1 sin arrastres: hash correcto y árbol íntegro, pero incluye `test/` (M2).
- PASA — Scripts propios ≤400 líneas (182, 157, 22), inglés; snake_case salvo dos funciones PowerShell (m6).
- FALLA — Sin TODOs silenciosos (M5).
- FALLA — Sin `.scn`/`.res` binarios (M3).

**`project.godot`**
- PASA — Claves exactas del paso 3 tras normalización; `Jolt Physics`; `d3d12`.
- PASA — Sin `directory_rules`, sin `treat_warnings_as_errors`.
- PASA — `[editor_plugins]` y `[gdunit4]` correctos.
- PASA — Segundo arranque del editor sin diff (reproducido).

**Tests y `run_tests`**
- PASA — `tests/smoke_test.gd`: tres tests, tipado completo, sin `@warning_ignore`; el ejecutor corrigió el formato de versión del plan y añadió el pin por hash.
- FALLA — `run_tests.*` exige ≥3 tests y sale 0 solo si todo pasó: la lógica existe pero es burlable con un reporte viejo (M1).
- PASA — Evidencias 04 y 05 coherentes: detecta fallos (100) y código sin tipo (105); D34 activa para `untyped_declaration`. `inferred_declaration` sin ejercitar (M4).
- PASA — Código de `--import` reportado tal cual (0 en todas las pasadas; el bug de apagado no se reprodujo).

**Git**
- PASA — `main` con un solo commit; rama con el resto; mensajes referencian la tarea.
- PASA — `*.uid`/`*.import` versionados; `.godot/`, `reports/`, `tools/godot_bin.local` ignorados.
- PASA — `.gitattributes` con `text=auto eol=lf`, pins LF, LFS sin `*.svg`, comentario D36.
- PASA — Bits ejecutables en `runtest.sh` y `run_tests.sh`.

**Documentos**
- FALLA — `AGENTS.md` con R1–R14 textuales (B1).
- FALLA — `DECISIONS.md` D1–D39 y ADR-000..009 (D30–D39 sí coinciden con el plan; D1–D29 y ADR-001..008 vacíos).
- PASA — `CREDITS.md` con las tres licencias. `README.md` con hash y recetas (con m3). `BACKLOG.md` con §9 (sin los pendientes propios, M5).
- FALLA — Maestro y briefing en `docs/` (stubs).

**Solo el dueño**
- NO PROBADO — `run_tests.sh` en la Mac.
- NO PROBADO — Remoto Git con LFS y primer push. Nota: ya existen 13 punteros LFS (10 objetos únicos) por los PNG del addon; el remoto debe tener LFS antes del primer push.

## Defectos del plan v1.0 (responsabilidad del revisor, corregidos en v1.1)

1. Paso 5: `Engine.get_version_info()["string"]` es `4.7.2-stable (official)` (guion y paréntesis), no `4.7.2.stable`. El ejecutor lo detectó y corrigió bien.
2. Paso 4: "copiar solo `addons/gdUnit4/`" tras `git clone` arrastra `test/`; la distribución oficial la excluye. Debía decir "sin `test/`".
3. Paso 6: `--remote-debug tcp://127.0.0.1:0` produce dos `ERROR` por corrida.
4. Sección 6: macOS mínimo es 13 en Apple Silicon.
5. Sección 7: no pidió evidencia de una corrida con 0 tests (la única que ejercita la corrección #1 del maestro).
6. Paso 1: la receta Windows del README debía incluir `git lfs install` y `core.autocrlf input`.

## Acciones para la ronda 2

**Dueño (bloquean la ronda):**
1. Entregar al ejecutor el texto del maestro y del briefing, o autorizar a Claude a escribirlos desde su copia del chat.
2. Confirmar por R12 el borrado de `addons/gdUnit4/test/`.

**Ejecutor:** M1, M2 (tras confirmación), M4, M5, m1–m8, y recapturar 03 post-normalización con volcados completos en 07. Seguir el plan v1.1.

**Revisor:** plan v1.1 publicado junto con esta revisión.

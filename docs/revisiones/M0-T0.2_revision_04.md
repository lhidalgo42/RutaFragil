# Revisión 04 — M0-T0.2 GameConfig + TuningTable (ronda 4 exprés, cierre)

**Fecha:** 2026-09-10 · **Revisa:** Claude · **Revisado:** rama `m0/t0.2-game-config`, HEAD `8ff1378` (`e59591a` docs del revisor, `62d45a8` ronda 4, `8ff1378` evidencia 07 post-commit; `main` intacto en `e42f80b`) · **Pliego:** plan v1.1 + revisión 03 + prompt de ronda 4

## Veredicto: APTA para el gate del dueño. Los siete ítems de la revisión 03 están cerrados, la sonda `unsafe_*=2` vuelve a pasar y no hay cambios de producto. Queda solo lo que únicamente el dueño puede hacer (Inspector, checklist §9, merge).

La ronda 4 tocó 14 archivos, todos en `tests/`, `docs/` y `DECISIONS.md`. `src/`, `tools/`, `addons/`, `AGENTS.md`, `project.godot` y `data/` no cambiaron respecto a `17a788a`; 0 borrados. El código de producción de T0.2 es el mismo que revisé en la ronda 3 y que ya estaba en verde; lo que esta ronda arregla es la propiedad "cero accesos inseguros" (r3.1) y la precisión de la evidencia y los documentos.

## Reproducción del revisor (Windows, 2026-09-10 18:09–18:10 UTC)

| Prueba | Resultado |
|---|---|
| `powershell -ExecutionPolicy Bypass -File tools/run_tests.ps1` | código 0; `Overall Summary: 61 test cases | 0 errors | 0 failures`; `Executed test cases : (61/61)`; `Discovered tests: 61`; `SUCCESS` |
| `bash tools/run_tests.sh` | código 0; mismos 61/61 y `SUCCESS` |
| Únicas líneas `WARNING`/`ERROR` del log | dos, las que los tests MR3 provocan a propósito: `WARNING: REJECTED mod:bad.json: key 'starting_money': expected an int, got String` y `ERROR: data load: 0 applied, 0 warning(s), 2 error(s)` |
| Sonda: los cuatro `gdscript/warnings/unsafe_*` de 1 a 2 en `project.godot` (`git diff --stat`: 4 líneas), `tools/run_tests.ps1`, `git checkout -- project.godot` | **código 0, 61/61, `SUCCESS`** (en la revisión 03 daba 105 con 0 tests descubiertos). `project.godot` revertido; `git status --porcelain` vacío |
| Estático | 0 `:=`, 0 `@warning_ignore`, 0 `load("res://src…` en tests, 61 `func test_` en 8 suites (6+6+2+17+6+7+14+3); el único "alphabetical" que queda en `tests/` es el comentario "never case-insensitive alphabetical" de `json_file_test.gd:65` |
| Git | `main` = `e42f80b`; `git diff --stat 17a788a HEAD -- src tools addons AGENTS.md project.godot data` vacío; árbol limpio |

## Estado de los ítems de la revisión 03

| Ítem | Estado | Verificación |
|---|---|---|
| r3.1 `load(...).new()` en MR3 | CERRADO | `const AUTOLOAD_SCRIPT: GDScript = preload("res://src/autoloads/game_config.gd")` y `auto_free(AUTOLOAD_SCRIPT.new())` en ambos tests (`game_config_autoload_test.gd:13,69,86`). Sonda `unsafe_*=2` reproducida por el revisor: código 0. |
| r3.2 cabecera 02 "primera línea" | CERRADO | La 02 dice "summary COMPLETO" y explica que `is_push_error` solo admite cadena exacta o `any()`. |
| r3.3 cabeceras 03/04/05 | CERRADO | "HEAD 0645a87; `data/` no cambió desde 6656a8c" en las tres, con el comando que lo demuestra (`git diff 6656a8c..0645a87 -- data/` vacío). |
| r3.4 orden por bytes sin test | CERRADO | `test_list_json_files_is_sorted_and_filtered` escribe `a.json` **antes** que `B.json` y afirma `["B.json", "a.json"]`: 0x42 < 0x61 distingue orden por bytes de alfabético insensible a mayúsculas, y el orden de escritura no puede producir la expectativa. `test_two_mods_apply_in_alphabetical_order` → `test_two_mods_apply_in_byte_order`; comentarios de `game_data_loader_test.gd:4,95` corregidos. |
| r3.5 D46 y esquema §7 | CERRADO | D46 con `reload(base_dir: String = "res://data", mods_dir: String = "user://mods")` y la cláusula "los argumentos existen para pruebas; el juego usa los defaults; ronda 3"; cabecera de la tabla con "D46 en la ronda 3"; esquema §7 con la misma firma que §1. |
| r3.6 docstring de la suite | CERRADO | Describe los tests del runner y los dos MR3 con instancia propia (`auto_free`, fuera del árbol, `_ready` no corre) y directorios inyectados. |
| r3.7 (opcional) | CERRADO completo | MR3-a afirma `last_report.is_ok()` verdadero; MR3-b `warnings` vacío; ambos con `assert_bool(report_v is DataLoadReport).is_true()` **antes** del `if`, así que el `if` no puede dejar el test vacuo. `assert_str(report.errors[0]).starts_with(...)` en el test de base truncada. Nuevo `test_mod_with_only_unknown_sections_applies_with_warning`: `is_ok()`, 1 aviso con texto exacto `mod:mystery.json: unknown section 'workshop' ignored` (coincide con el formato de `game_data_loader.gd:160`), `applied_files` de 3 con el mod en `[2]`. Nota `:35/:24 → :37/:26` en la 09; la 08 remite a ella y actualiza `:63/:74 → :70/:87`, que coinciden con HEAD. |

## Evidencia de la ronda 4

- **02** recapturada a las 17:30:49 y 17:31:10 UTC con ambos arneses (61 tests, 8 suites), **antes** del commit de código `62d45a8` (17:37:43 UTC) y sobre el árbol final: los nombres nuevos (`test_two_mods_apply_in_byte_order`, `test_mod_with_only_unknown_sections_applies_with_warning`) aparecen en el listado ejecutado y los backtraces citan `:70/:87`, las líneas vigentes. La sección "Sonda unsafe_*=2" incluye el volcado completo con `Overall Summary: 61`, `gdUnit4 exit code 0` y `SUCCESS`.
- **07** en dos partes: pre-commit dentro de `62d45a8` con `git diff main..HEAD -- project.godot` mostrando solo el bloque `[autoload]` de la ronda 1; post-commit en `8ff1378` con `git status --short` vacío y `git log main..HEAD` encabezado por `62d45a8`.
- **08** actualizada (17:29 UTC): 17 tests en `game_data_loader_test`, ronda 4 sin comportamiento nuevo sin probar, sonda registrada.

## Hallazgos de la ronda 4 (ninguno bloquea)

**r4.1 — evidencia, menor.** El volcado de la sonda en la 02 no incluye el `git diff -- project.godot` tomado **durante** la corrida. Con cero accesos inseguros, la salida de una corrida con `unsafe_*=2` es idéntica a la de una corrida normal, así que el volcado no se autoevidencia: la prueba real es la reproducción del revisor (tabla de arriba, código 0 con el diff de 4 líneas confirmado antes de correr). Para futuras sondas, capturar el diff de `project.godot` dentro del volcado. Sin acción para T0.2.

**Del revisor.** El criterio del prompt de ronda 4 "`git diff main..HEAD -- project.godot` sin cambios" era impreciso: el bloque `[autoload]` es legítimo (D43, ronda 1) y forma parte de T0.2. El ejecutor lo interpretó correctamente y lo documentó en la 07. Corrijo el criterio aquí para que no se lea como incumplimiento.

## Checklist §9 (plan v1.1) — estado final

- **Reglas y alcance:** PASA todo. R3: PASA con la configuración del proyecto **y** con la sonda `unsafe_*=2` (reproducida). R6: la ronda 4 no salió de los archivos listados en el prompt. R13: PASA.
- **Datos:** PASA (sin cambios desde `6656a8c`; ida y vuelta byte-estable verificada en la revisión 03).
- **Loader y autoload:** PASA (sin cambios de producción en la ronda 4).
- **Tests y evidencia:** PASA. Reservas r3.1–r3.4 cerradas; r4.1 es una recomendación.
- **Documentos:** PASA. Reservas r3.5–r3.6 cerradas.
- **Solo el dueño:** NO PROBADO — Inspector mostrando `data/tuning.tres` (39 campos de §8.1, `base_pay` y `shop_prices` como diccionarios tipados) **sin guardar**.

## Qué hace falta para cerrar T0.2

**Dueño (gate §0.7):**
1. Abrir el proyecto en el editor, abrir `data/tuning.tres` en el Inspector y comprobar los valores de §8.1 **sin guardar** (un guardado accidental cambiaría el `.tres` y la suite de deriva lo detectaría; si pasa, `git checkout -- data/`).
2. Recorrer el checklist §9 del plan v1.1 con esta revisión al lado.
3. Decidir si `docs/revisiones/M0-T0.2_revision_04.md` se commitea en la rama antes del merge (como las revisiones 01–03).
4. `git merge --no-ff m0/t0.2-game-config` en `main`; push.

**Ejecutor:** nada pendiente en T0.2.

**Revisor:** plan de M0-T0.3 (Playground) cuando el dueño lo pida.

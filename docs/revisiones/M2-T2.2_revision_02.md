# Revisión 02 — M2-T2.2 Personaje en primera persona (ronda 2, arreglo bloqueante)

**Fecha:** 2026-09-14 · **Revisa:** Claude · **Revisado:** rama `m2/t2.2-crew`, HEAD `1a48213`; `main` en `3c8eca8` · **Pliego:** revisión 01 + prompt de ronda 2

## Veredicto: PASA. El dueño puede jugar, y esta vez lo comprobé yo en vez de deducirlo.

El bloqueante está cerrado y, más importante, las dos redes de seguridad que pedí existen y **disparan de verdad**: lo verifiqué borrando una acción del mapa de entrada y viendo fallar el arnés por partida doble.

## Reproducción del revisor (Windows, 2026-09-14 02:43–02:55 UTC)

| Prueba | Resultado |
|---|---|
| `tools/run_tests.ps1`, tres corridas seguidas | código **0, 0, 0**; 123 tests, cero fallidos |
| `tools/run_tests.sh` (el que no corrieron en esta ronda) | código 0, 123 tests; **sin divergencia** con la forma de PowerShell |
| Corrida con ventana de 25 s, leyendo `user://logs/godot.log` | **3 líneas en total, cero ERROR, cero WARNING**. Antes eran 11.232 líneas de error |
| Acciones registradas | las catorce: siete de conducción y cámara, seis de andar, más `interact` |
| Árbol | limpio |

**La prueba que más quería hacer: quitarle una acción al juego y ver si el arnés se da cuenta.** Borré `walk_left` de `project.godot` y corrí el arnés:

- `tests/static/input_actions_test.gd > test_every_action_read_in_src_is_registered` **FALLA**.
- `tests/crew/crew_input_test.gd > test_walk_left_and_right_actions_give_the_right_sign` **FALLA** también, por su cuenta, porque la tecla deja de producir movimiento.
- Arnés en código 100. Restaurado el archivo, todo vuelve a verde.

Dos redes independientes cazan el mismo fallo: una comprueba que la acción está declarada, la otra que **pulsarla hace algo**. Es exactamente la estructura que faltaba, y el conjunto de tests lo confirma: `test_interact_action_seats_and_lifts_the_crew` prueba el cable entero, y `test_toggle_nearest_seat_out_of_reach_does_nothing` prueba el caso negativo.

## Hallazgo: mi dato era descuidado, y la corrección de ellos también

Les dije que `Input.is_action_just_pressed` funciona en headless. Ellos midieron que un nodo en `_physics_process` no lo ve nunca, con cuarenta pulsaciones y cero observadas, y por eso detectan el flanco a mano. Volví a medirlo y **ninguna de las dos afirmaciones es correcta**:

| Situación | Resultado medido |
|---|---|
| Justo después de `action_press`, en la misma llamada | **falso** |
| Desde el manejador de la señal `physics_frame` | falso |
| Desde el `_physics_process` de un nodo del árbol, pulsando en un tick y soltando en el siguiente | **verdadero, 20 de 20** |

O sea: el flanco no está disponible en la misma llamada, pero **sí llega a un nodo** si la pulsación y la suelta caen en ticks de física distintos, que es justo el patrón de taps realistas que ellos acabaron usando. Mi afirmación original venía de una llamada cuyo "verdadero" era estado residual de una pulsación anterior; no la comprobé bien. La suya generaliza de más.

Consecuencia práctica: ninguna. La detección manual del flanco en `crew_input.gd` funciona y es defensiva frente a distintas tasas de refresco. Pero su justificación escrita es falsa, y conviene cambiarla por la medición de arriba antes de que alguien la cite como hecho.

## Lo que está bien, y hay bastante

- **Se corrigieron a sí mismos.** Descubrieron que sus propios tests contaminaban el grupo de la tripulación, que un crew sin liberar quedaba en el árbol y el siguiente se enganchaba al viejo, y que **por eso una cota de la ronda 1 estaba medida con un personaje residual en la trayectoria del bus**. Volvieron a medir limpio, 0,197 m, y ajustaron la cota con la razón escrita en el test. Corregir una evidencia propia ya entregada es lo más difícil de pedir y lo hicieron sin que nadie se lo pidiera.
- **La sonda de accesos inseguros les cazó un error propio** del arreglo de la gravedad, y lo cuentan en la evidencia en vez de esconderlo.
- Leyeron el registro donde había que leerlo, en el archivo y no en la consola.
- Declararon lo que queda sin probar: el salto no tiene test de efecto porque depende del flanco.

## Qué hace falta para cerrar

**Dueño:** tu gate, y ahora sí es alcanzable. Pulsa F5, camina con WASD hasta la puerta lateral, entra, pulsa E en el asiento del conductor, conduce, pulsa E para levantarte y sal. Tres minutos.

**Ejecutor:** solo corregir la justificación del flanco con la medición de arriba. Puede ir en el paso 0 de T2.3.

**Revisor:** el plan de M2-T2.3, agarrar, soltar, lanzar y amarrar, que trae el gate duro de M2 con dos instancias.

## Nota de estado

`main` está en `3c8eca8` e incluye la biblia de arte. La rama `m2/t2.2-crew` está publicada en el remoto y contiene T1.1, T2.1 y T2.2, más el commit `eb72fb6` de esa misma biblia, que al mergear se resolverá solo por ser idéntico.
